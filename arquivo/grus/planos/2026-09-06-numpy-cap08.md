# Capítulo 8 (Machine Learning) — plano da reescrita em numpy

**Data:** 2026-09-06 · **Branch:** `reescrita-numpy` · **Grus:** capítulo 11
**Spec:** `docs/superpowers/specs/2026-09-06-reescrita-numpy-design.md`
**Instruções:** `docs/superpowers/plans/2026-09-06-numpy-00-instrucoes.md`

Este capítulo **não constrói modelo nenhum** — constrói o vocabulário com que os oito
seguintes são julgados. Por isso a reescrita é pequena em superfície e delicada em dois
pontos: as **duas figuras**, que dependem de sorteio e mudam de desenho; e o **`split_data`**,
que é o contrato que os capítulos 9, 10 e 13 importam. As quatro métricas não são tocadas:
são funções escalares sobre quatro inteiros, e forçar numpy nelas seria decoração.

---

## 1. Inventário

### `index.qmd` (52 linhas) — **nada muda**

Sem chunk executável. Nenhuma frase menciona listas, Python puro, `zip`, laço ou lentidão.
Os sete objetivos (linhas 27–33) falam de conceitos (*overfitting*, matriz de confusão, viés,
variância, atributos) e continuam verdadeiros palavra por palavra. A tabela de seções, o
callout de correspondência, o link do Colab e as leituras adicionais ficam como estão.

Conferido com `grep -niE "python puro|lista|laço|zip|lento|array|numpy"`: zero ocorrências.

### `01-modelagem.qmd` (41 linhas) — **nada muda**

100% conceitual, sem chunk. Fala de modelo de negócio, receita de cozinha e pôquer. Nenhuma
frase descreve código.

### `02-o-que-e-machine-learning.qmd` (60 linhas) — **nada muda**

100% conceitual, sem chunk. Única menção a código é "você especifica a *forma* — uma reta, uma
árvore, uma rede" (linha 12), que não é sobre listas.

### `03-overfitting-e-underfitting.qmd` (243 linhas) — o coração da reescrita

**Chunks executáveis (4) e o `eval: false` (1):**

| Label | Linhas | O que faz | Importa |
|---|---|---|---|
| `fig-overfitting` | 13–65 | figura `echo: false`: 10 pontos ao redor de `y = 2x`, polinômios de grau 0, 1 e 9 | `random`, `math`, `matplotlib` |
| `split-data` | 91–116 | define `split_data` em listas; asserts 750/250 e `sorted(train+test)==data`; saída `(750, 250)` | `random`, `typing` |
| `train-test-split` | 126–141 | define `train_test_split` em listas (índices, quatro compreensões) | — |
| `verifica-split` | 151–166 | asserts de pareamento; saída `(750, 250)` | — |
| (sem label) | 170–176 | `eval: false`, esboço `SomeKindOfModel()` — **fica como está** | — |

**Números que a prosa afirma:** 750/250 nos asserts e nas duas saídas de célula (determinísticos,
não mudam); a descrição do desenho da figura na linha 71 (muda, ver abaixo); "0,333 / 0,787",
"0,98 0,96 0,98", "8 positivos em 200", "18 de 200 sementes" no callout `scikit-learn` (medições
sobre o `scikit-learn`, não sobre o nosso código — **não mudam**).

**Figura:** `fig-overfitting`, semente `random.seed(12)`.

**Frases que descrevem o código em listas e vão deixar de ser verdade (literais):**

1. L71 — *"Repare no formato dele. Entre os pontos, ele sobe e desce em ondas que não correspondem a nada — sobe até 7 antes do quarto ponto, mergulha depois do oitavo."* (descreve o desenho da semente antiga)
2. L73 — *"(Na [seção sobre viés e variância](05-vies-e-variancia.qmd) você vai ver o que acontece quando se repete este ajuste com dez amostras diferentes: aí a curva de grau 9 chega a disparar para fora da escala.)"* (a curva de grau 9 **desta** figura já sai da escala agora)
3. L100–103 (comentários no código) — *"# Faz uma cópia rasa,"* / *"# porque shuffle modifica a lista."* / *"# e divide a lista embaralhada ali."*
4. L119 — *"Repare no `data = data[:]` da primeira linha. Sem essa cópia, `random.shuffle` embaralharia a lista **do chamador**, não uma cópia dela."*
5. L121 — *"Esse é o tipo de efeito colateral que não gera erro e aparece muito depois, quando alguém descobre que a ordem original dos dados sumiu. O `assert sorted(train + test) == data` no fim é o que prova que nada foi perdido nem duplicado — e ele só passa porque a cópia existe."*
6. L144 — *"A função não embaralha `xs` e `ys` separadamente — ela embaralha os **índices** e usa os mesmos índices nas duas listas."* (o mecanismo continua; "nas duas listas" não)
7. L153 (comentário) — *"# xs são 1 ... 1000"* — **já está errado hoje** (`range(1000)` é 0…999); corrigir na passagem
8. L219 (callout `scikit-learn`) — *"**A primeira coisa que ela esconde é a semente.** O nosso `split_data` usa `random.shuffle` da biblioteca padrão, então um `random.seed(42)` antes da chamada basta para tornar a divisão reprodutível. […] Chamar `random.seed(42)` antes de `train_test_split` não torna a divisão reprodutível […]"* — o argumento inteiro cai: agora o **nosso** `split_data` também exige gerador explícito
9. L221 — *"`shuffle=True` é o padrão, e é o que o nosso `split_data` também faz"* — **continua verdade**, não mexer

### `04-correcao.qmd` (205 linhas) — quatro chunks, todos **mantidos**

| Label | Linhas | O que faz | Números afirmados |
|---|---|---|---|
| `acuracia` | 50–60 | define `accuracy`; `assert … == 0.98114`; saída `0.98114` | 98,1% (L62, L107) |
| `precisao` | 80–88 | define `precision`; `assert … == 0.014` | 1,4% (L108) |
| `revocacao` | 92–100 | define `recall`; `assert … == 0.005` | 0,5% (L109) |
| `f1` | 116–125 | define `f1_score`; saída `0.00736842105263158` | — |

Nenhum importa `scratch.`; nenhum sorteia; nenhum usa lista. **Verificado no venv:** os três
`assert` fecham com igualdade exata (são divisões de inteiros com resultado exato em binário —
`0.98114`, `0.014`, `0.005`). Não há `ddof`, não há float instável, não há o que recalcular.

Sem figura. Nenhuma frase descreve código em listas. O callout `scikit-learn` (155–204) já fala
de `np.nan` em `zero_division` (L202) — coerente com um capítulo em numpy, fica como está,
inclusive o comentário `# 0.007368421052631579` da L167 (é o valor que o `scikit-learn` devolve,
não a saída da nossa célula).

**Falta só uma coisa:** dizer que as quatro funções moram em `scratch_np.machine_learning`, de
onde os caps. 10 e 13 as importam. Hoje o texto não diz — é acréscimo, não correção.

### `05-vies-e-variancia.qmd` (168 linhas) — duas figuras

| Label | Linhas | O que faz | Importa |
|---|---|---|---|
| `fig-variancia` | 26–77 | `echo: false`: `random.seed(7)`, `amostra(n=10)`, `reta(xs, ys)` por fórmula fechada, `lagrange` redefinida, 10 amostras em dois painéis | `random`, `matplotlib` |
| `fig-mais-dados` | 91–113 | `echo: false`: `random.seed(7)` de novo, `xs` uniformes, N = 10/100/1000, 10 retas por painel; **reaproveita `reta` e `grade` do chunk anterior** | — |

**Números afirmados:** nenhum. As duas figuras são qualitativas; o callout de fechamento
(129–167) traz números do `scikit-learn` (`0.667`, `0.96`, `0.787`, `0.333`) que **não mudam**.

**Frases que descrevem o desenho e precisam ser conferidas contra a figura nova (literais):**

1. L79 — *"À esquerda, as dez retas quase se sobrepõem: o modelo é estável, mesmo que nenhuma delas acerte a relação verdadeira em cheio. À direita, os dez polinômios de grau 9 vão para todo lado — cada conjunto de treino produz um modelo essencialmente diferente, embora cada um deles acerte os seus próprios dez pontos perfeitamente."*
2. L115 — *"Com dez pontos, as retas ajustadas se espalham visivelmente. Com mil, elas praticamente coincidem com a relação verdadeira."*

**Conferidas no scratchpad com `default_rng(7)`: as duas continuam verdadeiras, palavra por
palavra.** (Medições em §3.)

### `06-extracao-e-selecao-de-atributos.qmd` (142 linhas) — sem chunk executável

Conceitual. O callout `## Na prática: sklearn.feature_selection` (61–128) tem **dois blocos
```` ```python ```` que não executam** e usam `random.seed(0)`, `random.randint`, `random.random`,
`random.gauss` para montar dados de brinquedo.

**Frases com números que dependem daquele sorteio (literais):**

1. L87–92 (comentários) — `# .scores_  [0.170, 0.259, 0.258]`, `# .get_support()  [False, True, True]`, `# .estimator_.feature_importances_  [0.524, 0.415, 0.061]`, `# .get_support()  [True, True, False]`
2. L95 — *"Olhe os `scores_`. As duas colunas que **determinam** o rótulo pontuam 0,170 e 0,259; a coluna de puro ruído pontua 0,258 — indistinguível delas."*
3. L117, L122 (comentários) — `# 0.81` e `# 0.45`
4. L125 — *"**81% de acurácia sobre dados onde não existe nada para aprender.**"* e *"o número volta para perto de 0,5, que é a verdade"*

Nada mais no arquivo descreve código nosso. O fecho do capítulo (`## Onde este capítulo deságua`,
130–142) é conceitual e fica intocado.

---

## 2. O módulo `scratch_np/machine_learning.py`

**Já existe** (andaime), e **não muda nem uma linha**. As assinaturas são contrato da spec
(caps. 9, 10 e 13 importam daqui); os `assert` de nível de módulo já rodam limpos — verificado
com `PYTHONPATH=. .venv/bin/python -c "import scratch_np.machine_learning"`, saída vazia,
`f1_score(70, 4930, 13930, 981070) == 0.00736842105263158`.

O trabalho do implementador aqui é o inverso do usual: **fazer os chunks da seção 8.3 baterem
com o módulo**, e não o módulo com os chunks. O corpo das funções nos `.qmd` tem de ficar
**idêntico** ao arquivo — inclusive os comentários e o ramo `isinstance` —, porque a 8.3 é a
seção que ensina e as seguintes importam o mesmo código.

Conteúdo atual, para conferência (não reescrever):

```python
"""Divisão treino/teste e métricas — o que o capítulo 8 ensina.

O capítulo 8 escreve este código inline; os capítulos 9, 10 e 13 importam daqui.
Toda função estocástica recebe o `rng` como parâmetro obrigatório: a semente é
decisão visível de quem chama, nunca estado global.
"""
from typing import Sequence, Tuple, TypeVar

import numpy as np

X = TypeVar("X")  # um ponto de dado qualquer


def split_data(data, prob: float, rng: np.random.Generator):
    """Divide `data` nas frações [prob, 1 - prob], em ordem sorteada.

    Array -> dois arrays, fatiados pelas mesmas linhas.
    Sequência qualquer (lista de mensagens, de pontos rotulados) -> duas listas.
    """
    idx = rng.permutation(len(data))       # uma permutação dos índices...
    cut = int(len(data) * prob)            # ...cortada na proporção pedida
    if isinstance(data, np.ndarray):
        return data[idx[:cut]], data[idx[cut:]]
    return [data[i] for i in idx[:cut]], [data[i] for i in idx[cut:]]


_dados = np.arange(1000)
_treino, _teste = split_data(_dados, 0.75, np.random.default_rng(0))
assert len(_treino) == 750 and len(_teste) == 250
assert np.array_equal(np.sort(np.concatenate([_treino, _teste])), _dados)

_lista = list(range(1000))
_treino_l, _teste_l = split_data(_lista, 0.75, np.random.default_rng(0))
assert len(_treino_l) == 750 and sorted(_treino_l + _teste_l) == _lista


def train_test_split(xs: np.ndarray,
                     ys: np.ndarray,
                     test_pct: float,
                     rng: np.random.Generator
                     ) -> Tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]:
    """-> (x_train, x_test, y_train, y_test), fatiados pelos MESMOS índices."""
    xs = np.asarray(xs)
    ys = np.asarray(ys)
    assert len(xs) == len(ys), "xs e ys precisam ter o mesmo número de linhas"
    train_idx, test_idx = split_data(np.arange(len(xs)), 1 - test_pct, rng)
    return xs[train_idx], xs[test_idx], ys[train_idx], ys[test_idx]


_xs = np.arange(1000)
_ys = 2 * _xs
_x_tr, _x_te, _y_tr, _y_te = train_test_split(_xs, _ys, 0.25, np.random.default_rng(0))
assert len(_x_tr) == len(_y_tr) == 750
assert len(_x_te) == len(_y_te) == 250
assert np.all(_y_tr == 2 * _x_tr) and np.all(_y_te == 2 * _x_te)


def accuracy(tp: int, fp: int, fn: int, tn: int) -> float:
    correct = tp + tn
    total = tp + fp + fn + tn
    return correct / total


assert accuracy(70, 4930, 13930, 981070) == 0.98114


def precision(tp: int, fp: int, fn: int, tn: int) -> float:
    return tp / (tp + fp)


assert precision(70, 4930, 13930, 981070) == 0.014


def recall(tp: int, fp: int, fn: int, tn: int) -> float:
    return tp / (tp + fn)


assert recall(70, 4930, 13930, 981070) == 0.005


def f1_score(tp: int, fp: int, fn: int, tn: int) -> float:
    p = precision(tp, fp, fn, tn)
    r = recall(tp, fp, fn, tn)
    return 2 * p * r / (p + r)
```

**`lagrange` e `reta` NÃO entram no módulo.** Decisão registrada em §4.

---

## 3. Plano seção a seção

### `index.qmd` — **nada muda**

Nenhum chunk, nenhum objetivo desatualizado, nenhuma frase sobre listas. Não abrir o arquivo
para editar.

### `01-modelagem.qmd` — **nada muda**

### `02-o-que-e-machine-learning.qmd` — **nada muda**

---

### `03-overfitting-e-underfitting.qmd`

#### chunk `fig-overfitting` — reescreve

Motivo: `random.seed(12)` + `random.gauss` viram `np.random.default_rng(12)` + `rng.normal`; a
avaliação da reta e do polinômio em 401 pontos da grade vira operação de array.

Código novo (completo):

```{python}
#| label: fig-overfitting
#| fig-cap: "Overfitting e underfitting: polinômios de grau 0, 1 e 9 ajustados aos mesmos dez pontos"
#| echo: false
import numpy as np
from matplotlib import pyplot as plt

rng = np.random.default_rng(12)

# Dez pontos ao redor da reta y = 2x
xs = 2.5 + 0.65 * np.arange(10)
ys = 2 * xs + rng.normal(0, 1.2, size=xs.shape)

# --- grau 0: a média ---
media = ys.mean()

# --- grau 1: mínimos quadrados, em duas fórmulas ---
dx = xs - xs.mean()
dy = ys - ys.mean()
slope = (dx * dy).sum() / (dx ** 2).sum()
intercept = ys.mean() - slope * xs.mean()

# --- grau 9: o polinômio que passa exatamente pelos 10 pontos ---
# Com n pontos distintos existe um único polinômio de grau n-1 que passa por
# todos eles, e a forma de Lagrange o escreve diretamente — sem resolver sistema.
# O laço percorre os n termos da soma, que é a fórmula; o produto sobre os
# outros pontos e a avaliação em toda a grade são operações de array.
def lagrange(x, xs, ys):
    x = np.asarray(x, dtype=float)
    total = np.zeros_like(x)
    for i in range(len(xs)):
        outros = np.arange(len(xs)) != i           # máscara: todos menos o i
        base = np.prod((x[:, None] - xs[outros]) / (xs[i] - xs[outros]), axis=1)
        total += ys[i] * base
    return total

grade = np.linspace(xs[0], xs[-1], 401)

fig, ax = plt.subplots(figsize=(7.5, 4.5))
ax.plot(grade, np.full_like(grade, media), "--", label="grau 0", color="#4472c4")
ax.plot(grade, intercept + slope * grade, "-", label="grau 1", color="#2e7d32")
ax.plot(grade, lagrange(grade, xs, ys), ":", label="grau 9", color="#c00000")
ax.scatter(xs, ys, color="#1f3864", zorder=5)

ax.set_ylim(ys.min() - 6, ys.max() + 6)
ax.set_xlabel("x")
ax.set_ylabel("y")
ax.set_title("Melhores polinômios de vários graus")
ax.legend()
plt.tight_layout()
plt.show()
```

Saída esperada: uma figura. **Medido no scratchpad** (`default_rng(12)`, PNG inspecionado):

- `ys` = 4,99 · 7,56 · 8,49 · 9,77 · 12,14 · 10,05 · 12,05 · 12,52 · 15,27 · 17,90
- grau 0: `media` = 11,07 · grau 1: `intercept` = 1,21, `slope` = 1,82
- moldura: `ylim` = (−1,01 · 23,90)
- grau 9: mínimo −3,81 em x ≈ 2,69 (**abaixo** da moldura); máximo 28,64 em x ≈ 8,16 (**acima**)
- sobe a 10,00 entre o 2º ponto (7,56) e o 3º (8,49); sobe a 13,28 entre o 7º e o 8º; desce a 11,37 depois do 8º
- a versão vetorizada de `lagrange` bate com a versão em laços duplos: diferença máxima **4,3 × 10⁻¹⁴** sobre os 401 pontos

Prosa afetada:

- antes: *"Repare no formato dele. Entre os pontos, ele sobe e desce em ondas que não correspondem a nada — sobe até 7 antes do quarto ponto, mergulha depois do oitavo."*
  → depois: *"Repare no formato dele. Entre os pontos, ele sobe e desce em ondas que não correspondem a nada — sobe a 10 entre o segundo e o terceiro pontos, que valem 7,6 e 8,5, e a 13,3 entre o sétimo e o oitavo. Nas duas pontas ele nem cabe na figura: mergulha abaixo de zero logo depois do primeiro ponto e dispara acima de 28 antes do último."*
- antes: *"(Na [seção sobre viés e variância](05-vies-e-variancia.qmd) você vai ver o que acontece quando se repete este ajuste com dez amostras diferentes: aí a curva de grau 9 chega a disparar para fora da escala.)"*
  → depois: *"(Na [seção sobre viés e variância](05-vies-e-variancia.qmd) você vai ver o que acontece quando se repete este ajuste com dez amostras diferentes: cada uma escapa da moldura num lugar diferente, e é essa discordância que dá nome à variância.)"*

Semente: `np.random.default_rng(12)` — a mesma numeração de antes, mantida para não trocar
sorteio sem motivo. Chunk `echo: false`, então **não há prosa justificando a semente aqui** (o
leitor não vê o código); a justificativa do `rng` explícito já foi dada no bloco *De listas a arrays* da seção 7.1 e reaparece
neste capítulo no `split-data`, que é visível.

#### chunk `split-data` — reescreve

Motivo: é o código que a seção **ensina** e que o módulo guarda; tem de ficar idêntico a
`scratch_np/machine_learning.py`, inclusive o ramo `isinstance`.

Código novo (completo):

```{python}
#| label: split-data
from typing import Tuple, TypeVar

import numpy as np

X = TypeVar('X')  # tipo genérico para representar um ponto de dados

def split_data(data, prob: float, rng: np.random.Generator):
    """Divide `data` nas frações [prob, 1 - prob], em ordem sorteada.

    Array -> dois arrays, fatiados pelas mesmas linhas.
    Sequência qualquer (lista de mensagens, de pontos rotulados) -> duas listas.
    """
    idx = rng.permutation(len(data))       # uma permutação dos índices...
    cut = int(len(data) * prob)            # ...cortada na proporção pedida
    if isinstance(data, np.ndarray):
        return data[idx[:cut]], data[idx[cut:]]
    return [data[i] for i in idx[:cut]], [data[i] for i in idx[cut:]]

data = np.arange(1000)
train, test = split_data(data, 0.75, np.random.default_rng(0))

# As proporções devem estar corretas
assert len(train) == 750
assert len(test) == 250

# E os dados originais devem estar preservados (em alguma ordem)
assert np.array_equal(np.sort(np.concatenate([train, test])), data)

# O outro ramo: uma sequência qualquer devolve duas listas
mensagens = list(range(1000))
train_msg, test_msg = split_data(mensagens, 0.75, np.random.default_rng(0))
assert len(train_msg) == 750
assert sorted(train_msg + test_msg) == mensagens

len(train), len(test)
```

Saída esperada: `(750, 250)` — determinística, não depende do sorteio. **Medido:** os quatro
`assert` passam; `type(train).__name__ == 'ndarray'`, `type(train_msg).__name__ == 'list'`;
com `default_rng(0)` as cinco primeiras linhas do treino são `[459 206 222 162 711]`.

Prosa afetada:

- **novo parágrafo**, imediatamente depois do chunk (antes do callout): *"O `isinstance` no meio da função existe porque nem todo conjunto de dados é um array. Quando `data` é uma matriz de medidas — as flores do [Capítulo 9](../cap09/index.qmd), as linhas de uma regressão —, `data[idx[:cut]]` é indexação por array: o `numpy` devolve de uma vez as linhas escolhidas, na ordem escolhida. Quando `data` é uma sequência de objetos que não cabem num array de números — a lista de mensagens de e-mail do [Capítulo 10](../cap10/index.qmd) —, a compreensão de lista faz o mesmo trabalho, índice a índice. As duas linhas dizem a mesma coisa em duas linguagens, e é por isso que um único `split_data` serve aos dois capítulos."*
- antes (callout-note inteiro, L118–122): *"Repare no `data = data[:]` da primeira linha. Sem essa cópia, `random.shuffle` embaralharia a lista **do chamador**, não uma cópia dela. / Esse é o tipo de efeito colateral que não gera erro e aparece muito depois, quando alguém descobre que a ordem original dos dados sumiu. O `assert sorted(train + test) == data` no fim é o que prova que nada foi perdido nem duplicado — e ele só passa porque a cópia existe."*
  → depois:

  ```markdown
  ::: {.callout-note}
  Repare no que a função **não** faz: ela não embaralha `data`. `rng.permutation(len(data))` devolve um array com os índices `0, 1, …, n-1` em ordem sorteada, e tanto `data[idx[:cut]]` quanto a compreensão de lista **copiam** as posições escolhidas para uma coleção nova. O que o chamador passou continua exatamente como estava.

  A versão em listas de @grus2019 precisa de uma cópia explícita antes de embaralhar, porque o `shuffle` da biblioteca padrão embaralha no lugar — e esquecer essa cópia é o tipo de efeito colateral que não gera erro e aparece muito depois, quando alguém descobre que a ordem original dos dados sumiu. Aqui a cópia é consequência da indexação, não uma linha que alguém precisa lembrar de escrever. O `assert` do fim — os mil números de volta, ordenados — é o que prova que nada foi perdido nem duplicado.
  :::
  ```

- **novo parágrafo**, logo antes do chunk `split-data` (emenda com "…medindo o desempenho no terço restante:"): não é necessário; o texto de L85–89 continua valendo palavra por palavra.
- **novo callout**, depois do parágrafo do `isinstance` — justificativa da semente, na forma que o cap. 7 fixou:

  ```markdown
  ::: {.callout-note}
  ## A semente não é opcional

  `split_data` recebe o gerador como parâmetro **obrigatório**: não há valor padrão, e nenhum estado global é consultado. Quem chama precisa escrever `np.random.default_rng(0)`, e a semente fica visível ao lado da divisão que ela produziu.

  Sem isso, "o modelo acertou 93% no teste" seria uma afirmação sobre um sorteio que ninguém consegue repetir — nem você, na semana seguinte. Todo capítulo daqui em diante divide dados assim, com o gerador escrito no chunk.
  :::
  ```

#### chunk `train-test-split` — reescreve

Motivo: mesmo do anterior — o corpo tem de ser o do módulo.

Código novo (completo):

```{python}
#| label: train-test-split
def train_test_split(xs: np.ndarray,
                     ys: np.ndarray,
                     test_pct: float,
                     rng: np.random.Generator
                     ) -> Tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]:
    """-> (x_train, x_test, y_train, y_test), fatiados pelos MESMOS índices."""
    xs = np.asarray(xs)
    ys = np.asarray(ys)
    assert len(xs) == len(ys), "xs e ys precisam ter o mesmo número de linhas"
    train_idx, test_idx = split_data(np.arange(len(xs)), 1 - test_pct, rng)
    return xs[train_idx], xs[test_idx], ys[train_idx], ys[test_idx]
```

Saída esperada: nenhuma (só define).

Prosa afetada:

- **sai**: a linha `Y = TypeVar('Y')  # tipo genérico para representar variáveis de saída` — o
  módulo não a tem, e as anotações agora são `np.ndarray`. Nenhuma frase da prosa cita `Y`.
- antes: *"A função não embaralha `xs` e `ys` separadamente — ela embaralha os **índices** e usa os mesmos índices nas duas listas."*
  → depois: *"A função não embaralha `xs` e `ys` separadamente — ela embaralha os **índices** e usa os mesmos índices nos dois arrays. `xs[train_idx]` é indexação por array: `train_idx` diz quais linhas, e em que ordem."*
- o resto do `::: {.exemplo}` (L146, *"Se embaralhasse cada uma por conta própria…"*) continua
  valendo palavra por palavra.

#### chunk `verifica-split` — reescreve

Motivo: os asserts com `zip` e `all(...)` viram comparação de arrays; o comentário de `xs` está
errado hoje.

Código novo (completo):

```{python}
#| label: verifica-split
xs = np.arange(1000)           # xs são 0 ... 999
ys = 2 * xs                    # cada y_i é o dobro de x_i
x_train, x_test, y_train, y_test = train_test_split(xs, ys, 0.25, np.random.default_rng(0))

# Verifica que as proporções estão corretas
assert len(x_train) == len(y_train) == 750
assert len(x_test) == len(y_test) == 250

# Verifica que os pontos correspondentes estão pareados corretamente
assert np.all(y_train == 2 * x_train)
assert np.all(y_test == 2 * x_test)

len(x_train), len(x_test)
```

Saída esperada: `(750, 250)` — determinística. **Medido:** os quatro `assert` passam.

Prosa afetada:

- **novo parágrafo**, entre este chunk e *"Depois disso, você pode fazer algo assim:"*: *"As duas funções ficam em `scratch_np.machine_learning`, exatamente como você as escreveu aqui — o módulo é o chunk salvo em arquivo. O [Capítulo 9](../cap09/index.qmd) importa `split_data` para separar as flores do Iris, o [Capítulo 10](../cap10/index.qmd) para separar mensagens de e-mail, e o [Capítulo 13](../cap13/index.qmd) importa `train_test_split`. Nenhum deles reescreve a divisão."*

#### chunk sem label (L170–176, `eval: false`) — mantém

Motivo: é um esboço de API (`SomeKindOfModel()`), não roda, e não menciona listas. A chamada
`train_test_split(xs, ys, 0.33)` continua legível como pseudocódigo; **não** acrescentar `rng`
aqui seria inconsistente com a assinatura nova, então acrescente:
`x_train, x_test, y_train, y_test = train_test_split(xs, ys, 0.33, np.random.default_rng(0))`.
Uma linha, nada mais.

#### callout `## Na prática: scikit-learn` — reescreve o primeiro parágrafo

Motivo: o argumento "o nosso escuta `random.seed`, o dela não" morreu — os dois exigem semente
explícita agora. **Medido no venv** (sklearn instalado): duas chamadas de `train_test_split` sem
`random_state`, com `np.random.default_rng(42)` criado antes, devolvem conjuntos **diferentes**;
com `random_state=42`, iguais.

Texto novo (substitui integralmente a L219; a numeração "A segunda…/A terceira…" dos parágrafos
seguintes é preservada, então **é um parágrafo só**):

> **A primeira coisa que ela esconde é a semente — e vale dizer com cuidado o que "esconde" significa.** O nosso `split_data` recebe o gerador como parâmetro **obrigatório**: sem um `np.random.default_rng(42)` escrito na chamada, ele nem roda. O `train_test_split` da biblioteca tem `random_state=None` por padrão e, nesse caso, sorteia do gerador **global** do `numpy`: duas chamadas seguidas devolvem conjuntos diferentes, e criar um `np.random.default_rng(42)` antes não muda nada, porque o gerador que ela usa é outro. Só `random_state=42` torna a divisão reprodutível, e a regra vale para o pacote inteiro — todo objeto do `scikit-learn` que sorteia tem o seu próprio `random_state`. Os dois caminhos chegam ao mesmo lugar; a diferença é o padrão, e o dela deixa esquecer. O que ela esconde mesmo é a **permutação**: o nosso `split_data` monta `idx = rng.permutation(len(data))` e divide esse array, que dá para imprimir, guardar e conferir; o `train_test_split` devolve os pedaços e joga os índices fora. Para saber *quais* linhas foram para o teste — casar com um identificador, auditar uma divisão que deu resultado estranho —, o jeito é dividir um `np.arange(len(X))` junto com os dados, como se fosse mais uma variável, ou pedir os índices a `ShuffleSplit(n_splits=1, test_size=0.25, random_state=42).split(X)`.

Os parágrafos "**A segunda é o embaralhamento**" (inclusive *"é o que o nosso `split_data` também
faz"*, que continua verdade), "**A terceira é a estratificação**" e "**E a divisão em três
partes**" ficam **intocados**, com os números que já têm.

#### Números a recalcular — 8.3

| Onde | Tipo | Por quê muda | Valor |
|---|---|---|---|
| L71, descrição do desenho | frase | sorteio novo | medido: sobe a 10 entre o 2º e o 3º ponto; a 13,3 entre o 7º e o 8º; mínimo −3,8; máximo 28,6 |
| L73, "disparar para fora da escala" | frase | a figura da 8.5 muda junto | reescrita acima |
| saída de `split-data` | célula | — | `(750, 250)`, determinístico |
| saída de `verifica-split` | célula | — | `(750, 250)`, determinístico |
| callout `scikit-learn`, demais parágrafos | frases | não dependem do nosso código | **não mudam** |

---

### `04-correcao.qmd`

#### chunks `acuracia`, `precisao`, `revocacao`, `f1` — **mantém os quatro**

Motivo: são funções escalares sobre quatro inteiros. Não há vetor, não há ponto de dado, não há
laço. Meter `np.array([tp, fp, fn, tn])` aqui seria decoração — exatamente o que a spec proíbe
("reimplementar em Python o que a linha à esquerda já faz" tem o gêmeo "usar numpy onde não há
o que vetorizar"). O código dos quatro chunks já é **idêntico** ao de
`scratch_np/machine_learning.py`, linha por linha.

Saída esperada (medida, inalterada): `0.98114`, `0.014`, `0.005`, `0.00736842105263158`. Os três
`assert` de igualdade exata fecham — são divisões de inteiros exatas em binário, não há aqui o
caso do briefing (item 6) que pediria `np.isclose`.

Prosa afetada:

- **novo parágrafo**, depois de *"Este é a **média harmônica** de precisão e revocação, e necessariamente fica entre as duas."* e antes do callout-note sobre a harmônica: *"As quatro funções desta seção moram em `scratch_np.machine_learning`, junto com o `split_data` da [seção anterior](03-overfitting-e-underfitting.qmd) — o mesmo código que você acabou de escrever. É de lá que o [Capítulo 10](../cap10/index.qmd) e o [Capítulo 13](../cap13/index.qmd) as importam para avaliar os modelos deles."*
- nada mais muda. O callout `scikit-learn` (155–204) fica **inteiro**, inclusive `np.nan` e o
  comentário `# 0.007368421052631579`.

**Atenção do implementador:** este arquivo fica **sem `import numpy as np`**, e está certo. O
`test_capitulo_numpy_importa_numpy` exige numpy em pelo menos um chunk **do capítulo**, não de
cada arquivo — a 8.3 e a 8.5 cumprem.

#### Números a recalcular — 8.4

Nenhum. Tabela vazia de propósito: é a única seção do capítulo em que a reescrita não toca em
número algum.

---

### `05-vies-e-variancia.qmd`

#### chunk `fig-variancia` — reescreve

Motivo: `random.seed(7)` e `random.gauss` viram `default_rng(7)` e `rng.normal`; `reta` e
`lagrange` passam a operar sobre arrays e a avaliar a grade inteira de uma vez.

Código novo (completo):

```{python}
#| label: fig-variancia
#| fig-cap: "Dez conjuntos de treino diferentes, da mesma população. À esquerda, grau 1; à direita, grau 9."
#| echo: false
import numpy as np
from matplotlib import pyplot as plt

rng = np.random.default_rng(7)

def amostra(rng, n=10):
    xs = 2.5 + 0.65 * np.arange(n)
    ys = 2 * xs + rng.normal(0, 1.2, size=xs.shape)
    return xs, ys

def reta(xs, ys):
    dx = xs - xs.mean()
    dy = ys - ys.mean()
    slope = (dx * dy).sum() / (dx ** 2).sum()
    return ys.mean() - slope * xs.mean(), slope

def lagrange(x, xs, ys):
    x = np.asarray(x, dtype=float)
    total = np.zeros_like(x)
    for i in range(len(xs)):
        outros = np.arange(len(xs)) != i
        base = np.prod((x[:, None] - xs[outros]) / (xs[i] - xs[outros]), axis=1)
        total += ys[i] * base
    return total

grade = np.linspace(2.5, 8.35, 301)

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(10, 4), sharey=True)

for _ in range(10):
    xs, ys = amostra(rng)
    b, m = reta(xs, ys)
    ax1.plot(grade, b + m * grade, color="#2e7d32", alpha=0.6)
    ax2.plot(grade, lagrange(grade, xs, ys), color="#c00000", alpha=0.6)

ax1.set_title("grau 1 — variância baixa")
ax2.set_title("grau 9 — variância alta")
ax1.set_ylabel("y")
for ax in (ax1, ax2):
    ax.plot(grade, 2 * grade, "k--", lw=1, label="relação verdadeira")
    ax.set_ylim(0, 22)
    ax.set_xlabel("x")
    ax.legend(fontsize=8)
plt.tight_layout()
plt.show()
```

Saída esperada: uma figura de dois painéis. **Medido no scratchpad (PNG inspecionado):**

- inclinações das dez retas: 1,99 · 1,61 · 2,15 · 2,24 · 2,20 · 2,08 · 1,96 · 2,22 · 2,17 · 1,58 — a
  relação verdadeira é 2, e o painel esquerdo mostra um feixe apertado que quase se sobrepõe
- extremos dos dez polinômios de grau 9 na grade: mínimos de −4,4 a 5,6; máximos de 15,3 a 31,1 —
  com `ylim` (0, 22), **quatro** escapam por baixo e **duas** por cima, cada uma num lugar diferente
- o painel direito é um emaranhado de curvas divergindo, exatamente o desenho que a prosa descreve

Prosa afetada:

- L79 (*"À esquerda, as dez retas quase se sobrepõem… À direita, os dez polinômios de grau 9 vão para todo lado…"*) — **continua verdadeira, não mexer.** Conferida contra o PNG.

Semente: `np.random.default_rng(7)`, criado **uma vez** no topo do chunk e passado a `amostra` —
as dez amostras saem do mesmo fluxo, como as dez saíam do mesmo `random.seed(7)`. Chunk
`echo: false`; sem prosa justificando (o leitor não vê o código).

#### chunk `fig-mais-dados` — reescreve

Motivo: mesmo do anterior.

Código novo (completo):

```{python}
#| label: fig-mais-dados
#| fig-cap: "Reduzindo a variância com mais dados: o mesmo modelo, ajustado a amostras de 10, 100 e 1000 pontos"
#| echo: false
rng = np.random.default_rng(7)

fig, axes = plt.subplots(1, 3, figsize=(11, 3.6), sharey=True)

for ax, n in zip(axes, [10, 100, 1000]):
    for _ in range(10):
        xs = 2.5 + 5.85 * rng.random(n)
        ys = 2 * xs + rng.normal(0, 1.2, size=xs.shape)
        b, m = reta(xs, ys)
        ax.plot(grade, b + m * grade, color="#4472c4", alpha=0.6)
    ax.plot(grade, 2 * grade, "k--", lw=1)
    ax.set_title(f"N = {n}")
    ax.set_ylim(2, 20)
    ax.set_xlabel("x")

axes[0].set_ylabel("y")
plt.tight_layout()
plt.show()
```

Reaproveita `reta`, `grade`, `np` e `plt` do chunk anterior — mesmo arquivo, mesmo kernel, como
já acontece hoje.

Saída esperada: uma figura de três painéis. **Medido (PNG inspecionado):** as inclinações das dez
retas vão de 1,59 a 2,55 com N = 10; de 1,84 a 2,06 com N = 100; de 1,97 a 2,04 com N = 1000. O
feixe fecha visivelmente da esquerda para a direita, e no painel de mil pontos as dez retas
somem sob a tracejada.

Prosa afetada:

- L115 (*"Com dez pontos, as retas ajustadas se espalham visivelmente. Com mil, elas praticamente coincidem com a relação verdadeira."*) — **continua verdadeira, não mexer.**
- L117 (*"O efeito é ainda mais dramático em modelos complexos: o polinômio de grau 9…"*) — não é
  medida desta figura nem do nosso código; **não mexer**.

#### callout `## Na prática: learning_curve e validation_curve` — mantém

Motivo: todos os números são do `scikit-learn` sobre o Iris, não do nosso código. O último
parágrafo (*"os gráficos daqui precisaram de dados simulados, em que a população é conhecida e
dá para sortear dez amostras dela"*) continua descrevendo exatamente o que os chunks fazem.

#### Números a recalcular — 8.5

| Onde | Tipo | Por quê muda | Valor |
|---|---|---|---|
| L79, descrição dos dois painéis | frase | sorteio novo | **conferido: continua valendo** |
| L115, descrição dos três painéis | frase | sorteio novo | **conferido: continua valendo** |
| callout `learning_curve` | números | do `scikit-learn` | **não mudam** |

---

### `06-extracao-e-selecao-de-atributos.qmd`

Nenhum chunk executável. Muda **só** o código de exemplo dos dois blocos ```` ```python ```` do
callout, para falar o vocabulário do livro (`default_rng`, `rng.integers`, `rng.random`,
`rng.normal`, `np.column_stack`), e os números que esse código produz.

#### bloco de código do XOR (L72–93) — reescreve

Motivo: `random.seed(0)` e as compreensões de lista são o vocabulário dos caps. 1–5; o leitor
deste capítulo já conhece `default_rng`. O bloco não executa, mas o livro fala uma língua só.

Código novo (completo):

```python
import numpy as np
from sklearn.ensemble import RandomForestClassifier
from sklearn.feature_selection import SelectKBest, SelectFromModel, f_classif

rng = np.random.default_rng(7)
n = 400
a     = rng.integers(0, 2, size=n)
b     = rng.integers(0, 2, size=n)
ruido = rng.random(n)

y = a ^ b                                     # y só existe na combinação de a e b
X = np.column_stack([a, b, ruido]).astype(float)

SelectKBest(f_classif, k=2).fit(X, y)
# .scores_                            [0.165, 1.695, 1.160]   (a, b, ruído)
# .get_support()                      [False,  True,  True]

SelectFromModel(RandomForestClassifier(random_state=0), max_features=2).fit(X, y)
# .estimator_.feature_importances_    [0.524, 0.395, 0.081]
# .get_support()                      [ True,  True, False]
```

Saída esperada: **medida no venv com `default_rng(7)`** — os quatro comentários acima são os
valores reais, arredondados a três casas. O `.get_support()` do `SelectKBest` sai
`[False, True, True]`, **idêntico ao de hoje**: ele descarta `a` e mantém o ruído.

Prosa afetada:

- antes: *"Olhe os `scores_`. As duas colunas que **determinam** o rótulo pontuam 0,170 e 0,259; a coluna de puro ruído pontua 0,258 — indistinguível delas. O `SelectKBest` então descarta `a`, uma das duas únicas colunas informativas, e mantém o ruído. O `SelectFromModel` com uma floresta acerta, porque a floresta usa `a` e `b` juntas dentro da mesma árvore e a importância registra isso."*
  → depois: *"Olhe os `scores_`. A coluna `a` — que, junto com `b`, **determina** o rótulo — pontua 0,165; a coluna de puro ruído pontua 1,160, sete vezes mais. O `SelectKBest` então descarta `a`, uma das duas únicas colunas informativas, e mantém o ruído. O `SelectFromModel` com uma floresta acerta, porque a floresta usa `a` e `b` juntas dentro da mesma árvore e a importância registra isso."*
- **novo parágrafo**, logo em seguida: *"Os valores exatos mudam com o sorteio; a cegueira, não. Isolada, nem `a` nem `b` diz coisa alguma sobre `y` — a pontuação de cada uma é ruído amostral, e cai ora acima, ora abaixo da coluna que é ruído de verdade. Repetindo o experimento com 300 sementes diferentes, o `SelectKBest` ficou com as duas colunas informativas em 116 delas: pouco mais do que as 100 que sortear duas de três colunas daria."*
  **(Medido: 116/300, sementes 1000–1299.)**

#### bloco de código do vazamento (L107–123) — reescreve

Motivo: mesmo do anterior. **O `rng` continua o do bloco acima**, sem re-semear — é a tradução
fiel do original, em que o segundo bloco seguia consumindo o fluxo de `random.seed(0)`.

Código novo (completo):

```python
from sklearn.model_selection import cross_val_score
from sklearn.neighbors import KNeighborsClassifier
from sklearn.pipeline import Pipeline

X = rng.normal(0, 1, size=(100, 2000))   # 100 linhas, 2.000 colunas de puro ruído
y = np.array([0, 1] * 50)

# ERRADO: escolhe as 10 melhores olhando tudo, depois valida
X_sel = SelectKBest(f_classif, k=10).fit_transform(X, y)
cross_val_score(KNeighborsClassifier(5), X_sel, y).mean()   # 0.81

# CERTO: a seleção é refeita dentro de cada dobra
pipe = Pipeline([('sel', SelectKBest(f_classif, k=10)),
                 ('knn', KNeighborsClassifier(5))])
cross_val_score(pipe, X, y).mean()                          # 0.45
```

Saída esperada: **medida** — 0,81 e 0,45, com o `rng` de `default_rng(7)` já tendo consumido os
400 + 400 inteiros e os 400 uniformes do bloco anterior. Os dois números são exatamente os que
o texto de hoje afirma.

Prosa afetada:

- L125 (*"**81% de acurácia sobre dados onde não existe nada para aprender.**"* … *"o número volta para perto de 0,5, que é a verdade"*) — **continua valendo, não mexer.** É por isso que a semente
  escolhida foi a 7 e não a 0 (ver §4).
- L105 (*"100 linhas, 2.000 colunas de ruído gaussiano, rótulo alternando 0 e 1"*) — continua
  descrevendo o código; não mexer.

#### Números a recalcular — 8.6

| Onde | Tipo | Por quê muda | Valor medido |
|---|---|---|---|
| L87 `.scores_` | comentário | sorteio novo | `[0.165, 1.695, 1.160]` |
| L88 `.get_support()` | comentário | sorteio novo | `[False, True, True]` (igual ao de hoje) |
| L91 `feature_importances_` | comentário | sorteio novo | `[0.524, 0.395, 0.081]` |
| L92 `.get_support()` | comentário | sorteio novo | `[True, True, False]` (igual ao de hoje) |
| L95 frase dos scores | frase | sorteio novo | reescrita acima |
| L117 `# 0.81` | comentário | sorteio novo | `0.81` (igual ao de hoje) |
| L122 `# 0.45` | comentário | sorteio novo | `0.45` (igual ao de hoje) |
| "116 em 300 sementes" | frase nova | medição nova | 116/300 |

**Script de conferência** (o implementador roda e copia os números reais; ele reproduz os dois
blocos na ordem em que aparecem no callout, com um `rng` só):

```python
import warnings; warnings.filterwarnings("ignore")
import numpy as np
from sklearn.ensemble import RandomForestClassifier
from sklearn.feature_selection import SelectKBest, SelectFromModel, f_classif
from sklearn.model_selection import cross_val_score
from sklearn.neighbors import KNeighborsClassifier
from sklearn.pipeline import Pipeline

rng = np.random.default_rng(7)
n = 400
a, b, ruido = rng.integers(0, 2, n), rng.integers(0, 2, n), rng.random(n)
y = a ^ b
X = np.column_stack([a, b, ruido]).astype(float)
k = SelectKBest(f_classif, k=2).fit(X, y)
sm = SelectFromModel(RandomForestClassifier(random_state=0), max_features=2).fit(X, y)
print(np.round(k.scores_, 3), k.get_support())
print(np.round(sm.estimator_.feature_importances_, 3), sm.get_support())

X2 = rng.normal(0, 1, size=(100, 2000)); y2 = np.array([0, 1] * 50)
Xs = SelectKBest(f_classif, k=10).fit_transform(X2, y2)
print(round(cross_val_score(KNeighborsClassifier(5), Xs, y2).mean(), 2))
pipe = Pipeline([('sel', SelectKBest(f_classif, k=10)), ('knn', KNeighborsClassifier(5))])
print(round(cross_val_score(pipe, X2, y2).mean(), 2))
```

---

## 4. Riscos e decisões

**1. As figuras dependem das sementes 12 e 7, e o desenho muda.** Foi o risco principal, e foi
resolvido antes de escrever o plano: as duas figuras foram geradas no scratchpad com
`default_rng(12)` e `default_rng(7)` e **os PNGs foram inspecionados**.

- `fig-overfitting` com `default_rng(12)`: o overfitting fica **mais** visível que hoje — ondas
  claras entre todos os pontos e as duas pontas escapando da moldura. A única frase que descrevia
  o desenho antigo (L71) é reescrita acima com os valores medidos. A moldura
  `ylim = (ys.min() - 6, ys.max() + 6)` é mantida: o polinômio saindo por cima e por baixo é
  informação, não defeito.
- `fig-variancia` e `fig-mais-dados` com `default_rng(7)`: as duas frases que descrevem os
  desenhos (L79 e L115) **continuam verdadeiras sem alteração**. Nenhuma semente precisou ser
  trocada.

**2. `lagrange` vetorizada: onde parar.** A versão totalmente vetorizada (array 3-D com
`fill_diagonal` na matriz de diferenças) é ilegível e esconde a fórmula. A versão adotada mantém
o **laço sobre os n termos da soma** — que *é* a fórmula de Lagrange — e vetoriza o produto
sobre os outros pontos (com máscara booleana) e a avaliação nos 401 pontos da grade. É
exatamente o corte que a spec pede: sai o laço sobre pontos de avaliação, fica o laço que é o
algoritmo. **Conferida contra a versão em laços duplos: diferença máxima 4,3 × 10⁻¹⁴.**
`np.polyfit` seria a alternativa óbvia e está **proibido** pela spec.

**3. `lagrange` e `reta` não vão para `scratch_np/machine_learning.py`.** Três razões: (a) são
auxiliares de figura, não API do capítulo — ninguém mais as importa; (b) os dois chunks são
`echo: false`, então levá-las ao módulo as esconderia ainda mais do leitor sem ganho nenhum; (c)
o módulo é contrato dos caps. 9, 10 e 13, e acrescentar nomes públicos a ele aumenta a superfície
sem necessidade. Ficam redefinidas em cada chunk, como já estão hoje.

**4. As quatro métricas ficam sem numpy, de propósito.** Registrado aqui para o revisor não
apontar como esquecimento: `accuracy(70, 4930, 13930, 981070)` é aritmética sobre quatro
inteiros. Consequência operacional: `04-correcao.qmd` não tem `import numpy as np`, e o
`test_capitulo_numpy_importa_numpy` continua passando porque exige numpy no **capítulo**.

**5. Os `assert` do Grus.** Os três de igualdade exata (`== 0.98114`, `== 0.014`, `== 0.005`)
foram verificados no venv e **fecham**. Nenhum vira `np.isclose`. Os asserts do `split_data`
mudam de forma (`sorted(train + test) == data` → `np.array_equal(np.sort(np.concatenate(...)), data)`
no ramo de array), mas a afirmação é a mesma: nada perdido, nada duplicado. O ramo de lista
mantém o `sorted(...) == ...` original — os dois estão no chunk, e os dois estão no módulo.

**6. Semente do callout de seleção de atributos: 7, não 0.** A tradução literal de
`random.seed(0)` seria `default_rng(0)`, e ela **funciona** (descarta `a`, mantém o ruído), mas
lá a coluna `b` pontua 4,454 contra 3,208 do ruído — o que enfraquece a frase, sugerindo que o
`SelectKBest` "meio que achou" uma das colunas. Com `default_rng(7)` a leitura é a de hoje
(0,165 contra 1,160) **e** os dois números do segundo bloco saem 0,81 e 0,45, idênticos aos que
o texto já afirma — nenhuma outra frase muda. Registrado como escolha deliberada, com o
contraponto honesto embutido na prosa nova ("os valores exatos mudam com o sorteio; a cegueira,
não", com o 116/300 medido).

**7. O chunk `eval: false` do modelo genérico ganha um `rng`.** Sem isso, ficaria um exemplo de
uso incompatível com a assinatura ensinada três parágrafos acima. É uma linha.

**8. Tempo de execução.** Nada aqui é caro: as duas figuras são 401 e 301 pontos de grade, 10
amostras. O capítulo inteiro roda em segundos. Nenhuma frase do texto afirma nada sobre tempo, e
**nenhuma deve passar a afirmar** — não há comparação de velocidade a fazer neste capítulo.

**9. Nada de subida de gradiente, nada de `dados/`, nada de tqdm.** `tests/test_gradiente.py` não
é afetado; nenhum caminho de dado aparece no capítulo.

---

## 5. Verificação

```bash
export MPLBACKEND=Agg PYTHONHASHSEED=0
.venv/bin/python scripts/executar-secoes.py 8        # cada .qmd num kernel próprio (como o site)
.venv/bin/python scripts/gerar-notebooks.py          # regenera os 17 notebooks
.venv/bin/python scripts/executar-notebooks.py cap08 # o capítulo num kernel só (como a aula)
.venv/bin/pytest tests/ -q
```

Os quatro verdes **antes** de mexer no teste. Só então:

- `tests/test_scratch_np.py`, linha 27: `CAPITULOS_NUMPY: set[int] = {6, 7}` → `{6, 7, 8}`.
- `PYTHON_PURO_PERMITIDO`: **nada a registrar.** A expressão "Python puro" não aparece em nenhum
  `.qmd` do capítulo 8, nem antes nem depois da reescrita.
- `RANDOM_PERMITIDO`: **nada a registrar.** Depois da reescrita não sobra `random.` da stdlib nem
  `np.random.seed` em chunk executável. (As ocorrências de `random.` no callout da 8.6 estão em
  blocos ```` ```python ````, que o teste não inspeciona — mas são substituídas mesmo assim, por
  coerência de vocabulário.)

Depois de rodar, conferir à mão:

1. `grep -n "random\.\|scratch\." content/cap08/*.qmd` — só pode sobrar `random_state=` do
   `scikit-learn` nos callouts e `scratch_np.` na prosa.
2. Abrir os dois PNGs gerados (`_freeze` não; use o scratchpad) e reler as frases L71 (8.3), L79
   e L115 (8.5) contra o desenho. **Se alguma delas deixar de descrever a figura, é a frase que
   se corrige, nunca a semente** — as três já foram conferidas e passam.
3. O corpo de `split_data` e de `train_test_split` nos chunks tem de ser byte a byte o de
   `scratch_np/machine_learning.py` (fora os nomes das variáveis de demonstração, que no módulo
   levam `_` na frente para não vazar no `import`).

Commit atômico: `content/cap08/*.qmd` + `notebooks/cap08-machine-learning.ipynb` +
`tests/test_scratch_np.py`. **`scratch_np/machine_learning.py` não entra — não muda.**
Mensagem: `feat(cap08): reescrita em numpy — split_data com rng explícito e figuras vetorizadas`.

---

## Desvios na implementação

*(Implementador, 2026-09-07. Host: `.venv/`, numpy 2.x, Python 3.12, `MPLBACKEND=Agg PYTHONHASHSEED=0`.)*

O plano foi executado na íntegra: os sete chunks reescritos (`fig-overfitting`, `split-data`, `train-test-split`, `verifica-split` na 8.3; `fig-variancia` e `fig-mais-dados` na 8.5), os dois blocos ```` ```python ```` do callout da 8.6, e toda a prosa `antes → depois` de §3. `scratch_np/machine_learning.py` não mudou; `index.qmd`, 8.1 e 8.2 não foram abertos. Os corpos de `split_data` e `train_test_split` no chunk foram comparados com o módulo por script (regex `^def nome(...)` até a próxima linha em coluna zero): **idênticos byte a byte**. Todo número de §3 foi remedido nesta execução, e os três PNGs foram gerados no scratchpad e inspecionados antes de a prosa ser dada por verdadeira.

1. **Nenhum número do plano mudou.** `fig-overfitting` com `default_rng(12)`: `ys` = 4,99 · 7,56 · 8,49 · 9,77 · 12,14 · 10,05 · 12,05 · 12,52 · 15,27 · 17,90; `media` 11,07; `intercept` 1,21, `slope` 1,82; `ylim` (−1,01 · 23,90); grau 9 mínimo −3,81 em x ≈ 2,69 e máximo 28,64 em x ≈ 8,16; máximo 10,00 entre o 2.º e o 3.º pontos, 13,28 entre o 7.º e o 8.º, mínimo 11,37 entre o 8.º e o 9.º. `fig-variancia` com `default_rng(7)`: inclinações 1,99 · 1,61 · 2,15 · 2,24 · 2,20 · 2,08 · 1,96 · 2,22 · 2,17 · 1,58; extremos dos polinômios −4,4 a 5,6 (mínimos) e 15,3 a 31,1 (máximos), quatro escapando por baixo e duas por cima. `fig-mais-dados`: inclinações 1,59–2,55 (N = 10), 1,84–2,06 (N = 100), 1,97–2,04 (N = 1000). Callout da 8.6: `scores_` `[0.165, 1.695, 1.160]`, `get_support` `[False, True, True]`; importâncias `[0.524, 0.395, 0.081]`, `[True, True, False]`; 0,81 e 0,45; **116/300** sementes (1000–1299). `split_data(np.arange(1000), 0.75, default_rng(0))` → primeiras linhas do treino `[459 206 222 162 711]`, `data` e `mensagens` intocados depois da chamada. `sklearn.train_test_split` sem `random_state`, com `default_rng(42)` criado antes: duas chamadas diferem; com `random_state=42`, iguais — como o callout novo afirma.

2. **Diferença entre `lagrange` vetorizada e a versão em laços duplos: 3,2 × 10⁻¹⁴**, não os 4,3 × 10⁻¹⁴ de §3 (a versão de referência aqui recebeu `list(xs)`/`list(ys)`; a do planejador provavelmente arrays — o último bit muda com a ordem das multiplicações). Nada na prosa cita esse número; fica registrado para o revisor não estranhar.

3. **Ordem dos dois callouts depois do chunk `split-data`.** §3 pede um parágrafo novo (o `isinstance`) "imediatamente depois do chunk", o callout-note reescrito, e "um novo callout, depois do parágrafo do `isinstance`" com a justificativa da semente — o que deixa dois callouts consecutivos sem dizer qual vem primeiro. Ficou: parágrafo do `isinstance` → callout-note (*"Repare no que a função **não** faz"*, que comenta o `assert` do fim do chunk) → callout *A semente não é opcional* (cuja última frase, *"Todo capítulo daqui em diante divide dados assim"*, fecha o assunto antes de o texto passar ao `train_test_split`).

4. **`X = TypeVar('X')` fica no chunk `split-data` sem ser usado**, como o plano manda e como o módulo o tem — as anotações passaram a `np.ndarray` e nenhuma função o cita. Não é desvio; registro porque é o tipo de linha que o revisor pode apontar como "código que a prosa não explica". Se for cortar, cortar dos dois lugares (chunk e módulo) na mesma passagem, para a regra "o módulo é o chunk salvo em arquivo" continuar literal.

5. **Verificação (§5).** `gerar-notebooks.py` alterou só `notebooks/cap08-machine-learning.ipynb`; `executar-secoes.py 8` → 8.3 (4 chunks, 1,1 s), 8.4 (4 chunks, 0,9 s), 8.5 (2 chunks, 1,1 s), as outras três sem chunk executável, todas OK; `executar-notebooks.py cap08` → OK em 1,5 s; `pytest tests/ -q` → **52 passed** antes e depois de `CAPITULOS_NUMPY = {6, 7, 8}`. `PYTHON_PURO_PERMITIDO` e `RANDOM_PERMITIDO` continuam sem entrada para o capítulo 8. `grep -n "random\.\|scratch\." content/cap08/*.qmd`, descontados `np.random.default_rng`, `random_state=` e `scratch_np.`, devolve só as duas anotações `rng: np.random.Generator` das assinaturas; `grep -rni "python puro" content/cap08/` está vazio. Saída real das células, lida via `nbclient`: `split-data` → `(750, 250)`; `verifica-split` → `(750, 250)`; `acuracia` → `0.98114`; `precisao` → `0.014`; `revocacao` → `0.005`; `f1` → `0.00736842105263158`.

---

## Correções da revisão

*(Implementador, 2026-09-07. Revisão: "aprovado com correções menores", cinco itens.)*

1. **8.3, frase sobre a 8.5 (item obrigatório).** *"cada uma escapa da moldura num lugar diferente"* era falso. A redação sugerida pela revisão (*"quatro delas ainda escapam da moldura, cada uma num lugar diferente"*) foi conferida contra os números e o PNG e também não vale: as quatro curvas que saem da moldura `(0, 22)` saem **todas por baixo, na mesma zona** (x ∈ 2,5–2,9, logo depois do primeiro ponto), e duas delas saem **também por cima**, perto do último ponto (x ∈ 7,9–8,3). Ficou: *"as curvas discordam entre si em toda a faixa, e quatro delas ainda escapam da moldura nas pontas — todas por baixo, logo depois do primeiro ponto, e duas também por cima, antes do último. É essa discordância que dá nome à variância."*
2. **Docstring de `split_data`** (chunk e módulo): *"Sequência qualquer (lista de mensagens, de pontos rotulados)"* → *"Sequência qualquer (uma lista de mensagens)"*. Decorrência: a frase da 8.3 que dizia *"O Capítulo 9 importa `split_data` para separar as flores do Iris"* deixaria de ser verdade com o Iris entrando como matriz + rótulos via `train_test_split` (informação do orquestrador); passou a *"O Capítulo 9 importa `train_test_split` para dividir as flores do Iris — a matriz de medidas e o array de espécies, pelos mesmos índices —, o Capítulo 10 importa `split_data` para separar a lista de mensagens de e-mail, e o Capítulo 13 volta ao `train_test_split`"*. **Se o plano do cap. 9 fizer diferente, é esta frase que se ajusta.**
3. **`X = TypeVar('X')` e `TypeVar` saíram** do chunk `split-data` e do módulo (lá saiu também o `Sequence` sem uso). `Tuple` fica nos dois — é a anotação de retorno de `train_test_split`. Nenhuma assinatura do contrato mudou; `test_pacote_importa_sem_rede_e_sem_erro` passa. O item 4 dos *Desvios* fica superado.
4. **Comentário no `x[:, None]`** de `lagrange`, nos dois chunks: `# x[:, None] põe a grade em coluna (401, 1) para casar com xs[outros] (9,)` na 8.3 e `(301, 1)` na 8.5 — as grades reais de cada figura.
5. **`f1_score` na 8.4** perdeu a linha em branco antes do `return`; chunk e módulo idênticos.

**Verificação:** corpos das seis funções (`split_data`, `train_test_split`, `accuracy`, `precision`, `recall`, `f1_score`) comparados por script com o módulo — todos idênticos; `grep -n "TypeVar\|Sequence" content/cap08/*.qmd scratch_np/machine_learning.py` vazio; `gerar-notebooks.py` alterou só o notebook do cap. 8; `executar-secoes.py 8` → 8.3 (4 chunks, 1,3 s), 8.4 (4, 0,9 s), 8.5 (2, 1,1 s) OK; `executar-notebooks.py cap08` → OK em 1,4 s; `pytest tests/ -q` → **52 passed**.
