# Capítulo 7 — Trabalhando com Dados: plano da reescrita em numpy

**Grus:** capítulo 10, *Working with Data*. **Nosso:** `content/cap07/`, 8 seções + `index.qmd`.
**Spec:** `docs/superpowers/specs/2026-09-06-reescrita-numpy-design.md`. **Instruções:** `2026-09-06-numpy-00-instrucoes.md`.
**Posição na fila:** segundo capítulo reescrito, depois do 6 (commits `7f029ff` e `0842282`; plano `2026-09-06-numpy-cap06.md`, inclusive "Desvios na implementação" e "Correções da revisão").

**O que este capítulo herda do 6, e o que deve a ele.** A seção 6.1 fixou o vocabulário do primeiro contato — `np.loadtxt`, `shape`, `dtype`, `X[:, j]`, `v - w`, `2 * v`, `.max()/.min()/.mean()`, `np.isnan` — e **deliberadamente deixou de fora** máscaras booleanas, `axis`, *broadcasting* com vetor e `argmax`, para que o bloco *De listas a arrays* os apresentasse. A 6.1 remete a esse bloco pelo título exato (*"o assunto do bloco *De listas a arrays* que abre a seção 7.1"*), então **título e posição são compromisso**. O plano do 6 também deixou uma pendência explícita: mostrar `np.genfromtxt` sobre `dados/comma_delimited_stock_prices.csv` na 7.4 como contraponto ao `try_parse_row` — decisão tomada aqui (§3.4: sim, num callout de nove linhas).

Duas convenções que este plano **inaugura** para os capítulos seguintes, porque nenhuma delas apareceu no 6 (que não tem sorteio nem modelo): o jeito de apresentar `rng = np.random.default_rng(semente)` (§3.1, chunk `distribuicoes-fake`) e o padrão "a seção implementa, a próxima importa de `scratch_np.`" (§3.5, `try_parse_row` importado de `scratch_np.working_with_data`).

## O que este capítulo é, e o que a reescrita faz com ele

O capítulo 7 é o primeiro com **dado numérico multidimensional** — histograma (1D), duas séries (2D), matriz de correlação (n-D), preços de quatro ações, reescalonamento por coluna, PCA. É exatamente onde a spec manda o numpy nascer para o leitor, e por isso a 7.1 abre com o bloco de apresentação. Mas metade do capítulo **não é número**: 7.2 (`NamedTuple`), 7.3 (`dataclass`) e 7.4 (`parse_row` com `dateutil`) são sobre o registro heterogêneo — símbolo, data, preço —, que é justamente o que um array, com seu `dtype` único, **não** guarda. A regra da spec lida ao contrário decide essas seções: onde não há conta, não há numpy. A reescrita, em vez de esconder isso, usa-o como argumento — o bloco *De listas a arrays* termina explicando por que um `str`, uma data e um `float` não cabem num array, e a 7.2 abre dizendo que é para esse caso que o `NamedTuple` existe.

A regra de ouro fica em duas frases: **o registro é heterogêneo até a fronteira do parsing; do parsing para dentro, cada coluna numérica é um array.** A 7.5 é onde isso se vê: `try_parse_row` continua devolvendo `StockPrice`, e a linha seguinte desempacota a lista em três arrays paralelos (`symbols`, `dates`, `closes`).

Hoje o capítulo importa `scratch.` em seis chunks (`probability`, `statistics` ×2, `linear_algebra` ×6 funções, `gradient_descent`), usa `random` da stdlib em quatro (7.1 ×3, 7.7 ×1), tem **oito figuras** (4 na 7.1, 4 na 7.8) e afirma um punhado de números que dependem de sorteio (as correlações 0,906/-0,907 da 7.1) ou não (tudo na 7.5, os `assert`s da 7.6, a componente (0,924, 0,383) da 7.8). O inventário abaixo lista cada um. A PCA é **determinística** (`guess` começa em uns) e a versão em arrays reproduz a componente do Grus a 6×10⁻¹⁷ — medido em §4.

---

## 1. Inventário

Convenção das tabelas: *exec* = chunk `{python}` que executa; *não exec* = `eval: false` ou bloco ```` ```python ```` de prosa. Números medidos no host (`.venv/`, numpy 2.5.2, Python 3.12.13) em 2026-09-07.

### `index.qmd` (47 linhas)

Sem chunks, sem figuras. Números: nenhum além de "oito seções".

Frases que descrevem código em listas/Python e mudam:

- Objetivo 5: *"Agrupar, ordenar e agregar dados tabulares com `defaultdict` e compreensões, no mesmo estilo do [Capítulo 2](../cap02/index.qmd)"* — a 7.5 passa a agrupar com máscaras booleanas e `argsort`.
- Objetivo 7: *"Reduzir a dimensionalidade de um conjunto de dados com PCA, construído do zero sobre o gradiente descendente do [Capítulo 5](../cap05/index.qmd)"* — continua verdadeiro; ganha "em arrays".
- Falta um objetivo para o bloco novo (mesmo critério do cap. 6: a seção ganhou um bloco que a lista omitiria).
- Parágrafo de abertura (*"O Capítulo 5 construiu a máquina... O Capítulo 6 construiu os canos..."*) — continua verdadeiro; ganha uma frase dizendo que é aqui que o array entra de vez.

### `01-explorando-seus-dados.qmd` (228 linhas)

| Label | exec? | O que faz | Importa de `scratch.` |
|---|---|---|---|
| `funcoes-histograma` | exec | `bucketize` (`math.floor`), `make_histogram` (`Counter`) | — |
| `funcao-plot-histograma` | exec | `plot_histogram` com `plt.bar(histogram.keys(), histogram.values(), ...)` | — |
| `import-probability` | exec, `include: false` | `from scratch.probability import inverse_normal_cdf`; `plt.close('all')` | `probability` |
| `distribuicoes-fake` | exec | `random_normal()`, `random.seed(0)`, `uniform`/`normal` por compreensão de 10.000; f-string das médias | — |
| `fig-histograma-uniforme` | exec, figura | `plot_histogram(uniform, 10, ...)` | — |
| `fig-histograma-normal` | exec, figura | `plot_histogram(normal, 10, ...)` | — |
| `xs-ys1-ys2` | exec | `random.seed(4)`; `xs`, `ys1`, `ys2` por compreensão de 1.000 | — |
| `fig-dispersao-conjunta` | exec, figura | `plt.scatter(xs, ys1, ...)` ×2 | — |
| `import-statistics` | exec, `include: false` | `from scratch.statistics import correlation`; `plt.close('all')` | `statistics` |
| `correlacoes` | exec | `correlation(xs, ys1), correlation(xs, ys2)` | — |
| `matriz-de-correlacao` | exec | `correlation_matrix` via `make_matrix` | `linear_algebra` (`Matrix`, `Vector`, `make_matrix`) |
| `fig-dispersao-matriz` | exec, figura | `random_row()`, `random.seed(0)`, `corr_rows` → `corr_data = [list(col) for col in zip(*corr_rows)]`; grade 4×4 de `scatter` | — |
| `matriz-de-correlacao-valores` | exec | `print([round(v, 2) for v in linha])` por linha | — |

Números afirmados e de onde saem: *"Ambos têm média perto de 0"* e *"desvio padrão próximo de 57"* ← saída de `distribuicoes-fake` (sorteio); *"cerca de 0,906 e -0,907"* (callout) ← `correlacoes` com `random.seed(4)` (**sorteio: muda**); *"gira em torno de 0,894"*, *"(0.89, 0.91)"*, *"cerca de uma vez em cada quatro execuções (medido: 24,6% e 25,0% em duas séries independentes de 20 mil repetições)"* ← medição registrada no `CLAUDE.md` sobre a construção do Grus (propriedade da distribuição, não do gerador — **continua**; §4 remede com o gerador do numpy). A matriz impressa em `matriz-de-correlacao-valores` não é repetida na prosa; a prosa só faz afirmações qualitativas (*"fortemente anticorrelacionada"*, *"positivamente correlacionada"*, *"só assume os valores 0 e 6"*), que precisam continuar valendo com o sorteio novo (conferido em §3.1).

Figuras: 4.

Frases que descrevem código em listas/Python puro e deixam de ser verdade:

- `.exemplo`: *"Repare que `make_histogram` é a mesma ideia do histograma de notas do [Capítulo 3](../cap03/02-graficos-de-barras.qmd): um `Counter` sobre valores arredondados para o piso do bucket. A única diferença é que aqui o arredondamento vira uma função nomeada, `bucketize`, em vez de uma expressão solta como `grade // 10 * 10`"* — o `Counter` vira `np.unique(..., return_counts=True)`.
- *"Chama-se `inverse_normal_cdf` e vem de `scratch/probability.py`."* — passa a vir de `scratch_np.probability`, vetorizada.
- Callout *Uma afirmação sobre dado aleatório precisa de semente*: o bloco `assert 0.89 < correlation(xs, ys1) < 0.91` e *"Por isso `random.seed(4)` aparece aqui em cima, antes de `xs`: com a semente fixada, a correlação sai sempre a mesma — cerca de 0,906 e -0,907 — em qualquer execução."*
- Comentário *"cada linha tem 4 pontos, mas queremos as colunas"* e a linha `corr_data = [list(col) for col in zip(*corr_rows)]` — a transposição some: as séries nascem como colunas.
- Callout `pandas`/`seaborn`: *"`.corr()` por padrão calcula a correlação de Pearson — a mesma que você implementou —"* — ninguém implementa correlação neste capítulo agora; é `np.corrcoef`.

### `02-namedtuples.qmd` (104 linhas)

| Label | exec? | O que faz |
|---|---|---|
| `dict-stock-price` | exec | `stock_price` como `dict` |
| `dict-typo` | exec | `'cosing_price'` — erro silencioso |
| `dict-anotado` | exec | `prices: Dict[datetime.date, float] = {}` |
| `namedtuple-simples` | exec | `collections.namedtuple`; 2 `assert` |
| `namedtuple-tipado` | exec | `typing.NamedTuple` com `is_high_tech`; 3 `assert` |

Nenhum import de `scratch.`, nenhum `random`, nenhuma figura, nenhum número além dos literais do código (`102.06`, `103.06`, `106.03`). Sem "Python puro".

Uma frase de **risco**, não de listas: o `.exemplo` *"Você vai ver este exato padrão de novo no [Capítulo 9](../cap09/01-o-modelo.qmd), sobre k-vizinhos mais próximos: `class LabeledPoint(NamedTuple): point: Vector; label: str`"* — o cap. 9 será reescrito, a spec proíbe o alias `Vector` em `scratch_np/`, e um k-NN em arrays pode nem ter `LabeledPoint`. Trocar por um exemplo que a spec **garante**: `Message(NamedTuple)` de `scratch_np/naive_bayes.py` (cap. 10), que hoje já é `text: str; is_spam: bool` em `content/cap10/03-implementacao.qmd`.

### `03-dataclasses.qmd` (67 linhas)

| Label | exec? | O que faz |
|---|---|---|
| `dataclass-stock-price` | exec | `@dataclass StockPrice2`; 3 `assert` |
| `dataclass-mutavel` | exec | `price2.closing_price /= 2`; `assert` |
| `dataclass-mesmo-erro` | exec | `price2.cosing_price = 75`; `hasattr` ×2 |

Nada de `scratch.`, `random`, figura, número, "Python puro". **Não muda.**

### `04-limpeza-e-transformacao.qmd` (123 linhas)

| Label | exec? | O que faz |
|---|---|---|
| `parse-row` | exec | `StockPrice(NamedTuple)`, `parse_row` com `dateutil.parser.parse`; 3 `assert` |
| `try-parse-row` | exec | `try_parse_row` (regex + 2 `try/except`); 4 `assert` |
| `mostra-csv-sujo` | exec | `print(f.read())` de `dados/comma_delimited_stock_prices.csv` |
| `le-csv-sujo` | exec | `csv.reader` + `try_parse_row`; `print("pulando linha inválida: ...")`; `len(data), data` |

Números: *"Só uma linha é rejeitada: `MSFT,6/19/2014,n/a`"*, *"As outras cinco entram — inclusive a da FB com data `6/20/3014`"*, *"o ano da data dela é **3014**"* ← saída de `le-csv-sujo` e inspeção do arquivo. **Nenhum muda.** Sem `scratch.`, sem `random`, sem figura, sem "Python puro". Ganha um callout com `np.genfromtxt` (a pendência do cap. 6).

### `05-manipulando-dados.qmd` (212 linhas)

| Label | exec? | O que faz | Importa de `scratch.` |
|---|---|---|---|
| (sem label) | não exec | `data = [StockPrice(...), # ...]` | — |
| `carrega-stocks-completo` | exec | **redefine** `StockPrice` e `try_parse_row`; `csv.DictReader` sobre `dados/stocks.csv`; `assert maybe_data`, `assert all(...)`; `len(data), sorted(set(...))` | — |
| `max-aapl` | exec | compreensão com `if stock_price.symbol == "AAPL"` → `max` | — |
| `max-precos-por-simbolo` | exec | `defaultdict(lambda: float('-inf'))` + laço | — |
| `agrupa-por-simbolo` | exec | `defaultdict(list)` + laço | — |
| `ordena-por-data` | exec | `{symbol: sorted(symbol_prices) ...}` | — |
| `variacoes-dia-a-dia` | exec | `pct_change`, `DailyChange(NamedTuple)`, `day_over_day_changes` com `zip(prices, prices[1:])` | — |
| `todas-as-variacoes` | exec | compreensão dupla → `len(all_changes)` | — |
| `maior-menor-variacao` | exec | `max(..., key=lambda)`, `min(...)` | — |
| `variacao-media-por-mes` | exec | `changes_by_month: Dict[int, List[DailyChange]]`; `avg_daily_change` por compreensão | — |
| `melhor-mes` | exec | `max(avg_daily_change, key=...)`; `assert melhor_mes == 10` | — |

Números afirmados: *"as 23.105 linhas"* (×2) ← `len(data)`; *"quatro ações (AAPL, FB, GOOG, MSFT)"* ← `sorted(set(...))`; *"a AAPL, em 6 de agosto de 1997 — mais de 33% em um único pregão"*, *"29 de setembro de 2000 — quase 52%"* ← `maior-menor-variacao`; *"Outubro ... cerca de 0,29%"* ← `melhor-mes`. `len(all_changes)` = 23.101 aparece só como saída. **Nenhum depende de sorteio; todos foram reproduzidos em arrays (§3.5).** Sem figuras, sem `scratch.`, sem `random`, sem "Python puro".

Frases que descrevem o código em listas e mudam:

- *"Uma compreensão faz as três coisas de uma vez:"*
- *"1. Criar um `dict` para guardar o maior preço de cada símbolo (usando um `defaultdict` que devolve menos infinito para símbolos ainda não vistos, já que qualquer preço será maior que isso). 2. Percorrer os dados, atualizando esse `dict`."*
- *"Uma abordagem é agrupar os preços por símbolo e, dentro de cada grupo: 1. Ordenar os preços por data. 2. Usar `zip` para formar pares (anterior, atual). 3. Transformar os pares em novas linhas de "variação percentual"."*
- *"Como os preços são tuplas, eles são ordenados pelos campos, na ordem: primeiro por símbolo, depois por data, depois por preço. Isso significa que, se temos preços todos com o mesmo símbolo, `sort` os ordena por data (e depois por preço, o que não muda nada, já que só há um preço por data) — que é o que queremos:"*
- *"`zip(prices, prices[1:])` é o mesmo truque de percorrer duas sequências em paralelo que o [Capítulo 2](../cap02/06-ferramentas-e-tipos.qmd) apresentou: aqui, desalinhar uma lista de si mesma por uma posição transforma a sequência inteira numa sequência de pares (ontem, hoje), prontos para `pct_change`."*
- *"e depois juntar todas:"*, *"A partir daqui é fácil achar a maior e a menor:"*, *"Basta olhar a variação diária média por mês:"*
- Callout `.callout-note` inteiro: *"Repare na anotação de `changes_by_month`: `Dict[int, List[DailyChange]]` é o tipo que o valor de fato tem. Se estivesse escrito `List[DailyChange]` ali..."* — a âncora (`changes_by_month`) deixa de existir.
- Callout `pandas`: *"no lugar do `defaultdict` que acha o maior preço por símbolo"*, *"no lugar do `dict` `changes_by_month` construído à mão"*, *"exatamente a etapa `sorted(symbol_prices)` que esta seção gastou um parágrafo justificando antes de calcular `day_over_day_changes`"*.

### `06-reescalonamento.qmd` (138 linhas)

| Label | exec? | O que faz | Importa de `scratch.` |
|---|---|---|---|
| `distancia-polegadas` | exec | `distance([63, 150], [67, 160])` ×3; `round(..., 2)` | `linear_algebra.distance` |
| `distancia-centimetros` | exec | idem em cm | — |
| `import-statistics` | exec, `include: false` | `from scratch.statistics import standard_deviation`; `plt.close('all')` | `statistics` |
| `funcao-scale` | exec | `scale` via `vector_mean` + compreensão por posição; `assert means == [-1, 0, 1]`, `assert stdevs == [2, 1, 0]` | `linear_algebra` (`Vector`, `vector_mean`) |
| `funcao-rescale` | exec | `rescale` com cópia `v[:]` e laço duplo com `if stdevs[i] > 0` | — |
| `testa-rescale` | exec | `assert means == [0, 0, 1]`, `assert stdevs == [1, 1, 0]` | — |

Números: os seis valores de distância (10,77/22,14/11,40 e 14,28/27,53/13,37) só aparecem como saída; a prosa afirma *"o vizinho mais próximo de B é A"* e *"passa a ser C"* ← comparação das saídas (**continua**, verificado: 10,77 < 11,40 e 13,37 < 14,28). Os quatro `assert`s são igualdades **exatas** de listas de inteiros — em arrays, `np.array_equal` com os mesmos valores (verificado exato com `ddof=1`; com o `ddof=0` padrão do numpy, `stdevs` sairia `[1.63, 0.82, 0]` e o `assert` cairia — é o ponto que a prosa nova explica). Sem figuras, sem `random`, sem "Python puro".

Frases que mudam:

- *"Já temos uma função de distância euclidiana — do [Capítulo 4](../cap04/index.qmd) —, então uma abordagem natural é tratar pares (altura, peso) como pontos num espaço bidimensional."*
- *"Para começar, precisamos calcular a média e o desvio padrão de cada posição:"* e o docstring *"devolve a média e o desvio padrão de cada posição"*.
- *"Repare na terceira posição: todo vetor tem 1 ali, então o desvio padrão é 0"*; *"A terceira posição continua com desvio padrão 0"*; docstring de `rescale` (*"cada posição"*).
- Callout `scikit-learn`: *"`fit` calcula média e desvio padrão de cada coluna (o que você fez em `scale`); `transform` aplica a fórmula `(x - média) / desvio` (o que você fez em `rescale`)"* — continua, mas ganha o `ddof` (o `StandardScaler` usa `ddof=0`).

### `07-um-parenteses-tqdm.qmd` (80 linhas)

| Label | exec? | O que faz |
|---|---|---|
| `tqdm-basico` | exec, `warning: false` | `random.seed(0)`; `for i in tqdm.tqdm(range(100)): _ = [random.random() for _ in range(1_000_000)]` |
| `primes-up-to` | exec, `warning: false` | `primes_up_to(100_000)` com `tqdm.trange` + `set_description`; `len(my_primes)` |

Números: *"56%|... | 56/100 [00:08<00:06, 6.49it/s]"* e *"5088 primos: 50%|... 49529/99997 [00:03<00:03, 15905.90it/s]"* são amostras ilustrativas (*"algo como"*) — a primeira muda porque a iteração muda de custo; `len(my_primes)` = 9.592 só como saída. Sem `scratch.`, sem figura, sem "Python puro". Frases que mudam: o comentário `# faz algo lento` (a iteração nova é outra) e a linha-amostra da barra.

### `08-reducao-de-dimensionalidade.qmd` (394 linhas)

| Label | exec? | O que faz | Importa de `scratch.` |
|---|---|---|---|
| `pca-data` | exec, `echo: false` | lista literal de **99** pares; `len(pca_data)` | — |
| `fig-pca-brutos` | exec, figura | `xs = [p[0] for p in pca_data]` etc. | — |
| `de-mean` | exec | `de_mean` via `vector_mean` + `subtract` | `linear_algebra` (`Vector`, `subtract`, `vector_mean`) |
| `fig-pca-demeaned` | exec, figura | `xs2`, `ys2` por compreensão | — |
| `direction` | exec | `direction` via `magnitude` | `linear_algebra.magnitude` |
| `directional-variance` | exec | `sum(dot(v, w_dir) ** 2 for v in data)` | `linear_algebra.dot` |
| `directional-variance-gradient` | exec | compreensão dupla `[sum(2 * dot(v, w_dir) * v[i] for v in data) for i in range(len(w))]` | — |
| `first-principal-component` | exec, `warning: false` | `guess = [1.0 for _ in data[0]]`; `tqdm.trange`; `gradient_step(guess, gradient, step_size)` **positivo**; `assert 0.923 < fpc[0] < 0.925`, `assert 0.382 < fpc[1] < 0.384` | `gradient_descent.gradient_step` |
| `fig-pca-primeiro-componente` | exec, figura | reta `[-escala * fpc[0], escala * fpc[0]]` | — |
| `project` | exec | `scalar_multiply(dot(v, w), w)` | `linear_algebra.scalar_multiply` |
| `remove-projection` | exec | `remove_projection_from_vector`, `remove_projection` por compreensão | — |
| `fig-pca-residual` | exec, figura | `xs3`, `ys3` por compreensão | — |
| `pca` | exec | laço sobre componentes (fica) | — |
| `transform` | exec, `warning: false` | `transform_vector` (compreensão), `transform`; `pca(de_meaned, 2)`; `transformado[:5]` | — |

Números afirmados: *"aproximadamente $(0{,}924,\ 0{,}383)$"* ×3 (prosa e callout) ← `fpc` (determinístico; **reproduzido a 6×10⁻¹⁷** em arrays); *"os quatro solvers de `PCA` sobre estes mesmos dados, todos devolvem $(0{,}924,\ 0{,}383)$"* ← verificação anterior com `sklearn`, dados inalterados (**continua**). `pca_data` tem **99** linhas (contado no arquivo: `grep -c "^\[[0-9-]"`), não 100 — a prosa nunca diz 100, e o chunk novo imprime `pca_data.shape` = `(99, 2)`.

Figuras: 4. `test_gradiente.py` registra este arquivo como **a única subida de gradiente do livro** pelo caminho `cap07/08-reducao-de-dimensionalidade.qmd` e pelo terceiro argumento de `gradient_step(...)` não começar com `-`: a chamada `gradient_step(guess, gradient, step_size)` **fica exatamente assim**, importando de `scratch_np.gradient_descent`; nada muda no teste.

Frases que descrevem o código em listas e mudam:

- Callout: *"Esta seção depende do gradiente descendente do [Capítulo 5](../cap05/index.qmd) — especificamente de `gradient_step`, a função de cinco linhas construída lá que treina praticamente todo modelo deste livro."*
- *"a projeção de cada linha $x$ da matriz sobre $d$ tem comprimento `dot(x, d)`"*; *"o `dot(v, w)` que a seção "Projetando e removendo", adiante, vai calcular"*.
- Callout do gradiente: *"exatamente o `2 * dot(v, w_dir) * v[i]` acima, somado sobre todos os pontos"*.
- Callout `important`: *"é o mesmo `gradient_step` de cinco linhas do Capítulo 5, usado ao contrário"*.
- Callout `note`: *"`guess` começa sempre em `[1.0, 1.0]`... sem precisar de `random.seed`"*.
- *"depois de removermos a primeira componente o que sobra é efetivamente unidimensional"* — continua.
- Callout `scikit-learn`: *"O que você construiu acima acha uma componente de cada vez, por gradiente descendente..."* — continua; ganha uma frase dizendo que trocar listas por arrays não muda o método.

---

## 2. O módulo `scratch_np/working_with_data.py`

O `scratch/working_with_data.py` do Grus é **não-importável** aqui (`NAO_IMPORTAVEIS` em `tests/test_scratch.py`: abre `stocks.csv` relativo ao cwd e afirma uma correlação sem semente no corpo do módulo). O nosso não tem efeito colateral nenhum no import: não lê arquivo, não sorteia sem semente, não desenha, **não escreve em stderr** — o `assert` da PCA repete o laço de `first_principal_component` sem o `tqdm`, porque um `tqdm.trange` no import apareceria como saída em toda célula que fizesse `from scratch_np.working_with_data import ...` (a 7.5 faz).

O dataset dos `assert`s de PCA é construído para ter direção principal **conhecida por construção** — soma de quadrados 2000 ao longo de $u = (1, 2)/\sqrt{5}$ e 126 ao longo da perpendicular, com `t` e `s` ortogonais para não haver termo cruzado em $X^\top X$ (sem isso, $u$ não é autovetor; eu tropecei nisso ao montar o teste). A tolerância `atol=1e-3` é o que 100 passos de subida com passo 0,1 entregam nesse conjunto (medido: erro 2,6×10⁻⁴).

Este módulo **não** reexporta dado do Grus: `pca_data` continua literal no `.qmd` (`echo: false`, como hoje), porque o módulo do Grus não pode ser importado para pegá-lo, e os `assert`s do módulo não precisam dele. `REEXPORTA_DADOS` em `tests/test_scratch_np.py` **não** ganha entrada.

Código completo, validado no host (importa com todos os `assert`s em 0,5 s):

```python
"""Trabalhando com dados em numpy — a contraparte em arrays de `scratch/working_with_data.py`.

O módulo do Grus (capítulo 10) NÃO é importável neste projeto: abre `stocks.csv`
com caminho relativo ao cwd e afirma uma correlação sem semente, tudo no corpo
do módulo (`NAO_IMPORTAVEIS`, em tests/test_scratch.py). Este aqui é o mesmo
código, em arrays, sem efeito colateral no import: nenhum arquivo é lido,
nenhum sorteio sem semente é feito, nenhuma figura é desenhada.

Convenções: `X` tem forma (n, d) — uma linha por observação, uma coluna por
atributo; `ddof=1` onde o Grus usa a fórmula amostral (`standard_deviation`);
toda função estocástica recebe `rng: np.random.Generator` obrigatório.

O capítulo 7 escreve cada função inline na seção que a ensina; as seções e os
capítulos seguintes importam daqui O MESMO código. Os `assert`s abaixo rodam no
import — o render é o teste.
"""
import datetime
import re
from typing import List, NamedTuple, Optional, Tuple

import numpy as np
import tqdm
from dateutil.parser import parse
from matplotlib import pyplot as plt

from scratch_np.gradient_descent import gradient_step
from scratch_np.probability import inverse_normal_cdf


# --------------------------------------------------------------------------
# 7.1 Explorando seus dados
# --------------------------------------------------------------------------

def bucketize(points, bucket_size: float) -> np.ndarray:
    """Arredonda cada ponto para baixo até o múltiplo mais próximo de bucket_size"""
    return bucket_size * np.floor(np.asarray(points, dtype=float) / bucket_size)


def make_histogram(points, bucket_size: float) -> Tuple[np.ndarray, np.ndarray]:
    """Distribui os pontos nos buckets e conta quantos caem em cada um.

    Devolve dois arrays alinhados — os buckets, em ordem, e as contagens —,
    o par que `plt.bar` recebe direto. É o `Counter` do Grus, em arrays.
    """
    buckets, counts = np.unique(bucketize(points, bucket_size), return_counts=True)
    return buckets, counts


assert np.array_equal(bucketize([-3.2, 0.5, 7.9, 10.0], 5), [-5, 0, 5, 10])
_buckets, _counts = make_histogram([0.0, 10.0, 0.0, -10.0], 10)
assert np.array_equal(_buckets, [-10, 0, 10]) and np.array_equal(_counts, [1, 2, 1])


def plot_histogram(points, bucket_size: float, title: str = ""):
    buckets, counts = make_histogram(points, bucket_size)
    plt.bar(buckets, counts, width=bucket_size)
    plt.xlabel("valor")
    plt.ylabel("frequência")
    plt.title(title)


def random_normal(n: int, rng: np.random.Generator) -> np.ndarray:
    """n amostras da normal padrão, pela inversa da acumulada"""
    return inverse_normal_cdf(rng.random(n))


_amostra = random_normal(2000, np.random.default_rng(0))
assert _amostra.shape == (2000,)
assert abs(_amostra.mean()) < 0.1 and abs(_amostra.std(ddof=1) - 1) < 0.1


def correlation_matrix(X: np.ndarray) -> np.ndarray:
    """
    Devolve a matriz d x d cuja entrada (i, j) é a correlação
    entre a coluna i e a coluna j de X.

    `np.corrcoef` assume, por padrão, uma variável POR LINHA (`rowvar=True`);
    a convenção deste livro é uma variável por coluna — daí o `rowvar=False`.
    """
    return np.corrcoef(np.asarray(X, dtype=float), rowvar=False)


_X = np.array([[1.0, 2.0], [2.0, 4.0], [3.0, 6.5]])
_M = correlation_matrix(_X)
assert _M.shape == (2, 2) and np.allclose(np.diag(_M), 1)
assert np.isclose(_M[0, 1], np.corrcoef(_X[:, 0], _X[:, 1])[0, 1])


# --------------------------------------------------------------------------
# 7.2 NamedTuples e 7.4 Limpeza — o registro heterogêneo (texto, data, número)
# não é array; é o que fica de fora dele, de propósito.
# --------------------------------------------------------------------------

class StockPrice(NamedTuple):
    symbol: str
    date: datetime.date
    closing_price: float

    def is_high_tech(self) -> bool:
        """É uma classe — então também dá para acrescentar métodos"""
        return self.symbol in ['MSFT', 'GOOG', 'FB', 'AMZN', 'AAPL']


_price = StockPrice('MSFT', datetime.date(2018, 12, 14), 106.03)
assert _price.symbol == 'MSFT'
assert _price.closing_price == 106.03
assert _price.is_high_tech()


def parse_row(row: List[str]) -> StockPrice:
    symbol, date, closing_price = row
    return StockPrice(symbol=symbol,
                      date=parse(date).date(),
                      closing_price=float(closing_price))


_stock = parse_row(["MSFT", "2018-12-14", "106.03"])
assert _stock.symbol == "MSFT"
assert _stock.date == datetime.date(2018, 12, 14)
assert _stock.closing_price == 106.03


def try_parse_row(row: List[str]) -> Optional[StockPrice]:
    symbol, date_, closing_price_ = row

    # o símbolo da ação deveria ser só letras maiúsculas
    if not re.match(r"^[A-Z]+$", symbol):
        return None

    try:
        date = parse(date_).date()
    except ValueError:
        return None

    try:
        closing_price = float(closing_price_)
    except ValueError:
        return None

    return StockPrice(symbol, date, closing_price)


# deveria devolver None para erros
assert try_parse_row(["MSFT0", "2018-12-14", "106.03"]) is None  # símbolo ruim
assert try_parse_row(["MSFT", "2018-12--14", "106.03"]) is None  # data ruim
assert try_parse_row(["MSFT", "2018-12-14", "x"]) is None        # preço ruim

# mas deveria devolver o mesmo de antes se o dado é bom
assert try_parse_row(["MSFT", "2018-12-14", "106.03"]) == _stock


# --------------------------------------------------------------------------
# 7.5 Manipulando dados
# --------------------------------------------------------------------------

def day_over_day_changes(closes: np.ndarray) -> np.ndarray:
    """Variações percentuais dia a dia. Assume preços de uma única ação, em ordem."""
    closes = np.asarray(closes, dtype=float)
    return closes[1:] / closes[:-1] - 1


assert np.allclose(day_over_day_changes([100.0, 110.0, 99.0]), [0.1, -0.1])


# --------------------------------------------------------------------------
# 7.6 Reescalonamento
# --------------------------------------------------------------------------

def scale(X: np.ndarray) -> Tuple[np.ndarray, np.ndarray]:
    """devolve a média e o desvio padrão de cada coluna"""
    X = np.asarray(X, dtype=float)
    means = X.mean(axis=0)
    stdevs = X.std(axis=0, ddof=1)
    return means, stdevs


def rescale(X: np.ndarray) -> np.ndarray:
    """
    Reescalona os dados de entrada para que cada coluna tenha
    média 0 e desvio padrão 1. (Deixa uma coluna como está se
    o desvio padrão dela é 0.)
    """
    X = np.asarray(X, dtype=float)
    means, stdevs = scale(X)

    rescaled = X.copy()                 # não mexe no original
    varia = stdevs > 0                  # máscara sobre as colunas
    rescaled[:, varia] = (X[:, varia] - means[varia]) / stdevs[varia]
    return rescaled


_vectors = np.array([[-3, -1, 1], [-1, 0, 1], [1, 1, 1]], dtype=float)
_means, _stdevs = scale(_vectors)
assert np.array_equal(_means, [-1, 0, 1])
assert np.array_equal(_stdevs, [2, 1, 0])

_means, _stdevs = scale(rescale(_vectors))
assert np.array_equal(_means, [0, 0, 1])
assert np.array_equal(_stdevs, [1, 1, 0])
assert np.array_equal(_vectors[0], [-3, -1, 1])   # o original ficou intacto


# --------------------------------------------------------------------------
# 7.8 Redução de dimensionalidade (PCA por subida de gradiente)
# --------------------------------------------------------------------------

def de_mean(X: np.ndarray) -> np.ndarray:
    """Recentra os dados para que cada coluna tenha média 0"""
    return X - X.mean(axis=0)


def direction(w: np.ndarray) -> np.ndarray:
    return w / np.linalg.norm(w)


def directional_variance(X: np.ndarray, w: np.ndarray) -> float:
    """Devolve a variância de X na direção de w"""
    w_dir = direction(w)
    return np.sum((X @ w_dir) ** 2)


def directional_variance_gradient(X: np.ndarray, w: np.ndarray) -> np.ndarray:
    """O gradiente da variância direcional em relação a w"""
    w_dir = direction(w)
    return 2 * (X @ w_dir) @ X


def first_principal_component(X: np.ndarray,
                              n: int = 100,
                              step_size: float = 0.1) -> np.ndarray:
    # começa com um palpite aleatório
    guess = np.ones(X.shape[1])

    with tqdm.trange(n) as t:
        for _ in t:
            dv = directional_variance(X, guess)
            gradient = directional_variance_gradient(X, guess)
            guess = gradient_step(guess, gradient, step_size)
            t.set_description(f"dv: {dv:.3f}")

    return direction(guess)


def project(v: np.ndarray, w: np.ndarray) -> np.ndarray:
    """devolve a projeção de v na direção w"""
    projection_length = v @ w
    return projection_length * w


def remove_projection_from_vector(v: np.ndarray, w: np.ndarray) -> np.ndarray:
    """projeta v em w e subtrai o resultado de v"""
    return v - project(v, w)


def remove_projection(X: np.ndarray, w: np.ndarray) -> np.ndarray:
    """remove de cada linha de X a projeção dela em w"""
    return X - np.outer(X @ w, w)


def pca(X: np.ndarray, num_components: int) -> np.ndarray:
    """Devolve as componentes como linhas de uma matriz (num_components, d)"""
    components = []
    for _ in range(num_components):
        component = first_principal_component(X)
        components.append(component)
        X = remove_projection(X, component)

    return np.array(components)


def transform_vector(v: np.ndarray, components: np.ndarray) -> np.ndarray:
    return components @ v


def transform(X: np.ndarray, components: np.ndarray) -> np.ndarray:
    return X @ components.T


# Um conjunto com direção principal conhecida POR CONSTRUÇÃO: soma de quadrados
# 2000 ao longo de u = (1, 2)/√5 e 126 ao longo da perpendicular p. `t` e `s`
# somam zero (já centrado) e são ortogonais (sem termo cruzado em XᵀX), então
# u é exatamente a primeira componente e p a segunda.
_u = np.array([1.0, 2.0]) / np.sqrt(5)
_p = np.array([-2.0, 1.0]) / np.sqrt(5)
_t = np.array([-3.0, -1.0, 0.0, 1.0, 3.0]) * 10
_s = np.array([1.0, -2.0, 2.0, -2.0, 1.0]) * 3
_X = np.outer(_t, _u) + np.outer(_s, _p)

assert np.allclose(de_mean(_X), _X)
assert np.isclose(np.linalg.norm(direction(np.array([3.0, 4.0]))), 1)
assert np.isclose(directional_variance(_X, _u), 2000)
assert np.isclose(directional_variance(_X, 2 * _u), 2000)      # só a direção importa
assert np.allclose(project(np.array([1.0, 1.0]), np.array([1.0, 0.0])), [1, 0])
assert np.allclose(remove_projection_from_vector(np.array([1.0, 1.0]), np.array([1.0, 0.0])), [0, 1])

# A subida do gradiente, sem a barra de progresso — é o laço de
# `first_principal_component`, que escreveria em stderr a cada import.
_w = np.ones(2)
for _ in range(100):
    _w = gradient_step(_w, directional_variance_gradient(_X, _w), 0.1)
_c1 = direction(_w)
assert np.allclose(np.abs(_c1), _u, atol=1e-3)

_R = remove_projection(_X, _c1)
assert np.allclose(_R @ _c1, 0)                                  # nada sobrou na direção de c1
assert np.allclose(np.abs(transform(_X, np.array([_c1]))[:, 0]), np.abs(_t), atol=0.05)
assert np.allclose(transform_vector(_X[0], np.array([_c1])), transform(_X, np.array([_c1]))[0])
```

**Contratos que este módulo fixa para quem importa depois** (a spec não os lista; ficam aqui):

```python
scale(X: np.ndarray) -> tuple[np.ndarray, np.ndarray]   # (means (d,), stdevs (d,)), ddof=1
rescale(X: np.ndarray) -> np.ndarray                     # (n, d); coluna com desvio 0 fica intacta
random_normal(n: int, rng: np.random.Generator) -> np.ndarray   # (n,)
pca(X: np.ndarray, num_components: int) -> np.ndarray    # (k, d): uma componente por LINHA
transform(X: np.ndarray, components: np.ndarray) -> np.ndarray  # (n, k)
```

O cap. 13 hoje reescreve `scale`/`rescale` inline (`content/cap13/01-o-problema.qmd`, `04-qualidade-do-ajuste.qmd`); com este módulo pode importar — decisão do planejador do 13, mas a assinatura acima é a que ele encontra. O cap. 12 cita *"o `rescale` da seção 7.6"* em prosa — continua verdadeiro.

---

## 3. Plano seção a seção

Ordem do `_quarto.yml`: `index`, `01`, …, `08`. `import numpy as np` entra **uma vez por arquivo**, no primeiro chunk que o usa (a leitura do cap. 6, aceita pelo orquestrador). A saída de um escalar do numpy 2 aparece como `np.float64(...)`/`np.int64(...)`; a prosa não briga com isso (precedente do cap. 6).

### 3.0 `index.qmd`

**Chunks:** nenhum.

**Prosa:**

- Fim do primeiro parágrafo, depois de *"Este capítulo é sobre essa distância."* → acrescentar: *"É também o capítulo em que o `numpy` entra de vez: a [seção 7.1](01-explorando-seus-dados.qmd) abre apresentando o array que a [seção 6.1](../cap06/01-lendo-arquivos.qmd) mostrou de relance, e daqui até o fim do livro é ele que faz as contas de cada modelo."*
- Objetivo novo, **primeiro da lista**: *"Explicar o que um array do `numpy` é — forma, tipo único, fatias, máscaras booleanas, *broadcasting* e reduções por eixo — e o que cada uma dessas operações substitui no código em listas dos capítulos [4](../cap04/index.qmd) e [5](../cap05/index.qmd)"*
- Objetivo 5: *"Agrupar, ordenar e agregar dados tabulares com `defaultdict` e compreensões, no mesmo estilo do [Capítulo 2](../cap02/index.qmd)"* → *"Agrupar, ordenar e agregar dados tabulares com máscaras booleanas, `argsort` e reduções sobre arrays — e saber onde o registro heterogêneo ainda pede um `NamedTuple`"*
- Objetivo 7: *"...com PCA, construído do zero sobre o gradiente descendente do [Capítulo 5](../cap05/index.qmd)"* → *"...com PCA, construído do zero, em arrays, sobre o mesmo `gradient_step` do [Capítulo 5](../cap05/index.qmd)"*
- Tudo o mais fica: epígrafe, callout de correspondência, link do Colab, tabela, *Leituras adicionais* (o item do `scikit-learn` — *"resolvida por decomposição em valores singulares, não pelo gradiente descendente que você usou na seção 7.8"* — continua exato).

**Números:** nenhum. **Semente:** nenhuma.

### 3.1 `01-explorando-seus-dados.qmd`

**Estrutura nova:** título → callout de correspondência → parágrafo de abertura (com uma frase-ponte a mais) → **`## De listas a arrays`** (bloco novo, sete chunks) → `## Dados unidimensionais` → `## Duas dimensões` → `## Muitas dimensões` → callout `pandas`/`seaborn`. Os chunks `import-probability` e `import-statistics` (`include: false`) **saem**: `scratch_np.probability` não desenha no import, e correlação vira `np.corrcoef`.

**Parágrafo de abertura:** *"...Resista. O primeiro passo deveria ser sempre **explorar** os dados."* → acrescentar: *"Antes, porém, uma parada obrigatória: a partir daqui os dados são arrays do `numpy`, e este é o lugar de dizer o que isso significa."*

#### O bloco `## De listas a arrays` — texto completo, pronto para colar

Entra imediatamente depois do parágrafo de abertura e antes de `## Dados unidimensionais`. São 118 linhas de `.qmd` (dentro do alvo de 80–120 da spec); as saídas medidas estão na tabela logo abaixo.

````markdown
## De listas a arrays

O [Capítulo 4](../cap04/01-vetores.qmd) fechou com um callout que mostrava, de relance, o que o `numpy` faria no lugar do `dot`, do `subtract` e do `get_column` que você escreveu — e a [seção 6.1](../cap06/01-lendo-arquivos.qmd) usou um array pela primeira vez, lendo `dados/stocks.csv` com `np.loadtxt`. Este é o bloco em que o array deixa de ser relance: daqui até o fim do livro, todo vetor é um `np.ndarray`, e é ele a calculadora de cada modelo. Vale uma página para saber o que ele é.

Comece pelo `dot` do Capítulo 4, ao lado do que o `numpy` faz:

```{python}
#| label: arrays-dot
import numpy as np

def dot(v, w):                       # o `dot` do Capítulo 4, sem alterações
    return sum(v_i * w_i for v_i, w_i in zip(v, w))

v = [1.0, 2.0, 3.0]
w = [4.0, 5.0, 6.0]

v_arr = np.array(v)
w_arr = np.array(w)

dot(v, w), v_arr @ w_arr
```

Os dois números são o mesmo 32 — a soma dos produtos, que você enxerga porque escreveu o `sum` sobre o `zip`. O que muda é o que está por baixo. Uma lista de `float` é um vetor de ponteiros para objetos Python, percorrido um a um pelo interpretador; um **`ndarray`** é um bloco contíguo de memória com números de um único tipo, e o `@` percorre esse bloco em código compilado. É por isso que o array existe: a conta é a mesma, o custo não — e o fim deste bloco mede a diferença.

### Forma e tipo

Um array sabe duas coisas sobre si mesmo que uma lista não sabe. Tome as três pessoas da [seção 7.6](06-reescalonamento.qmd) — altura em polegadas, altura em centímetros, peso em libras —, uma por linha:

```{python}
#| label: arrays-forma-e-tipo
X = np.array([[63, 160.0, 150],
              [67, 170.2, 160],
              [70, 177.8, 171]])

X.shape, X.dtype
```

`shape` é a forma: 3 linhas por 3 colunas — a matriz do Capítulo 4, só que o array a conhece sem que ninguém conte as listas. `dtype` é o tipo **único** de todos os elementos: bastou um `160.0` para tudo virar `float64`, inclusive o `63`. É a mesma regra que fez o `np.loadtxt` da seção 6.1 recusar uma coluna de texto — num array, ou tudo é número, ou nada é; um `str`, uma data e um `float` lado a lado não cabem nele, e é para esse caso que a [próxima seção](02-namedtuples.qmd) existe. Neste livro, uma matriz de dados `X` tem sempre uma linha por observação e uma coluna por atributo: `X.shape` é `(n, d)`.

### Índices, fatias e colunas

A indexação começa como a de listas — `X[0]` é a primeira linha — e ganha um eixo a mais: `X[1, 2]` é a linha 1, coluna 2, e uma fatia pode correr **qualquer** eixo:

```{python}
#| label: arrays-indexacao
X[0], X[1, 2], X[:, 0], X[1:, :2]
```

`X[:, 0]` é a coluna inteira das polegadas — o `get_column` do [Capítulo 4](../cap04/02-matrizes.qmd), sem percorrer linha nenhuma: o `:` significa "todas as linhas", e o `0` escolhe a coluna. `X[1:, :2]` recorta um bloco: da linha 1 em diante, as duas primeiras colunas.

### Máscaras booleanas

Comparar um array com um número não devolve `True` ou `False`: devolve um array de booleanos, um por elemento. Esse array é uma **máscara**, e uma máscara serve de índice:

```{python}
#| label: arrays-mascaras
pesados = X[:, 2] > 155        # quem pesa mais de 155 libras?
pesados, X[pesados], X[pesados, 0], pesados.sum()
```

`X[pesados]` fica só com as linhas em que a máscara é `True`; `X[pesados, 0]` combina a máscara com uma coluna. E como `True` conta como 1, `pesados.sum()` é quantos passaram no filtro. É o `if` de uma compreensão de lista, aplicado a todos os elementos de uma vez — e é assim que a [seção 7.5](05-manipulando-dados.qmd) vai separar "as linhas da AAPL" das outras 13 mil.

### Broadcasting

Você já viu aritmética entre arrays na seção 6.1: `fechamento - abertura` subtraiu 23.105 pares de uma vez. O `numpy` estende a mesma regra a operandos de formas **diferentes**, e chama isso de *broadcasting*. O caso mais simples é um escalar contra um array — a coluna de polegadas vezes 2,54 devolve a de centímetros:

```{python}
#| label: arrays-broadcasting-escalar
X[:, 0] * 2.54, X[:, 1]
```

O caso que importa para o resto do livro é um vetor contra uma matriz. `X.mean(axis=0)` tem forma `(3,)` — uma média por coluna, explicada logo abaixo —, e subtraí-lo de `X`, que tem forma `(3, 3)`, subtrai o vetor **de cada linha**:

```{python}
#| label: arrays-broadcasting-vetor
X - X.mean(axis=0)
```

A regra: quando as formas diferem, o `numpy` alinha os eixos pela direita e estica o operando de tamanho 1 até casar com o outro. Um vetor `(3,)` contra uma matriz `(3, 3)` casa com as colunas, e cada linha recebe a subtração. É esta linha, `X - X.mean(axis=0)`, que a [seção 7.8](08-reducao-de-dimensionalidade.qmd) vai chamar de `de_mean`.

### `axis`: em que direção reduzir

`X.mean()` sem argumento reduz o array inteiro a um número. Com `axis`, a redução corre um eixo só — e é aqui que a convenção "linha = observação, coluna = atributo" paga:

```{python}
#| label: arrays-axis
X.mean(), X.mean(axis=0), X.mean(axis=1)
```

`axis=0` percorre as linhas e devolve um valor **por coluna** — a média de cada atributo, que é quase sempre o que se quer. `axis=1` percorre as colunas e devolve um valor por linha — a média das três medidas de uma pessoa, que aqui não significa nada, mas o `numpy` calcula do mesmo jeito. `sum`, `std`, `max` e `min` aceitam o mesmo `axis`, e `argmax` diz **onde** o máximo está:

```{python}
#| label: arrays-argmax
X.max(axis=0), X.argmax(axis=0)
```

A pessoa da linha 2 é a maior nas três colunas. `argmax` devolve o índice, não o valor — é ele que vai apontar "a maior variação de um dia" na seção 7.5 e, mais adiante, a classe mais provável de um classificador.

### O laço que some

Tudo acima cabia num `for`. O motivo de não escrever o `for` é o custo — e ele se mede. O mesmo `dot` do começo do bloco, sobre um milhão de elementos:

```{python}
#| label: arrays-o-laco-que-some
import time

v_grande = np.linspace(0, 1, 1_000_000)
w_grande = np.linspace(1, 2, 1_000_000)
v_lista, w_lista = v_grande.tolist(), w_grande.tolist()

inicio = time.perf_counter()
soma_laco = dot(v_lista, w_lista)
tempo_laco = time.perf_counter() - inicio

inicio = time.perf_counter()
soma_array = v_grande @ w_grande
tempo_array = time.perf_counter() - inicio

assert abs(soma_laco - soma_array) < 1e-6
f"laço: {tempo_laco * 1000:.0f} ms; array: {tempo_array * 1000:.2f} ms; razão: {tempo_laco / tempo_array:.0f}x"
```

O resultado é o mesmo número; a diferença de tempo é de duas ordens de grandeza, e cresce com o tamanho. Não é o `for` que é lento — é cada iteração passar pelo interpretador, que precisa descobrir de novo, a cada `v_i * w_i`, que tipo de objeto está multiplicando. O `@` decide isso uma vez, para o bloco inteiro.

É a promessa que o callout do Capítulo 4 fez e não cumpriu de propósito: *"depois desta seção, você olha `v @ w` e enxerga a soma dos produtos"*. Você escreveu a soma; agora usa o `@`. Daqui em diante, cada laço sobre os pontos de um conjunto de dados vira uma dessas operações — fatia, máscara, *broadcasting*, redução por eixo —, e o laço que sobrevive é só o que **é** o algoritmo: os passos de um gradiente, as iterações de um agrupamento. Com isso na mão, dá para explorar os dados.
````

Saídas esperadas (host; o implementador confere na execução real):

| Chunk | Saída |
|---|---|
| `arrays-dot` | `(32.0, np.float64(32.0))` |
| `arrays-forma-e-tipo` | `((3, 3), dtype('float64'))` |
| `arrays-indexacao` | `(array([ 63., 160., 150.]), np.float64(160.0), array([63., 67., 70.]), array([[ 67. , 170.2], [ 70. , 177.8]]))` |
| `arrays-mascaras` | `(array([False,  True,  True]), array([[ 67. , 170.2, 160. ], [ 70. , 177.8, 171. ]]), array([67., 70.]), np.int64(2))` |
| `arrays-broadcasting-escalar` | `(array([160.02, 170.18, 177.8 ]), array([160. , 170.2, 177.8]))` |
| `arrays-broadcasting-vetor` | matriz 3×3: `[[-3.667, -9.333, -10.333], [0.333, 0.867, -0.333], [3.333, 8.467, 10.667]]` |
| `arrays-axis` | `(np.float64(132.11111111111111), array([ 66.66666667, 169.33333333, 160.33333333]), array([124.33333333, 132.4, 139.6]))` |
| `arrays-argmax` | `(array([ 70. , 177.8, 171. ]), array([2, 2, 2]))` |
| `arrays-o-laco-que-some` | `'laço: 27 ms; array: 0.27 ms; razão: 103x'` — **varia por máquina**; em três medições no host, 102×–110×. A prosa diz só *"duas ordens de grandeza"*; se no container a razão sair abaixo de ~50×, o implementador troca para *"mais de uma ordem de grandeza"* e registra. |

Por que o `dot`, e não `X.mean(axis=0)`, no chunk de custo: medi os dois. A média por coluna de uma matriz `(1.000.000, 3)` dá só ~8× (a redução por `axis=0` numa matriz estreita percorre a memória com passo largo, e o `sum` do Python é rápido em somas simples); o `dot` dá ~100×. O chunk que promete "duas ordens de grandeza" precisa de um exemplo que as entregue — e o `dot` é o que abre o bloco.

#### `## Dados unidimensionais`

**Prosa:** *"Um primeiro passo óbvio é calcular estatísticas resumo: quantos pontos você tem, o menor, o maior, a média, o desvio padrão."* → acrescentar: *"— com um array, `len(x)`, `x.min()`, `x.max()`, `x.mean()` e `x.std(ddof=1)`, uma chamada cada."*

**Chunk `funcoes-histograma`** (novo por inteiro):

```{python}
#| label: funcoes-histograma
from typing import Tuple

def bucketize(points: np.ndarray, bucket_size: float) -> np.ndarray:
    """Arredonda cada ponto para baixo até o múltiplo mais próximo de bucket_size"""
    return bucket_size * np.floor(np.asarray(points, dtype=float) / bucket_size)

def make_histogram(points: np.ndarray, bucket_size: float) -> Tuple[np.ndarray, np.ndarray]:
    """Distribui os pontos nos buckets e conta quantos caem em cada um"""
    buckets, counts = np.unique(bucketize(points, bucket_size), return_counts=True)
    return buckets, counts

assert np.array_equal(bucketize([-3.2, 0.5, 7.9, 10.0], 5), [-5, 0, 5, 10])
```

**Prosa** entre a definição e o `.exemplo` (nova): *"`bucketize` recebe o array inteiro: `np.floor` arredonda cada elemento, e a divisão e a multiplicação por `bucket_size` valem para todos de uma vez — é o *broadcasting* com escalar do bloco acima. `np.unique(..., return_counts=True)` devolve os valores distintos, em ordem, e quantas vezes cada um aparece: dois arrays alinhados, um bucket e a contagem dele na mesma posição."*

**`.exemplo`** → *"Repare que `make_histogram` é a mesma ideia do histograma de notas do [Capítulo 3](../cap03/02-graficos-de-barras.qmd): contar quantos valores caem em cada piso de bucket. Lá a contagem era um `Counter` sobre `grade // 10 * 10`; aqui é `np.unique` com `return_counts=True` sobre um array já arredondado, e o arredondamento vira uma função nomeada, `bucketize` — o que compensa quando o tamanho do bucket muda de um gráfico para o outro, como acontece logo abaixo."*

**Chunk `funcao-plot-histograma`:**

```{python}
#| label: funcao-plot-histograma
from matplotlib import pyplot as plt

def plot_histogram(points: np.ndarray, bucket_size: float, title: str = ""):
    buckets, counts = make_histogram(points, bucket_size)
    plt.bar(buckets, counts, width=bucket_size)
    plt.xlabel("valor")
    plt.ylabel("frequência")
    plt.title(title)
```

**Prosa:** *"Para desenhar, basta um gráfico de barras — o mesmo `plt.bar` do Capítulo 3:"* — continua; *"Chama-se `inverse_normal_cdf` e vem de `scratch/probability.py`."* → *"Chama-se `inverse_normal_cdf` e vem de `scratch_np.probability` — a versão em arrays da função do Grus, que aceita um array de probabilidades e devolve um array de valores."*

**Chunk `import-probability`: sai.** **Chunk `distribuicoes-fake`** (novo por inteiro):

```{python}
#| label: distribuicoes-fake
from scratch_np.probability import inverse_normal_cdf

def random_normal(n: int, rng: np.random.Generator) -> np.ndarray:
    """n amostras da normal padrão"""
    return inverse_normal_cdf(rng.random(n))

rng = np.random.default_rng(0)

# uniforme entre -100 e 100
uniform = 200 * rng.random(10000) - 100

# normal com média 0, desvio padrão 57
normal = 57 * random_normal(10000, rng)

f"médias: {uniform.mean():.2f} e {normal.mean():.2f}"
```

**Prosa nova, logo depois do chunk — é a primeira `default_rng` do livro reescrito, e o texto que os capítulos seguintes vão reaproveitar:** *"`np.random.default_rng(0)` cria um **gerador** de números aleatórios com a semente 0, e `rng.random(10000)` sorteia dez mil uniformes em `[0, 1)` de uma vez. Todo sorteio deste livro sai de um gerador assim, criado no chunk e passado explicitamente para quem sorteia — `random_normal` recebe o `rng` como parâmetro em vez de puxar de um estado global —, para que a semente fique visível onde o sorteio acontece. Sem ela, cada execução desenharia dois histogramas diferentes, e os números do parágrafo seguinte não seriam os seus."*

Saída: `'médias: -0.12 e 0.94'`. *"Ambos têm média perto de 0."* — continua (0,94 contra um desvio de 57). *"Os dois também têm desvio padrão próximo de 57."* — continua (57,81 e 56,66, medidos com `ddof=1`).

**Chunks `fig-histograma-uniforme` e `fig-histograma-normal`: mantêm** (só chamam `plot_histogram`). As figuras mudam de conteúdo (sorteio novo), não de forma.

#### `## Duas dimensões`

**Chunk `xs-ys1-ys2`:**

```{python}
#| label: xs-ys1-ys2
rng = np.random.default_rng(4)
xs = random_normal(1000, rng)
ys1 =  xs + random_normal(1000, rng) / 2
ys2 = -xs + random_normal(1000, rng) / 2
```

**Chunk `fig-dispersao-conjunta`: mantém** (`plt.scatter(xs, ys1, ...)` aceita arrays). **Chunk `import-statistics`: sai.**

**Chunk `correlacoes`:**

```{python}
#| label: correlacoes
np.corrcoef(xs, ys1)[0, 1], np.corrcoef(xs, ys2)[0, 1]
```

**Prosa** antes dele: *"`ys1` cresce com `xs`; `ys2` decresce. A diferença fica evidente também na correlação:"* → *"`ys1` cresce com `xs`; `ys2` decresce. A diferença fica evidente também na correlação — `np.corrcoef` devolve a matriz 2×2 de correlações entre os dois arrays, com 1 na diagonal, e a entrada `[0, 1]` é a que interessa:"*

Saída: `(np.float64(0.8931779883670059), np.float64(-0.8974145439902339))`.

**Callout *Uma afirmação sobre dado aleatório precisa de semente*** — texto novo por inteiro (os números mudam):

````markdown
::: {.callout-warning}
## Uma afirmação sobre dado aleatório precisa de semente

Suponha que você quisesse registrar esse resultado como um teste, do jeito que este livro faz o tempo todo:

```python
assert 0.89 < np.corrcoef(xs, ys1)[0, 1] < 0.91
assert -0.91 < np.corrcoef(xs, ys2)[0, 1] < -0.89
```

Se `xs`, `ys1` e `ys2` fossem gerados **sem** fixar semente, esse teste seria cara ou coroa. A correlação dessa construção gira em torno de 0,894 — perto o bastante da borda inferior da janela `(0.89, 0.91)` para que o `assert` falhe cerca de uma vez em cada quatro execuções (medido: 24,6% e 25,0% em duas séries independentes de 20 mil repetições do código do Grus, e 24,3% em mais 20 mil com o gerador do `numpy` — a distribuição é a mesma). Não seria um defeito no cálculo da correlação: é uma afirmação sobre uma amostra aleatória, escrita como se fosse determinística.

São duas lições, e valem para qualquer código seu: uma afirmação sobre dado aleatório só é reprodutível se a semente estiver fixada, e uma janela de tolerância só é segura se for larga o bastante para a variação real da amostra, não só para o valor obtido numa execução específica. Por isso `rng = np.random.default_rng(4)` aparece aqui em cima, antes de `xs`: com a semente fixada, a correlação sai sempre a mesma — 0,893 e -0,897, a três milésimos da borda, mas sempre os mesmos três milésimos — em qualquer execução.
:::
````

#### `## Muitas dimensões`

**Chunk `matriz-de-correlacao`:**

```{python}
#| label: matriz-de-correlacao
def correlation_matrix(X: np.ndarray) -> np.ndarray:
    """
    Devolve a matriz d x d cuja entrada (i, j) é a correlação
    entre a coluna i e a coluna j de X
    """
    return np.corrcoef(X, rowvar=False)
```

**Prosa** depois do chunk (nova): *"A função tem uma linha, e ela existe por causa de um detalhe que morde: `np.corrcoef` assume, por padrão, uma **variável por linha** — é o `rowvar=True` implícito. A convenção deste livro é o contrário, uma variável por coluna, e é o `rowvar=False` que diz isso. Esquecê-lo não dá erro: dá uma matriz $n \times n$ de correlações entre observações, um número que não significa nada, calculado sem aviso."*

**Prosa** antes de `fig-dispersao-matriz`: *"Para gerar um exemplo com correlações interessantes, construímos quatro séries relacionadas entre si de propósito:"* → *"Para gerar um exemplo com correlações interessantes, construímos quatro séries relacionadas entre si de propósito — cada uma um array de 100 valores, empilhadas como colunas de uma matriz `(100, 4)`:"*

**Chunk `fig-dispersao-matriz`:**

```{python}
#| label: fig-dispersao-matriz
#| fig-cap: "Matriz de dispersão de quatro séries correlacionadas"
rng = np.random.default_rng(0)
num_points = 100

col0 = random_normal(num_points, rng)
col1 = -5 * col0 + random_normal(num_points, rng)
col2 = col0 + col1 + 5 * random_normal(num_points, rng)
col3 = np.where(col2 > -2, 6, 0)

# uma série por coluna: (100, 4)
corr_data = np.column_stack([col0, col1, col2, col3])

num_vectors = corr_data.shape[1]
fig, ax = plt.subplots(num_vectors, num_vectors, figsize=(8, 8))

for i in range(num_vectors):
    for j in range(num_vectors):
        if i != j:
            ax[i][j].scatter(corr_data[:, j], corr_data[:, i], s=8)
        else:
            ax[i][j].annotate("série " + str(i), (0.5, 0.5),
                              xycoords='axes fraction',
                              ha="center", va="center")
        if i < num_vectors - 1: ax[i][j].xaxis.set_visible(False)
        if j > 0: ax[i][j].yaxis.set_visible(False)

ax[-1][-1].set_xlim(ax[0][-1].get_xlim())
ax[0][0].set_ylim(ax[0][1].get_ylim())
plt.show()
```

`np.where(condição, a, b)` é novo: uma frase de prosa antes do chunk — *"`np.where(cond, 6, 0)` escolhe, elemento a elemento, 6 onde a máscara é verdadeira e 0 onde não é: o `6 if ... else 0` do Grus, para a coluna inteira."* A grade 4×4 de laços **fica** — é um laço sobre painéis, não sobre pontos.

**Chunk `matriz-de-correlacao-valores`:**

```{python}
#| label: matriz-de-correlacao-valores
np.round(correlation_matrix(corr_data), 2)
```

Saída (semente 0):

```
array([[ 1.  , -0.98, -0.6 , -0.44],
       [-0.98,  1.  ,  0.62,  0.47],
       [-0.6 ,  0.62,  1.  ,  0.82],
       [-0.44,  0.47,  0.82,  1.  ]])
```

A prosa qualitativa continua valendo, verificada: série 1 × série 0 = -0,98 (*"fortemente anticorrelacionada"*), série 2 × série 1 = 0,62 (*"positivamente correlacionada"*), série 3 ∈ {0, 6} com 40/60 (*"só assume os valores 0 e 6"*). *"(a própria fórmula de `row[1]` inverte o sinal de `row[0]`)"* → *"(a própria fórmula de `col1` inverte o sinal de `col0`)"*.

**Callout `pandas`/`seaborn`:** *"`.corr()` por padrão calcula a correlação de Pearson — a mesma que você implementou —"* → *"`.corr()` por padrão calcula a correlação de Pearson — a mesma do `np.corrcoef`, e com a convenção certa, uma variável por coluna —"*. O resto fica (a *"dupla de laços"* continua existindo).

**Números a recalcular (7.1):** `'médias: -0.12 e 0.94'`; correlações 0,893/-0,897 (prosa do callout); a matriz acima. Todos por **sorteio novo** (`default_rng` ≠ `random.seed`). Nenhum número do bloco *De listas a arrays* depende de sorteio; só o tempo do último chunk varia.

**Semente:** `default_rng(0)` em `distribuicoes-fake` (justificada no parágrafo novo), `default_rng(4)` em `xs-ys1-ys2` (justificada no callout), `default_rng(0)` em `fig-dispersao-matriz` (coberta pelo callout, que já é a justificativa da seção). Mantive os inteiros do Grus (0, 4, 0) para a correspondência ser visível, sabendo que os sorteios não são os mesmos.

### 3.2 `02-namedtuples.qmd`

**Chunks:** todos os cinco "mantêm". Motivo: a seção é sobre o registro heterogêneo (`str`, `datetime.date`, `float`), que um array não representa — trocar por `np.array` com `dtype=object` apagaria a lição da anotação de tipo e não daria nada em troca.

**Prosa:**

- Abertura, *"Uma forma comum de representar um registro de dados é um `dict`:"* → *"O bloco *De listas a arrays* da [seção 7.1](01-explorando-seus-dados.qmd) deixou claro o que um array não faz: guardar um `str`, uma data e um `float` lado a lado — o `dtype` é um só. Um registro heterogêneo pede outra estrutura, e a forma mais comum de representá-lo é um `dict`:"*
- `.exemplo` inteiro → 

````markdown
::: {.exemplo}
Você vai ver este exato padrão de novo no [Capítulo 10](../cap10/index.qmd), sobre Naive Bayes:

```python
class Message(NamedTuple):
    text: str
    is_spam: bool
```

`Message` empacota um texto e o rótulo dele exatamente como `StockPrice` empacota um símbolo, uma data e um preço — um registro pequeno, imutável, com nomes de campo que o editor entende. É o uso mais comum de `NamedTuple` neste livro: dar nome e tipo ao que fica **fora** dos arrays — o texto, o rótulo, a data —, enquanto o que é número vai para dentro deles.
:::
````

Motivo da troca (cap. 9 → cap. 10): `Message` está no contrato de `scratch_np/naive_bayes.py` na spec, e já existe hoje com esses campos em `content/cap10/03-implementacao.qmd`; `LabeledPoint` com `point: Vector` pode não sobreviver à reescrita do cap. 9, e `Vector` certamente não.

**Números:** nenhum. **Semente:** nenhuma.

### 3.3 `03-dataclasses.qmd`

**Chunks:** os três "mantêm". **Prosa:** nada muda — nenhuma frase fala de listas, Python puro ou de código que deixou de existir. **Não entra no commit.**

### 3.4 `04-limpeza-e-transformacao.qmd`

**Chunks:** `parse-row`, `try-parse-row`, `mostra-csv-sujo`, `le-csv-sujo` "mantêm". Motivo: é a fronteira do parsing — texto virando `StockPrice` —, e a lição é a conversão explícita com `None` como sinal. É o código que o módulo carrega e que a 7.5 importa.

**Callout novo**, entre o `.callout-warning` *O erro que passa pelo filtro* e o parágrafo *"E então é preciso decidir o que fazer com as linhas inválidas"* — a pendência que o plano do cap. 6 deixou para cá:

````markdown
::: {.callout-note}
## O mesmo arquivo pelo `np.genfromtxt`

A [seção 6.1](../cap06/01-lendo-arquivos.qmd) mostrou o leitor tolerante do `numpy`. Sobre este arquivo, ele lê a coluna de preços assim:

```{python}
#| label: genfromtxt-csv-sujo
import numpy as np

np.genfromtxt("dados/comma_delimited_stock_prices.csv", delimiter=",", usecols=2)
```

O `n/a` virou `nan`, sem aviso — a linha que `try_parse_row` rejeitou em voz alta sobreviveu em silêncio. E o ano 3014 nem foi lido: `usecols=2` descartou a coluna da data antes de qualquer conferência. É o contraste inteiro desta seção: o array carrega os números; a decisão sobre o que é dado ruim continua sendo uma função sua.
:::
````

Saída: `array([90.91, 41.68, 64.5 , 91.86,   nan, 64.34])` (verificado). O chunk mora dentro de um callout — `gerar-notebooks.py` converte o corpo recursivamente e ele vira célula de código (é o 20º chunk-em-callout do livro).

**Prosa fora do callout:** nada muda. O callout `pandas` de fechamento continua exato — e ganha uma referência: *"É o mesmo tipo de problema que a linha da FB com ano 3014 ilustrou acima"* → *"É o mesmo tipo de problema que a linha da FB com ano 3014 ilustrou acima — e o mesmo `nan` silencioso do `np.genfromtxt` no callout anterior"*.

**Números:** nenhum muda. **Semente:** nenhuma.

### 3.5 `05-manipulando-dados.qmd`

É a seção que mais muda. O fio: **o registro fica heterogêneo até `try_parse_row`; depois, três arrays paralelos.** O bloco ```` ```python ```` de abertura (`data = [StockPrice(...), # ...]`) fica.

**Chunk `carrega-stocks-completo`:**

```{python}
#| label: carrega-stocks-completo
import csv
import numpy as np
from scratch_np.working_with_data import StockPrice, try_parse_row

with open("dados/stocks.csv", "r") as f:
    reader = csv.DictReader(f)
    rows = [[row['Symbol'], row['Date'], row['Close']]
            for row in reader]

# garante que todas carregaram com sucesso
maybe_data = [try_parse_row(row) for row in rows]
assert maybe_data
assert all(sp is not None for sp in maybe_data)

data = [sp for sp in maybe_data if sp is not None]
len(data)
```

Saída: `23105`. **Callout `.callout-note`** logo abaixo ganha uma frase no fim: *"...se uma linha só tivesse voltado `None`, ele estouraria. `StockPrice` e `try_parse_row` não são redefinidos aqui: vêm de `scratch_np.working_with_data`, o mesmo código que você escreveu na [seção 7.4](04-limpeza-e-transformacao.qmd)."*

**Chunk novo `desempacota-em-arrays`**, com prosa antes — *"`data` é uma lista de registros, e cada registro mistura texto, data e número: o formato certo para a fronteira do parsing, e o formato errado para fazer conta. O passo seguinte é desempacotar a lista em três arrays paralelos — um por campo —, cada um com o `dtype` que o campo pede:"*

```{python}
#| label: desempacota-em-arrays
symbols = np.array([sp.symbol for sp in data])                       # texto
dates   = np.array([sp.date for sp in data], dtype="datetime64[D]")  # datas, com resolução de dia
closes  = np.array([sp.closing_price for sp in data], dtype=float)   # números

symbols[:3], dates[:3], closes[:3], np.unique(symbols)
```

Saída: `(array(['AAPL', 'AAPL', 'AAPL'], dtype='<U4'), array(['1980-12-12', '1980-12-15', '1980-12-16'], dtype='datetime64[D]'), array([0.513393, 0.486607, 0.450893]), array(['AAPL', 'FB', 'GOOG', 'MSFT'], dtype='<U4'))`.

Prosa depois: *"Três `dtype`s diferentes, um por array: `<U4` é texto de até quatro caracteres, `datetime64[D]` é uma data que o `numpy` sabe ordenar e comparar, `float64` é o preço. A posição `i` dos três arrays é a mesma linha do arquivo — é isso que "paralelos" quer dizer, e é o que permite usar uma máscara feita num deles para fatiar outro. `np.unique` diz que são quatro ações (AAPL, FB, GOOG, MSFT), as mesmas do resto deste livro."* (A frase *"as 23.105 linhas de preços diários de quatro ações (AAPL, FB, GOOG, MSFT) já usadas no restante deste livro"* do parágrafo de abertura continua.)

**Passos do maior preço da AAPL** — a lista *"1. Restringir às linhas da AAPL. 2. Pegar o `closing_price` de cada linha. 3. Tirar o `max` desses preços."* continua; *"Uma compreensão faz as três coisas de uma vez:"* → *"Uma máscara faz as três coisas de uma vez — `symbols == "AAPL"` é o filtro, aplicado a `closes` é a coluna, e `.max()` é a resposta:"*

```{python}
#| label: max-aapl
max_aapl_price = closes[symbols == "AAPL"].max()
max_aapl_price
```

Saída: `np.float64(232.070007)`.

**Maior preço por símbolo** — os dois passos → *"1. Listar os símbolos distintos, com `np.unique`. 2. Para cada um, a mesma máscara e o mesmo `.max()` — um laço sobre quatro ações, não sobre 23.105 linhas."*

```{python}
#| label: max-precos-por-simbolo
for symbol in np.unique(symbols):
    print(f"{symbol}: {closes[symbols == symbol].max():.2f}")
```

Saída: `AAPL: 232.07`, `FB: 217.50`, `GOOG: 1268.33`, `MSFT: 115.61`. (O `defaultdict(lambda: float('-inf'))` sai: o "menos infinito para símbolos ainda não vistos" existia porque o laço percorria as linhas; com a máscara, cada símbolo é resolvido de uma vez.)

**Variação percentual** — os passos → *"Uma abordagem é agrupar os preços por símbolo e, dentro de cada grupo: 1. Ordenar os preços por data. 2. Alinhar cada dia com o anterior — duas fatias do mesmo array, deslocadas por uma posição. 3. Dividir uma pela outra."*

**Chunks `agrupa-por-simbolo` + `ordena-por-data` → um só, `agrupa-e-ordena`:**

```{python}
#| label: agrupa-e-ordena
prices = {}
for symbol in np.unique(symbols).tolist():
    mask = symbols == symbol                  # as linhas desta ação
    order = np.argsort(dates[mask])           # os índices que as põem em ordem de data
    prices[symbol] = (dates[mask][order], closes[mask][order])

{symbol: len(symbol_closes) for symbol, (_, symbol_closes) in prices.items()}
```

Saída: `{'AAPL': 9584, 'FB': 1655, 'GOOG': 3607, 'MSFT': 8259}`. O parágrafo *"Como os preços são tuplas, eles são ordenados pelos campos..."* **sai** e entra: *"`np.argsort` não ordena: devolve os **índices** que ordenariam — `dates[mask][order]` é o array de datas em ordem, e `closes[mask][order]`, indexado pelos mesmos índices, é o de preços na mesma ordem. É o que garante que a data da posição `i` e o preço da posição `i` continuam sendo o mesmo dia. O agrupamento em si é um `dict` comum, com quatro chaves: o dado heterogêneo (que ação) fica na chave; o numérico (datas e preços) fica em arrays. O `.tolist()` só transforma os símbolos de `np.str_` em `str` comum, para as chaves do `dict` saírem limpas."*

**Chunk `variacoes-dia-a-dia`:**

```{python}
#| label: variacoes-dia-a-dia
def day_over_day_changes(closes: np.ndarray) -> np.ndarray:
    """Variações percentuais dia a dia. Assume preços de uma única ação, em ordem."""
    return closes[1:] / closes[:-1] - 1
```

Prosa (substitui o parágrafo do `zip`): *"`closes[1:]` é "hoje" — do segundo dia em diante — e `closes[:-1]` é "ontem" — até o penúltimo. As duas fatias têm o mesmo tamanho e estão desalinhadas por uma posição, que é exatamente o truque de percorrer duas sequências em paralelo que o [Capítulo 2](../cap02/06-ferramentas-e-tipos.qmd) fazia com `zip(prices, prices[1:])`; a divisão elemento a elemento faz o resto. `pct_change` e `DailyChange` do Grus não aparecem: a fórmula `hoje / ontem - 1` cabe na própria linha, e o resultado é um array, não uma lista de registros."*

**Chunk `todas-as-variacoes`:**

```{python}
#| label: todas-as-variacoes
all_symbols = np.concatenate([np.full(len(c) - 1, symbol) for symbol, (_, c) in prices.items()])
all_dates   = np.concatenate([d[1:] for d, _ in prices.values()])
all_pct     = np.concatenate([day_over_day_changes(c) for _, c in prices.values()])

len(all_pct)
```

Saída: `23101`. Prosa: *"e depois juntar todas:"* → *"e depois juntar as quatro ações em três arrays paralelos de novo — símbolo, data e variação de cada dia. `np.full(k, valor)` faz um array com `k` cópias do valor; `d[1:]` descarta a primeira data de cada ação, que não tem "ontem"; e `np.concatenate` emenda as quatro partes:"*. Há 23.101 variações, quatro a menos que linhas — uma por ação.

**Chunk `maior-menor-variacao`:**

```{python}
#| label: maior-menor-variacao
i_max, i_min = all_pct.argmax(), all_pct.argmin()

print(f"maior alta:  {all_symbols[i_max]} em {all_dates[i_max]}: {all_pct[i_max]:+.2%}")
print(f"maior queda: {all_symbols[i_min]} em {all_dates[i_min]}: {all_pct[i_min]:+.2%}")
```

Saída: `maior alta:  AAPL em 1997-08-06: +33.23%` / `maior queda: AAPL em 2000-09-29: -51.87%`. Prosa: *"A partir daqui é fácil achar a maior e a menor:"* → *"A partir daqui é o `argmax` do bloco *De listas a arrays*: ele devolve a posição da maior variação, e a mesma posição nos outros dois arrays diz de que ação e de que dia ela é:"*. O parágrafo *"O maior salto de um dia para o outro foi a AAPL, em 6 de agosto de 1997 — mais de 33%... quase 52%..."* **continua exato**.

**Chunk `variacao-media-por-mes`:**

```{python}
#| label: variacao-media-por-mes
# datetime64[M] conta meses desde janeiro de 1970; o resto por 12 dá o mês do ano (0 = janeiro)
months = all_dates.astype("datetime64[M]").astype(int) % 12 + 1

avg_daily_change = {month: all_pct[months == month].mean()
                    for month in range(1, 13)}

{month: round(float(change), 5) for month, change in avg_daily_change.items()}
```

Saída: `{1: 0.00219, 2: 0.00014, 3: 0.001, 4: 0.00172, 5: 0.00105, 6: -0.00039, 7: 0.00087, 8: 0.00123, 9: -0.0, 10: 0.00291, 11: 0.00081, 12: 0.00092}` (idêntica à atual — verificado contra `.month` de cada data). Prosa: *"Basta olhar a variação diária média por mês:"* → *"Basta olhar a variação diária média por mês. Extrair o mês de um `datetime64` é uma conversão de unidade — para `datetime64[M]`, que conta meses desde 1970 — seguida de um resto por 12; com o array de meses na mão, cada mês é uma máscara e uma média:"*

O `.callout-note` sobre a anotação `Dict[int, List[DailyChange]]` **sai** (motivo: a âncora — a anotação errada do Grus em `changes_by_month` — não existe mais; a lição "anotações não são verificadas em tempo de execução" já foi dada no cap. 2 e é citada de novo na 7.3).

**Chunk `melhor-mes`:**

```{python}
#| label: melhor-mes
melhor_mes = max(avg_daily_change, key=avg_daily_change.get)
assert melhor_mes == 10   # outubro é o melhor mês

melhor_mes, f"{avg_daily_change[melhor_mes]:.2%}"
```

Saída: `(10, '0.29%')`. O parágrafo *"Outubro é o melhor mês para investir, com uma variação diária média de cerca de 0,29%..."* **continua exato**.

**Callout `pandas`:** *"`df.groupby('Symbol')['Close'].max()` no lugar do `defaultdict` que acha o maior preço por símbolo"* → *"...no lugar da máscara `symbols == symbol` seguida de `.max()`"*; *"`df.resample('ME')` para agregações por mês, no lugar do `dict` `changes_by_month` construído à mão"* → *"...no lugar da máscara por mês construída à mão"*; *"exatamente a etapa `sorted(symbol_prices)` que esta seção gastou um parágrafo justificando"* → *"exatamente o `np.argsort(dates[mask])` que esta seção gastou um parágrafo justificando"*.

**Números a recalcular:** nenhum muda de valor — todos reproduzidos em arrays (tabela acima). O que muda é a **forma** das saídas (arrays e `print`s em vez de `NamedTuple`s), e o implementador confere cada uma. **Semente:** nenhuma; a seção é determinística.

**O que fica como laço e por quê:** o `for` sobre `np.unique(symbols)` (quatro grupos) em dois chunks; a compreensão de listas do `csv.DictReader` e do `try_parse_row` (parsing linha a linha é a lição da 7.4); o `dict` `prices` de quatro chaves. O que **some**: o laço sobre 23.105 linhas do `max_prices`, o `defaultdict(list)` do agrupamento, o `sorted` de tuplas, o `zip` deslocado, a compreensão dupla de `all_changes`, o laço de `changes_by_month`.

### 3.6 `06-reescalonamento.qmd`

**Prosa:** *"Já temos uma função de distância euclidiana — do [Capítulo 4](../cap04/index.qmd) —, então uma abordagem natural..."* → *"A distância euclidiana do [Capítulo 4](../cap04/01-vetores.qmd) é, em arrays, `np.linalg.norm(a - b)` — o callout de fechamento daquela seção já a mostrava assim —, então uma abordagem natural..."*

**Chunk `distancia-polegadas`:**

```{python}
#| label: distancia-polegadas
import numpy as np

A = np.array([63, 150.])    # altura em polegadas, peso em libras
B = np.array([67, 160.])
C = np.array([70, 171.])

np.array([np.linalg.norm(A - B), np.linalg.norm(A - C), np.linalg.norm(B - C)]).round(2)
```

Saída: `array([10.77, 22.14, 11.4 ])`. **Chunk `distancia-centimetros`:** idem com `[160, 150.]`, `[170.2, 160.]`, `[177.8, 171.]` → `array([14.28, 27.53, 13.37])`. A prosa (*"o vizinho mais próximo de B é A"*, *"passa a ser C"*) continua verdadeira: A–B 10,77 < B–C 11,40; B–C 13,37 < A–B 14,28. O `.callout-warning` fica.

**Chunk `import-statistics`: sai.** **Prosa:** *"Para começar, precisamos calcular a média e o desvio padrão de cada posição:"* → *"Para começar, precisamos calcular a média e o desvio padrão de cada **coluna** — a redução por `axis=0` do bloco *De listas a arrays*:"*

**Chunk `funcao-scale`:**

```{python}
#| label: funcao-scale
from typing import Tuple

def scale(X: np.ndarray) -> Tuple[np.ndarray, np.ndarray]:
    """devolve a média e o desvio padrão de cada coluna"""
    X = np.asarray(X, dtype=float)
    means = X.mean(axis=0)
    stdevs = X.std(axis=0, ddof=1)
    return means, stdevs

vectors = np.array([[-3, -1, 1], [-1, 0, 1], [1, 1, 1]], dtype=float)
means, stdevs = scale(vectors)

assert np.array_equal(means, [-1, 0, 1])
assert np.array_equal(stdevs, [2, 1, 0])
means, stdevs
```

Saída: `(array([-1.,  0.,  1.]), array([2., 1., 0.]))`. **Prosa nova** depois do chunk: *"Repare no `ddof=1`. O `numpy` divide a variância por $n$ por padrão; o desvio padrão amostral, que divide por $n - 1$, é o `ddof=1` (*delta degrees of freedom*) — a mesma fórmula que o Grus usa e que você provavelmente aprendeu. Sem o argumento, `stdevs` sairia `[1.63, 0.82, 0]` e o `assert` cairia: é uma diferença de escala que não avisa. Repare também na terceira coluna: todo vetor tem 1 ali, então o desvio padrão é 0 — não há variação nenhuma para reescalonar."* (substitui *"Repare na terceira posição: todo vetor tem 1 ali..."*).

**Chunk `funcao-rescale`:**

```{python}
#| label: funcao-rescale
def rescale(X: np.ndarray) -> np.ndarray:
    """
    Reescalona os dados de entrada para que cada coluna tenha
    média 0 e desvio padrão 1. (Deixa uma coluna como está se
    o desvio padrão dela é 0.)
    """
    X = np.asarray(X, dtype=float)
    means, stdevs = scale(X)

    rescaled = X.copy()                 # não mexe no original
    varia = stdevs > 0                  # máscara sobre as colunas
    rescaled[:, varia] = (X[:, varia] - means[varia]) / stdevs[varia]
    return rescaled

rescale(vectors)
```

Saída: `array([[-1., -1.,  1.], [ 0.,  0.,  1.], [ 1.,  1.,  1.]])`. **Prosa nova** depois: *"O laço duplo do Grus — para cada vetor, para cada posição, `if stdevs[i] > 0` — virou uma máscara sobre as **colunas**: `varia` marca as que têm desvio positivo, `X[:, varia]` seleciona só essas, e a subtração e a divisão são o *broadcasting* de um vetor `(d,)` contra uma matriz `(n, d)`. As colunas fora da máscara ficam como estavam na cópia. `X.copy()` importa: sem ele, `rescaled` seria o próprio `X`, e a função alteraria o argumento de quem a chamou."*

**Chunk `testa-rescale`:**

```{python}
#| label: testa-rescale
means, stdevs = scale(rescale(vectors))
assert np.array_equal(means, [0, 0, 1])
assert np.array_equal(stdevs, [1, 1, 0])
```

*"A terceira posição continua com desvio padrão 0"* → *"A terceira coluna continua com desvio padrão 0"*.

**Callout `scikit-learn`** — parágrafo do meio, texto novo: *"`fit` calcula média e desvio padrão de cada coluna (o que você fez em `scale`, com `axis=0`); `transform` aplica a fórmula `(x - média) / desvio` (o que você fez em `rescale`, por *broadcasting*); `fit_transform` faz as duas coisas em sequência. Uma diferença que não está na interface: o `StandardScaler` divide por $n$, não por $n - 1$ — é o `ddof=0` do `numpy`, não o `ddof=1` do seu `scale` —, então os números não batem no último dígito, embora com centenas de linhas a diferença seja desprezível. A separação entre `fit` e `transform` importa na prática: ..."* (o resto do parágrafo e o do `MinMaxScaler` ficam).

**Números:** nenhum muda. **Semente:** nenhuma.

### 3.7 `07-um-parenteses-tqdm.qmd`

**Chunk `tqdm-basico`:**

```{python}
#| label: tqdm-basico
#| warning: false
import tqdm
import numpy as np

rng = np.random.default_rng(0)

for i in tqdm.tqdm(range(100)):
    # faz algo lento: sorteia um milhão de números e os ordena
    _ = np.sort(rng.random(1_000_000))
```

Motivo do `np.sort`: `rng.random(1_000_000)` sozinho leva 4,5 ms — o laço inteiro acabaria em meio segundo e a barra não teria o que mostrar; com o `np.sort` (40 ms por iteração, medido), o laço leva ~4 s e a barra ganha sentido. A linha-amostra da prosa vira, medida no host, algo como:

```
 56%|███████████████                     | 56/100 [00:02<00:01, 24.83it/s]
```

— **o implementador cola a linha real da execução** (o `it/s` varia; o formato não). Uma frase depois do chunk: *"O `rng = np.random.default_rng(0)` não muda nada do que a barra mostra — nada aqui depende dos valores sorteados —, mas todo sorteio deste livro tem semente, sem exceção para exemplos de brinquedo."*

**Chunk `primes-up-to`: mantém** — é um laço sobre inteiros que existe para demonstrar `tqdm.trange` e `set_description`, e o `.exemplo` já diz que ele é lento de propósito. Não há ponto de dado ali. A linha-amostra *"5088 primos: 50%|..."* fica.

**Prosa:** *"a começar pela próxima seção, onde `tqdm.trange` acompanha os passos do gradiente descendente ajustando a primeira componente principal"* — continua verdadeira.

**Números:** só a linha-amostra. **Semente:** `default_rng(0)`, justificada na frase acima.

### 3.8 `08-reducao-de-dimensionalidade.qmd`

**Chunk `pca-data`** (`echo: false`): a lista literal fica onde está, envolvida por `np.array(...)`:

```{python}
#| label: pca-data
#| echo: false
import numpy as np

pca_data = np.array([
[20.9666776351559,-13.1138080189357],
...   # as 99 linhas atuais, intocadas
[25.2049825789225,-14.1592086208169]
])
pca_data.shape
```

Saída: `(99, 2)`. *"Considere o conjunto de dados a seguir:"* — continua.

**Chunk `fig-pca-brutos`:** `xs = [p[0] for p in pca_data]` / `ys = ...` → `plt.scatter(pca_data[:, 0], pca_data[:, 1], s=15, color="darkblue")`; o resto igual.

**Callout `.callout-note` do `gradient_step`** → *"Esta seção depende do gradiente descendente do [Capítulo 5](../cap05/index.qmd) — especificamente de `gradient_step`, a função construída lá que treina praticamente todo modelo deste livro. Aqui ela vem de `scratch_np.gradient_descent`, em arrays: `v + step_size * gradient`, uma linha no lugar do `add(v, scalar_multiply(step_size, gradient))` do Capítulo 5, com a mesma convenção de sinal. Ela não ajusta parâmetro de modelo nenhum; ajusta a **direção** que melhor explica a variação dos dados. Se `gradient_step` não estiver familiar, vale revisitar aquele capítulo antes de continuar."*

**Chunk `de-mean`:**

```{python}
#| label: de-mean
import numpy as np

def de_mean(X: np.ndarray) -> np.ndarray:
    """Recentra os dados para que cada coluna tenha média 0"""
    return X - X.mean(axis=0)

de_meaned = de_mean(pca_data)
assert np.allclose(de_meaned.mean(axis=0), 0)
```

Prosa nova depois: *"É a linha do *broadcasting* da [seção 7.1](01-explorando-seus-dados.qmd): a média de cada coluna, forma `(2,)`, subtraída de cada uma das 99 linhas. O `assert` confere com `np.allclose`, não com `== 0` — a média de floats recentrados fica na casa de 10⁻¹⁵, não em zero exato."*

**Chunk `fig-pca-demeaned`:** `xs2`/`ys2` → `plt.scatter(de_meaned[:, 0], de_meaned[:, 1], ...)`.

**Prosa da seção "A direção de maior variância":** *"o `dot(v, w)` que a seção "Projetando e removendo", adiante, vai calcular"* → *"o `v @ w` que a seção "Projetando e removendo", adiante, vai calcular"*; *"a projeção de cada linha $x$ da matriz sobre $d$ tem comprimento `dot(x, d)`"* → *"a projeção de cada linha $x$ da matriz sobre $d$ tem comprimento `x @ d` — e `X @ d` calcula os $n$ comprimentos de uma vez, um por linha"*.

**Chunk `direction`:**

```{python}
#| label: direction
def direction(w: np.ndarray) -> np.ndarray:
    return w / np.linalg.norm(w)
```

**Chunk `directional-variance`:**

```{python}
#| label: directional-variance
def directional_variance(X: np.ndarray, w: np.ndarray) -> float:
    """Devolve a variância de X na direção de w"""
    w_dir = direction(w)
    return np.sum((X @ w_dir) ** 2)
```

Prosa nova: *"`X @ w_dir` é o vetor de comprimentos de projeção, um por ponto; elevar ao quadrado e somar é a soma dos quadrados de todos eles."* O `.callout-note` sobre "não divide por $n$" fica.

**Chunk `directional-variance-gradient`:**

```{python}
#| label: directional-variance-gradient
def directional_variance_gradient(X: np.ndarray, w: np.ndarray) -> np.ndarray:
    """O gradiente da variância direcional em relação a w"""
    w_dir = direction(w)
    return 2 * (X @ w_dir) @ X
```

Callout → *"...e a derivada de um quadrado como esse em relação a cada coordenada de $w$ é o dobro do produto interno vezes a coordenada correspondente de $v$, somado sobre todos os pontos. É o que `2 * (X @ w_dir) @ X` faz: `X @ w_dir` são os $n$ produtos internos; multiplicá-los por `X` pela esquerda pesa cada linha pelo seu produto e soma as linhas — a coordenada $i$ do resultado é $\sum_v 2\,(v \cdot w_{dir})\, v_i$. É cálculo de uma variável aplicado termo a termo, não uma fórmula nova."*

**Chunk `first-principal-component`:**

```{python}
#| label: first-principal-component
#| warning: false
import tqdm
from scratch_np.gradient_descent import gradient_step

def first_principal_component(X: np.ndarray,
                              n: int = 100,
                              step_size: float = 0.1) -> np.ndarray:
    # começa com um palpite aleatório
    guess = np.ones(X.shape[1])

    with tqdm.trange(n) as t:
        for _ in t:
            dv = directional_variance(X, guess)
            gradient = directional_variance_gradient(X, guess)
            guess = gradient_step(guess, gradient, step_size)
            t.set_description(f"dv: {dv:.3f}")

    return direction(guess)

fpc = first_principal_component(de_meaned)
assert 0.923 < fpc[0] < 0.925
assert 0.382 < fpc[1] < 0.384
fpc
```

Saída: `array([0.92373078, 0.38304235])` — **os dois `assert` valem sem mudança** (diferença para a versão em listas: 5,6×10⁻¹⁷). A chamada `gradient_step(guess, gradient, step_size)` fica **literalmente** assim: é ela que `tests/test_gradiente.py` registra em `PASSO_NAO_NEGATIVO["cap07/08-reducao-de-dimensionalidade.qmd"]`.

**Callout `important`** → *"...É o único uso ascendente do livro inteiro — e é o mesmo `gradient_step` do Capítulo 5, agora em arrays, usado ao contrário."* **Callout `note`** → *"O comentário diz "palpite aleatório", mas repare no código: `guess` começa sempre em `np.ones(2)` — `[1.0, 1.0]`. Não há aleatoriedade nenhuma aqui — o gradiente parte sempre do mesmo lugar e segue sempre o mesmo caminho, então o resultado é determinístico e os dois `assert` acima valem em qualquer execução, sem precisar de `default_rng`. Uma inicialização genuinamente aleatória — como a que o [Capítulo 15](../cap15/index.qmd) usa para os pesos de uma rede neural — exigiria a semente para valer o mesmo."*

**Chunk `fig-pca-primeiro-componente`:** `plt.scatter(de_meaned[:, 0], de_meaned[:, 1], ...)`; a reta com `fpc[0]`, `fpc[1]` fica igual.

**Chunk `project`:**

```{python}
#| label: project
def project(v: np.ndarray, w: np.ndarray) -> np.ndarray:
    """devolve a projeção de v na direção w"""
    projection_length = v @ w
    return projection_length * w
```

**Chunk `remove-projection`:**

```{python}
#| label: remove-projection
def remove_projection_from_vector(v: np.ndarray, w: np.ndarray) -> np.ndarray:
    """projeta v em w e subtrai o resultado de v"""
    return v - project(v, w)

def remove_projection(X: np.ndarray, w: np.ndarray) -> np.ndarray:
    """remove de cada linha de X a projeção dela em w"""
    return X - np.outer(X @ w, w)

residual = remove_projection(de_meaned, fpc)
assert np.allclose(residual @ fpc, 0)
```

Prosa nova depois: *"`remove_projection_from_vector` é a versão de um ponto; `remove_projection` faz o mesmo para todas as linhas sem laço. `np.outer(a, w)` é o **produto externo**: a matriz cuja linha $i$ é `a[i] * w` — aqui, a projeção de cada linha de `X` em `w`, empilhadas. Subtraí-la de `X` é aplicar `remove_projection_from_vector` linha a linha de uma vez. O `assert` confere o que a figura seguinte mostra: não sobrou nada na direção de `fpc`."*

**Chunk `fig-pca-residual`:** `xs3`/`ys3` → `plt.scatter(residual[:, 0], residual[:, 1], ...)`.

**Chunk `pca`:**

```{python}
#| label: pca
def pca(X: np.ndarray, num_components: int) -> np.ndarray:
    """Devolve as componentes como linhas de uma matriz (num_components, d)"""
    components = []
    for _ in range(num_components):
        component = first_principal_component(X)
        components.append(component)
        X = remove_projection(X, component)

    return np.array(components)
```

O laço **fica**: é o algoritmo (uma componente por vez, removendo a projeção). A única mudança é o retorno: uma matriz `(k, d)`, uma componente por linha, para que `transform` seja um produto.

**Chunk `transform`:**

```{python}
#| label: transform
#| warning: false
def transform_vector(v: np.ndarray, components: np.ndarray) -> np.ndarray:
    return components @ v

def transform(X: np.ndarray, components: np.ndarray) -> np.ndarray:
    return X @ components.T

componentes = pca(de_meaned, 2)
transformado = transform(de_meaned, componentes)
componentes, transformado[:5]
```

Saída:

```
(array([[ 0.92373078,  0.38304235],
        [-0.38296839,  0.92376145]]),
 array([[ 1.26171746,  1.30313287],
        [ 0.334121  , -5.64698882],
        [ 6.03345064,  0.53536004],
        [-3.42467839, -2.25891106],
        [ 2.51105545,  3.19333558]]))
```

Prosa nova antes do parágrafo *"Com as duas componentes deste exemplo bidimensional..."*: *"`transform_vector` é `components @ v`: um produto interno por componente, os $k$ de uma vez. `transform` é o mesmo para todas as linhas, `X @ components.T` — forma `(n, d)` vezes `(d, k)`, que dá `(n, k)`: cada ponto reexpresso em $k$ coordenadas novas. A segunda componente saiu $(-0{,}383,\ 0{,}924)$, perpendicular à primeira — é a direção que a figura do resíduo prometia."*

**Callout `scikit-learn`** — acrescentar, depois de *"...como o seu `first_principal_component` depende."*: *"Trocar listas por arrays não mudou o método: `X @ w_dir` só faz de uma vez o que o laço fazia ponto a ponto; a subida do gradiente, com seus 100 passos e seu palpite inicial, é a mesma. O SVD é outro algoritmo."* O resto do callout fica (as duas pegadinhas e os sinais).

**Números a recalcular:** **nenhum** — a PCA é determinística e os arrays reproduzem a versão em listas; `fpc`, os `assert`s e as três menções a $(0{,}924,\ 0{,}383)$ continuam. Só `transformado[:5]` e `componentes` ganham forma de array. **Semente:** nenhuma, e o callout explica por quê.

---

## 4. Riscos e decisões

**`assert`s do Grus.** Todos continuam valendo: os dois de `fpc` (reproduzidos a 6×10⁻¹⁷); os quatro de `scale`/`rescale` (exatos com `ddof=1` e `np.array_equal`; **falhariam com o `ddof=0` padrão**, que é o ponto da prosa nova); os de `parse_row`/`try_parse_row` (código inalterado, agora no módulo); `melhor_mes == 10`. O `assert` de correlação `(0.89, 0.91)` só existe em prosa, e com a semente 4 do `default_rng` a correlação (0,893) está **dentro** da janela — o texto usa isso ("a três milésimos da borda").

**Sorteios que mudam, e o que depende deles.** Só a 7.1: as médias dos dois conjuntos, as duas correlações, a matriz 4×4, e as quatro figuras. Nenhuma frase quantitativa fora do callout depende deles; as qualitativas da matriz foram conferidas com a semente 0 (-0,98; 0,62; 40/60). Se o implementador trocar alguma semente, reconfere as três afirmações qualitativas — com as sementes 1 e 2, por exemplo, elas também valem (-0,98/0,67/48–52 e -0,98/0,58/34–66).

**A subida de gradiente com passo fixo é frágil, e a versão em arrays herda isso.** Descobri montando o `assert` do módulo: num conjunto cuja variância residual é pequena, 100 passos de `step_size=0.1` a partir de `np.ones` mal saem do palpite inicial, e a "segunda componente" sai não ortogonal à primeira (num teste, `c1 @ c2 = 0,93`). O código do Grus em listas tem **exatamente** o mesmo comportamento — não é regressão da reescrita, é o algoritmo. Em `pca_data` a variância residual é grande (8,4 × 98) e a segunda componente sai ortogonal a 8×10⁻⁵. Não mudei o algoritmo (a spec manda escrever o do livro); o dataset do `assert` do módulo foi construído para convergir. Vale uma frase no callout `scikit-learn`? Ele já diz *"sem depender de tamanho de passo, número de iterações ou um palpite inicial"* — é isso; não acrescentei mais.

**`directional_variance_gradient` não é o gradiente de `directional_variance`.** Conferido numericamente: a função normaliza `w`, então seu gradiente verdadeiro é ortogonal a `w`; a fórmula do Grus é o gradiente de $\sum (v \cdot w)^2$ **sem** a normalização, avaliado em `w_dir`. Funciona como uma iteração de potência disfarçada. O texto do livro já apresenta a fórmula como "dada"; a reescrita reproduz a fórmula, não a corrige — corrigi-la mudaria o resultado numérico e a correspondência com o Grus. Registro para quem um dia revisar a matemática da seção.

**Tempo de execução.** Medido no host: 7.1 ~1,5 s (o `dot` de um milhão domina, ~30 ms; `inverse_normal_cdf` vetorizada sobre 22 mil valores, ~10 ms); 7.5 0,3 s (o `dateutil.parse` de 23 mil datas domina — inalterado); 7.7 ~4 s (`tqdm`) + os primos (~3–5 s, inalterado); 7.8 < 1 s (a PCA em arrays roda 100 passos em 0,01 s — a barra do `tqdm` termina antes de aparecer, o que é aceitável). O módulo importa em 0,5 s. `executar-notebooks.py cap07` deve cair de dezenas de segundos para ~10 s.

**O que fica como laço e por quê.** `for symbol in np.unique(symbols)` (4 grupos) ×2 na 7.5; a grade 4×4 de painéis na 7.1; `for _ in range(num_components)` no `pca` e `for _ in t` no `first_principal_component` (o laço **é** o algoritmo); `primes_up_to` (demonstração de `tqdm`, sem dado); as compreensões do `csv.DictReader` e de `try_parse_row` (parsing é linha a linha por definição). O que **some**: `bucketize` por ponto, `random_normal` por chamada, `random_row` por linha, a transposição por `zip(*)`, o `max_prices` por linha, o `defaultdict(list)`, o `sorted` de tuplas, o `zip(prices, prices[1:])`, `changes_by_month`, o laço duplo de `rescale`, e toda compreensão sobre `data` na 7.8.

**Decisão: o bloco de custo mede o `dot`, não a média por coluna.** Explicado em §3.1: a média por coluna dá ~8× e mentiria "duas ordens de grandeza"; o `dot` dá ~100× e é o exemplo que abre o bloco. A prosa afirma a ordem de grandeza, não o número — o número é saída de célula.

**Decisão: `make_histogram` devolve `(buckets, counts)`, não um `dict`.** É o que `np.unique(return_counts=True)` devolve e o que `plt.bar` recebe; um `dict` intermediário seria reconstruir o `Counter` por cima do array.

**Decisão: `correlation_matrix` existe, com uma linha.** A spec diz para não *"reimplementar em Python o que a linha à esquerda já faz"*; aqui a função não reimplementa — fixa o `rowvar=False`, que é a convenção do livro e a pegadinha real do `np.corrcoef`. Sem a função, a pegadinha ficaria sem lugar para ser explicada.

**Decisão: `random_normal(n, rng)` via `inverse_normal_cdf`, não `rng.normal`.** O andaime vetorizou `inverse_normal_cdf` dizendo que *"os capítulos 7, 12 e 16 precisam destas duas funções"*, e a 7.1 apresenta a inversa da acumulada como o mecanismo. `rng.normal(size=n)` faria o mesmo em menos texto, mas esconderia justamente o que a seção mostra. O `random_normal` do módulo é o que os caps. 12 e 16 devem importar se precisarem.

**Decisão: 7.5 desempacota em três arrays paralelos, e `pct_change`/`DailyChange` saem.** É a forma mais simples de "dicts onde heterogêneo, arrays onde numérico": o registro heterogêneo fica no parsing (`StockPrice`), e a resposta ("AAPL em 1997-08-06: +33,23%") é impressa com f-string em vez de embalada num `NamedTuple` novo. `DailyChange` não entra no módulo. Alternativa considerada e descartada: `np.lexsort((dates, symbols))` e uma máscara `symbols[1:] == symbols[:-1]` — três linhas mais compactas, mas escondem o agrupamento que a seção existe para ensinar.

**Decisão: `dates` como `datetime64[D]`, e o mês por `astype("datetime64[M]")`.** A alternativa — array de `datetime.date` com `dtype=object` — ordena e compara, mas o mês voltaria a ser uma compreensão. `datetime64` é o tipo do numpy para datas, e a conversão de unidade é a forma idiomática; a prosa explica o `% 12 + 1`. Verificado contra `.month` em todas as 23.101 datas.

**Decisão: `#| echo: false` em `pca-data` fica, e o `import numpy as np` aparece em dois chunks da 7.8.** O chunk oculto precisa do `np.array`; o primeiro chunk visível (`de-mean`) repete o import para o leitor não ver `np` surgir do nada. `gerar-notebooks.py` ignora `echo` (só entende `eval` e `fig-cap`), então no notebook a lista aparece — como já acontece hoje.

**Decisão: sem `Vector`, sem `Matrix`, sem alias.** Anotações são `np.ndarray`, como a spec manda. O `from scratch.linear_algebra import Matrix, Vector, make_matrix` da 7.1 e os cinco imports de `linear_algebra` da 7.8 somem sem substituto.

**Decisão: o `.exemplo` da 7.2 aponta para o cap. 10, não para o 9.** Registrado em §3.2. **O planejador do cap. 9 deve saber** que a 7.2 não promete mais `LabeledPoint`.

**Decisão: a pendência do cap. 6 (`genfromtxt` na 7.4) entra.** Nove linhas num callout; o contraste "rejeita em voz alta / sobrevive em silêncio / nem lê a data" é o melhor argumento da seção a favor do `try_parse_row`, e fecha a ponte que a 6.1 abriu.

**Risco: `np.float64(...)` nas saídas.** O numpy 2 exibe escalares assim dentro de tuplas (`arrays-dot`, `arrays-indexacao`, `arrays-axis`, `max-aapl`, `correlacoes`). Precedente do cap. 6; onde a saída é um `dict` (mês → variação) usei `float()` para as chaves e valores saírem limpos, e onde é um resultado para ler (7.5) usei `print`. O revisor não deve pedir para "limpar" as tuplas — é a saída real.

**Risco: o link da 6.1 para a 7.1 não tem âncora.** O plano do 6 deixou `[seção 7.1](../cap07/01-explorando-seus-dados.qmd)` sem `#de-listas-a-arrays`, esperando o bloco existir. Com o bloco no lugar, acrescentar a âncora é uma edição de **um caractere em `content/cap06/`** — fora do commit deste capítulo; fica para o orquestrador decidir (o link funciona sem ela).

**Risco: `test_notebooks` e os chunks em callout.** O livro tinha 19 chunks executáveis dentro de callouts; a 7.4 acrescenta o 20º (`genfromtxt-csv-sujo`). `test_todo_chunk_executavel_do_livro_virou_celula` conta de forma independente, então não há número fixo a atualizar — mas o `CLAUDE.md` diz "19" em prosa; é o passe de coerência global que ajusta, não este capítulo.

**Sem desvio da spec além dos registrados acima.** Nenhum `sklearn` em chunk executável; nenhum `scratch.` (os seis imports viram `scratch_np.` ou numpy); nenhum `random.`; toda semente por `default_rng`; nenhum "Python puro" na prosa nova (conferir com `grep -rni "python puro" content/cap07/` → vazio antes e depois).

---

## 5. Verificação

No host, a partir da raiz:

```bash
export MPLBACKEND=Agg PYTHONHASHSEED=0
.venv/bin/python -c "import scratch_np.working_with_data"     # o módulo, com todos os asserts, sem saída
.venv/bin/python scripts/executar-secoes.py 7                 # cada .qmd num kernel próprio
.venv/bin/python scripts/gerar-notebooks.py                   # só notebooks/cap07-trabalhando-com-dados.ipynb deve mudar
.venv/bin/python scripts/executar-notebooks.py cap07          # o capítulo num kernel só
.venv/bin/pytest tests/ -q
```

Linha de base antes da reescrita: `pytest tests/ -q` → 52 passed (registrado pelo implementador do cap. 6). Contagem de chunks esperada depois: 7.1 sobe de 13 para **19** (sete do bloco, menos os dois `include: false`, e `agrupa`…); 7.4 sobe de 4 para 5; 7.5 cai de 11 para **10** (`agrupa-por-simbolo` + `ordena-por-data` viram um, `desempacota-em-arrays` entra); 7.6 cai de 6 para 5; 7.8 continua com 14.

Em `tests/test_scratch_np.py`:

- `CAPITULOS_NUMPY = {6, 7}` — depois dos cinco comandos verdes, não antes.
- `PYTHON_PURO_PERMITIDO`: **nenhuma entrada** para o capítulo 7. Não há a expressão hoje, e a prosa nova não a usa.
- `RANDOM_PERMITIDO`: **nenhuma entrada**. `grep -rn "random\." content/cap07/` deve devolver só `np.random.default_rng` e `np.random.Generator` (a anotação de tipo), que o regex do teste não casa.
- `REEXPORTA_DADOS`: **sem entrada** para `working_with_data.py` — o módulo não importa `scratch.` (§2).
- `test_capitulo_numpy_importa_numpy`, `test_capitulo_numpy_nao_importa_scratch_puro`, `test_capitulo_numpy_usa_default_rng` passam pelo que §3 descreve; `test_pacote_importa_sem_rede_e_sem_erro` passa a importar o módulo novo (asserts rodam no subprocesso).

Em `tests/test_gradiente.py`: **nada muda**. A entrada `cap07/08-reducao-de-dimensionalidade.qmd` continua descrevendo a única chamada com passo positivo, e a chamada continua com o mesmo texto.

Checagens manuais do revisor, além das de praxe:

1. O bloco *De listas a arrays* está entre o parágrafo de abertura e `## Dados unidimensionais`, com esse título exato (a 6.1 o cita), e tem entre 80 e 120 linhas.
2. As nove saídas da tabela de §3.1 batem, exceto os tempos do último chunk, cuja **razão** deve ser compatível com "duas ordens de grandeza".
3. `content/cap07/01-explorando-seus-dados.qmd` não tem mais `include: false`, `scratch.`, `random.seed` nem `Counter`.
4. As três afirmações qualitativas sobre a matriz 4×4 valem para a matriz impressa.
5. Na 7.5, os cinco números da prosa (23.105; 33%/6 de agosto de 1997; 52%/29 de setembro de 2000; outubro/0,29%) batem com as saídas novas.
6. Na 7.6, os quatro `assert`s passam com `np.array_equal` (não `np.allclose`) — é a prova de que `ddof=1` reproduz o Grus exatamente.
7. Na 7.8, `fpc` continua `[0.92373078, 0.38304235]` e a chamada `gradient_step(guess, gradient, step_size)` não foi reescrita.
8. Nenhum chunk executável de `content/cap07/` contém `sklearn`, `scratch.` ou `random.`; nenhum chunk importa `scratch_np.working_with_data` **na seção que define a função** (7.4 define `try_parse_row`; 7.5 importa — nunca o contrário).
9. `notebooks/cap07-trabalhando-com-dados.ipynb` não tem saída gravada e tem o número de células de código que a contagem acima prevê.

Commit atômico: `content/cap07/index.qmd`, `01-explorando-seus-dados.qmd`, `02-namedtuples.qmd`, `04-limpeza-e-transformacao.qmd`, `05-manipulando-dados.qmd`, `06-reescalonamento.qmd`, `07-um-parenteses-tqdm.qmd`, `08-reducao-de-dimensionalidade.qmd`, `scratch_np/working_with_data.py`, `notebooks/cap07-trabalhando-com-dados.ipynb`, `tests/test_scratch_np.py`, e este plano (com a seção "Desvios na implementação" preenchida). `03-dataclasses.qmd` **não** entra: não muda. Mensagem: `feat(cap07): reescrita em numpy — bloco De listas a arrays na 7.1, scratch_np/working_with_data`. Não usar `git add -A`.

`make render`, `make refresh`, `make offline` e `make clean` são do orquestrador.

---

## Desvios na implementação

Registrado pelo implementador em 2026-09-07, depois de executar o plano no host (numpy 2.5.2, Python 3.12.13, `.venv/`).

**Nenhum `assert` foi afrouxado e nenhum chunk saiu diferente do que §3 escreveu.** Todo número que §3 previa bateu com a execução real, lido das células executadas com `nbclient` (não da memória): `(32.0, np.float64(32.0))`; `((3, 3), dtype('float64'))`; as quatro saídas de indexação/máscara/*broadcasting*/`axis`/`argmax` da tabela de §3.1, palavra por palavra; `'médias: -0.12 e 0.94'`; `(np.float64(0.8931779883670059), np.float64(-0.8974145439902339))`; a matriz 4×4 com -0,98/0,62/0,82; `array([90.91, 41.68, 64.5 , 91.86,   nan, 64.34])`; `23105`; os três arrays paralelos e `np.unique` = AAPL/FB/GOOG/MSFT; `np.float64(232.070007)`; AAPL 232.07 / FB 217.50 / GOOG 1268.33 / MSFT 115.61; `{'AAPL': 9584, 'FB': 1655, 'GOOG': 3607, 'MSFT': 8259}`; `23101`; `maior alta:  AAPL em 1997-08-06: +33.23%` / `maior queda: AAPL em 2000-09-29: -51.87%`; o `dict` mês → variação idêntico ao antigo; `(10, '0.29%')`; `array([10.77, 22.14, 11.4 ])` e `array([14.28, 27.53, 13.37])`; `(array([-1.,  0.,  1.]), array([2., 1., 0.]))` e a matriz reescalonada; `9592`; `(99, 2)`; `array([0.92373078, 0.38304235])` (os dois `assert` valem sem mudança); `componentes` e `transformado[:5]` exatamente como §3.8. As três afirmações qualitativas sobre a matriz 4×4 continuam valendo com a semente 0.

O que saiu diferente do plano, e por quê:

1. **A razão do chunk de custo saiu 95×** (`'laço: 32 ms; array: 0.33 ms; razão: 95x'`), não 103×: varia por máquina, como §3.1 previa; está dentro de "duas ordens de grandeza" (o gatilho para trocar a prosa era abaixo de ~50×), então a frase ficou como está.

2. **A medição "com o gerador do `numpy`" deu 24,6%, não 24,3%.** O plano citava 24,3% sem dizer como mediu. Medido nesta execução com a construção literal do capítulo — `random_normal(1000, rng)` via `inverse_normal_cdf` do andaime, `xs + random_normal(...)/2`, `np.corrcoef` —, 20 mil repetições com `default_rng(20260907)`: **4.914/20.000 = 24,6%**, média da correlação 0,8944, em 70 s. Cruzamento com `rng.standard_normal` no lugar da inversa da acumulada (a distribuição que ela aproxima a 10⁻⁵), 20 mil repetições com outra semente: 4.976/20.000 = 24,9%, média 0,8943 — a mesma distribuição, como o callout afirma. O callout da 7.1 diz 24,6%. (Uma primeira tentativa de medir foi abandonada por eu ter superestimado o custo do `np.vectorize(math.erf)` em uma ordem de grandeza; a medida acima é a definitiva.)

3. **O bloco *De listas a arrays* tem 125 linhas** (do título até a linha anterior a `## Dados unidimensionais`), não as 118 que §3.1 contou; o alvo da spec era 80–120. Mantive o texto do plano na íntegra em vez de cortar para bater número — a checagem 1 do revisor deve ler "125"; se quiser 120, o corte sem perda é o parágrafo final ("É a promessa que o callout do Capítulo 4 fez...").

4. **A 7.1 tem 20 chunks, não 19.** O bloco novo tem **nove** chunks (`arrays-dot`, `-forma-e-tipo`, `-indexacao`, `-mascaras`, `-broadcasting-escalar`, `-broadcasting-vetor`, `-axis`, `-argmax`, `-o-laco-que-some`), não os "sete" de §5: 13 − 2 (`include: false`) + 9 = 20. As outras contagens bateram: 7.4 = 5, 7.5 = 10, 7.6 = 5, 7.7 = 2, 7.8 = 14.

5. **A linha-amostra do `tqdm` da 7.7 é `57/100`, não `56/100`.** A ~26 it/s a barra se redesenha a cada 3 iterações (54, 57, 60...), então não existe frame "56/100". Colei o frame real do kernel: ` 57%|█████▋    | 57/100 [00:02<00:01, 26.43it/s]` — com a barra de 10 caracteres que o kernel (sem tty) desenha, não a barra larga do exemplo antigo. É a linha literal desta execução.

6. **`Counter` ainda aparece uma vez na 7.1**, na prosa do `.exemplo` — é o contraste com o cap. 3 que o próprio §3.1 escreveu (*"Lá a contagem era um `Counter`..."*). A checagem 3 do revisor ("não tem mais `Counter`") vale para os chunks; nenhum chunk o usa.

7. **Prosa que entrou além do marcado em §3:** (a) depois do chunk `pca`, o parágrafo *"O laço fica: é o algoritmo — uma componente por vez, removendo a projeção antes de procurar a próxima. A única mudança é o retorno: uma matriz `(k, d)`, uma componente por linha, para que `transform` seja um produto."* — §3.8 o tinha como nota do planejador, e o leitor precisa dele porque a mudança de retorno é o que faz `transform` virar um produto; (b) na 7.5, a frase *"Há 23.101 variações, quatro a menos que linhas — uma por ação."* logo após `todas-as-variacoes`, que §3.5 escreveu fora do bloco de prosa; (c) na 7.8, a frase do comprimento ficou *"tem comprimento `x @ d` — é o quanto daquele ponto "cabe" naquela direção — e `X @ d` calcula os $n$ comprimentos de uma vez, um por linha"*, mesclando o trecho novo com o final da frase original. O parágrafo novo do `de-mean` entrou entre o chunk e o parêntese *"(Se não fizermos isso, ...)"*.

8. **Tempo de execução maior que o previsto, por um chunk inalterado.** `executar-notebooks.py cap07` → 39,8 s (§4 previa ~10 s); `executar-secoes.py 7` → 7.7 em 37,9 s e as outras sete seções em 10,2 s somadas. O que domina é `primes_up_to(100_000)` da 7.7, que o plano mandou manter; a barra do `tqdm-basico` leva ~4 s, como medido.

9. **`executar-secoes.py`/`executar-notebooks.py` não mostram saída de célula.** Para ler os números acima usei um script no scratchpad que reaproveita `celulas_de_codigo` e `PREPARO` de `executar-secoes.py` e imprime a saída de cada célula com `nbclient`; não entra no repositório.

**Verificação (§5):** `.venv/bin/python -c "import scratch_np.working_with_data"` importa em 0,65 s sem saída; `gerar-notebooks.py` alterou só `notebooks/cap07-trabalhando-com-dados.ipynb` (139 células, 65 de código, 0 saídas gravadas, sem a linha do Colab); `executar-secoes.py 7` → 8/8 OK, cada uma no seu kernel; `executar-notebooks.py cap07` → OK; `pytest tests/ -q` → **52 passed** antes e depois de `CAPITULOS_NUMPY = {6, 7}`. `PYTHON_PURO_PERMITIDO`, `RANDOM_PERMITIDO` e `REEXPORTA_DADOS` continuam sem entrada para o capítulo 7. `grep -rni "python puro" content/cap07/`, `grep -rn "random\." content/cap07/ | grep -v np.random`, `grep -rn "from scratch\." content/cap07/` e `grep -n "include: false\|random.seed" content/cap07/01-*.qmd` — todos vazios. `content/cap07/03-dataclasses.qmd` não foi tocado. `tests/test_gradiente.py` não mudou e passa: a chamada `gradient_step(guess, gradient, step_size)` está literalmente igual.

**Fora do commit, para o orquestrador:** `docs/superpowers/plans/2026-09-06-numpy-00-instrucoes.md` apareceu modificado durante a implementação (o *Briefing para o planejador*) — não é deste capítulo. E `content/cap04/01-vetores.qmd`, no callout que o bloco *De listas a arrays* cita, diz *"a regra deste livro — nada de NumPy"* — é o passe de coerência global da spec que ajusta, não este capítulo.

---

## Correções da revisão

Veredito do revisor (2026-09-07): aprovado com correções menores — dez frases, nenhum chunk nem número de saída. Aplicadas todas, no commit `fix(cap07)`:

1. 7.1, *O laço que some*: *"duas ordens de grandeza"* → *"cerca de cem vezes — o número exato varia por máquina e por execução"* (medições entre 67× e 196× conforme o processo e o aquecimento do `@`).
2. 7.5, `day_over_day_changes`: o cap. 2 apresenta `zip(list1, list2)`, não `zip(prices, prices[1:])` → *"o `zip` do Capítulo 2, que o Grus usa aqui como `zip(prices, prices[1:])`"*.
3. 7.8, callout do palpite: `np.ones(2)` → *"`np.ones(X.shape[1])` — aqui, `[1.0, 1.0]`"*, que é o que o código diz.
4. 7.1, *Dados unidimensionais*: `x.std(ddof=1)` aparecia antes de `ddof` ser explicado → acrescentado *"(o `ddof=1` pede o desvio amostral — a seção 7.6 explica)"*.
5. 7.1, abertura do bloco: o `get_column` está no callout de `02-matrizes.qmd`, não no de `01-vetores.qmd` → *"O Capítulo 4 fechou cada seção com um callout..."*, com link para os dois arquivos.
6. 7.1, bloco *De listas a arrays* tinha 126 linhas (spec: 80–120) → cortado o par chunk/prosa `arrays-argmax`, e a frase que o anunciava termina em *"aceitam o mesmo `axis`."*. Consequência: a 7.5, que dizia *"é o `argmax` do bloco *De listas a arrays*"*, agora o apresenta ali (*"entra um método novo, `argmax` — primo das reduções por `axis` do bloco..."*), porque é a primeira vez que o leitor o vê. A 7.1 fica com 19 chunks; o bloco, com 118 linhas.
7. 7.1, fecho do bloco: a promessa do cap. 4 (entender o `@`) foi cumprida; o que ele adiou foi usá-lo → *"É a promessa que o callout do Capítulo 4 fez — [...] — cumprida; o que ele adiou de propósito foi usar o `@`."*
8. 7.5: parágrafo que começava em minúscula (*"e depois juntar..."*, sobra da frase antiga) → *"Depois, juntar..."*.
9. 7.1, *Máscaras booleanas*: 23.105 − 9.584 = 13.521 → *"das outras 13,5 mil"*.
10. 7.5, callout do `assert all`: o `StockPrice` do módulo carrega o `is_high_tech` da 7.2 → acrescentado *"(com o `is_high_tech` da 7.2)"*.

Nenhum chunk mudou além do cortado no item 6; as saídas continuam as de "Desvios na implementação". Verificação repetida antes do commit: `gerar-notebooks.py` (só o notebook do cap. 7 muda), `executar-secoes.py 7` (8/8), `pytest tests/ -q` (52 passed).
