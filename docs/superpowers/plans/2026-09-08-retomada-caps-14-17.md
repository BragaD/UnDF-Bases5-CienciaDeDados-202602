# Retomada da reescrita em numpy — capítulos 14 a 17

**Data:** 2026-09-08
**Branch:** `reescrita-numpy`
**Estado:** capítulos 6 a 13 prontos; 14 a 17 pendentes; falta o passe de coerência global.

Este documento existe para que a próxima sessão comece **despachando**, não redescobrindo. A spec é `docs/superpowers/specs/2026-09-06-reescrita-numpy-design.md`; o processo e o briefing comum estão em `2026-09-06-numpy-00-instrucoes.md`. Leia os dois antes de qualquer coisa.

## O que está pronto

| Cap. | Commits | Módulo em `scratch_np/` |
|---|---|---|
| 6 Obtendo Dados | `7f029ff`, `0842282` | — (primeiro contato com `np.loadtxt` na 6.1) |
| 7 Trabalhando com Dados | `7b6b345`, `cfaa849` | `working_with_data.py` + o bloco *De listas a arrays* na 7.1 |
| 8 Machine Learning | `c41f1a9`, `8a6d389` | `machine_learning.py` |
| 9 k-Vizinhos | `fb68f78`, `302fa4b` | `k_nearest_neighbors.py` |
| 10 Naive Bayes | `d9bd87c`, `2b38aa7` | `naive_bayes.py` |
| 11 Regressão Linear Simples | `633f662`, `5142f95` | `simple_linear_regression.py` |
| 12 Regressão Múltipla | `73a65b9`, `58d6479` | `multiple_regression.py` |
| 13 Regressão Logística | `ab92a65`, `9c94816` | `logistic_regression.py` |

`CAPITULOS_NUMPY = {6, …, 13}` em `tests/test_scratch_np.py`, sem nenhuma exceção registrada em `PYTHON_PURO_PERMITIDO` nem em `RANDOM_PERMITIDO` — os oito capítulos passaram sem precisar de dispensa. Suíte: 52 testes verdes. Último render: `render OK (tentativa 1)`.

## O que falta

Quatro capítulos, na ordem, um por vez (planejador Opus → implementador → revisor → correções), e depois o passe de coerência global. Os mapas abaixo foram levantados nesta sessão contra os arquivos reais; confira-os, não confie neles cegamente.

### Capítulo 14 — Árvores de Decisão (Grus 17), 6 seções

Dono de `scratch_np/decision_trees.py`. É o capítulo em que **numpy ajuda menos**, e o plano precisa dizer isso com todas as letras — forçar array onde o dado é categórico e a estrutura é recursiva seria decoração, exatamente o que a spec proíbe.

- Onde numpy ganha: `entropy` sobre `np.unique(y, return_counts=True)` com `-np.sum(p * np.log2(p))`; `partition_by` como máscaras booleanas (`X[:, j] == valor`); `np.argmin` sobre as entropias por atributo; rótulo majoritário por `np.unique` + `argmax`.
- Onde o laço fica: a recursão de `build_tree_id3` e o `classify` — o laço **é** o algoritmo.
- `inputs` (os 14 candidatos, `NamedTuple Candidate` com level/lang/tweets/phd + rótulo) vem de `scratch.decision_trees` por reexportação; decida a representação (matriz de strings `(14, 4)` + `y` booleano, com os nomes das colunas à parte) e mantenha-a coerente no capítulo inteiro.
- **As entropias não dependem de sorteio** e devem bater com as publicadas — use isso como verificação forte.
- Florestas (14.6): três experimentos com `random.seed(12)`, `(0)` e `(1)`; viram três `default_rng`, e o callout *A semente não é opcional* da 14.6 já diz que `random.seed` não afeta o `numpy.random` — essa frase fica **mais** verdadeira depois da reescrita, não menos.
- Cuidado: a 14.6 tem experimento sintético com `gera(n, ruido)` e varredura de profundidade; os números mudam com o sorteio novo.

### Capítulo 15 — Redes Neurais (Grus 18), 4 seções

Dono de `scratch_np/neural_networks.py`. **Contrato fixado na spec** (o cap. 16 importa): `sigmoid(t)`, `binary_encode(x) -> (10,)`, `fizz_buzz_encode(x) -> (4,)`.

- Representação: cada camada é **uma matriz** `(n_saídas, n_entradas + 1)` com o viés na última coluna; a rede é uma lista de matrizes. `feed_forward` devolve a lista de saídas por camada; `sqerror_gradients` devolve a lista de gradientes, com `np.outer(deltas, entradas_com_viés)` e `W[:, :-1].T @ deltas` para propagar.
- Verificação sem sorteio: a rede XOR com os pesos fixos do Grus (`[[20, 20, -30], [20, 20, -10]]` e `[[-60, 60, -30]]`) tem de reproduzir as quatro saídas.
- O Fizz Buzz levava **cerca de um minuto** em Python puro, e a prosa da 15.4 afirma isso e discute o platô do treino (epochs 20 e 50 respondendo "o próprio número" para os 923 exemplos). Meça a versão em numpy: se o platô mudar de lugar, a prosa muda junto — é o trecho mais instrutivo do capítulo.
- A 15.3 afirma "vinte mil epochs custam menos de um segundo" e discute a simetria dos pesos aprendidos (`[7, 7, -3]`, `[5, 5, -8]`, `[11, -12, -5]`): tudo isso é `[RECALCULAR]`.

### Capítulo 16 — Deep Learning (Grus 19), 8 seções, 2.465 linhas — o maior

Dono de `scratch_np/deep_learning.py`. Duas decisões estruturais que o plano precisa tomar **antes** de escrever qualquer chunk:

1. **A 16.1 muda de assunto.** Hoje ela constrói `Tensor` como listas aninhadas e escreve seis funções recursivas (`shape`, `is_1d`, `tensor_sum`, `tensor_apply`, `zeros_like`, `tensor_combine`). Com `Tensor = np.ndarray`, todas viram `.shape`, `np.sum`, ufuncs, `np.zeros_like` e operações elemento a elemento. A seção **continua existindo com o mesmo nome** e passa a ser sobre essa troca — o aluno vem do cap. 4 em listas e do bloco *De listas a arrays* da 7.1. O `##` "`shape` acredita no primeiro elemento" (o tensor mentiroso) some ou vira outra coisa: o ndarray não aceita linhas de tamanhos diferentes.
2. **Exemplo a exemplo ou lote inteiro?** O Grus treina um exemplo por vez. Processar o lote como matriz é a otimização natural do numpy e é o que torna o MNIST viável, mas muda a forma dos gradientes (`X.T @ grad`). Escolha, justifique na prosa e **mantenha em todo o módulo** — meia decisão aqui contamina oito seções.

Outros pontos: `param[:] =` (o `##` que explica por que não é `param =`) continua valendo e fica mais importante com arrays; `Dropout` com máscara do `rng`; `SoftmaxCrossEntropy` numericamente estável; a leitura do MNIST com `np.frombuffer` sobre os `.idx.gz` é o exemplo perfeito de numpy no livro inteiro. A 16.6 e a 16.7 **imprimem tempos medidos** e a 16.7 traz uma tabela comparativa de perdas, epochs e acurácia — tudo `[RECALCULAR]`, e o argumento "300 epochs e não 1.000, porque um epoch custa 0,12 s" muda de valor com a vetorização (pode passar a caber 1.000, e aí a comparação com o livro-texto melhora).

### Capítulo 17 — Clustering (Grus 20), 6 seções

Dono de `scratch_np/clustering.py`. `KMeans(k)` com `fit(X, rng)`: atribuição por distâncias vetorizadas (broadcasting `X[:, None, :] - means[None, :, :]` com norma em `axis=2`, ou um `norm` por centro — escolha o que o aluno lê melhor e explique), médias por `X[assignments == i].mean(axis=0)`, repetição até as atribuições não mudarem (o laço fica). O clustering de cores é onde numpy é mais natural: `np.asarray(img)` → `(n_pixels, 3)` por reshape, e a recoloração vira `means[assignments].reshape(altura, largura, 3)`. O hierárquico mantém `Leaf`/`Merged` e a fusão par a par, com a distância entre clusters por `np.min`/`np.max` sobre a matriz de distâncias.

## Passe de coerência global (depois dos quatro)

Estes arquivos ainda afirmam que o livro é todo em Python puro:

- `content/cap04/01-vetores.qmd` — o callout diz *"a regra deste livro — nada de NumPy"*, e é o **destino de links** dos capítulos 6 e 7. É o mais urgente.
- `content/cap04/index.qmd` — *"não usa `numpy` em lugar nenhum"*.
- `content/cap01/` — verificar a abertura e a hipótese motivadora.
- `index.qmd` da raiz e `README.md`.
- `docs/superpowers/specs/2026-08-15-estrutura-livro-bases5-design.md` — merece uma nota de correção apontando para a spec de 2026-09-06, no mesmo formato das notas que ela já carrega.
- `CLAUDE.md` já foi atualizado (`4f90a24`), mas revise a linha de estado da subseção "A reescrita em numpy" quando os quatro entrarem.

Depois: `make render`, `make offline` e `make notebooks-teste` (este demora ~11 min e vale rodar uma vez no fim).

## Armadilhas que custaram tempo nesta sessão

1. **Doze agentes em paralelo estouram o limite de sessão da API** e não produzem nada. Um capítulo por vez.
2. **Não espere um render com `until ! pgrep -f "quarto render"`** — o padrão casa com a linha de comando do próprio watcher, e o laço nunca termina. Espere pelo log (`grep -qE "render OK|ERROR"`) ou use `run_in_background` e a notificação.
3. **Planejadores escrevendo planos de 2.000 linhas travam num único `Write`.** Mande anexar em partes (`cat >> arquivo <<'EOF'`, uma seção por vez).
4. **Não escreva referência de seção sem conferir.** Dois planos apontaram para seções erradas (`&`/`~` estão na 10.5, não na 10.3) e um inventou um termo que o livro não usa ("polimorfia"). `grep -rn` antes.
5. **O revisor tem de entregar tudo numa resposta final** — dois revisores pararam esperando medição em segundo plano e precisaram ser cutucados.
6. **`site_libs/` na raiz** é lixo de render de `.qmd` isolado; já está no `.gitignore` e no `make clean`.
