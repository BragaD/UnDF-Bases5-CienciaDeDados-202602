# Cap. 11 — Regressão Linear Simples (Grus 14) em numpy

**Branch:** `reescrita-numpy` · **Spec:** `docs/superpowers/specs/2026-09-06-reescrita-numpy-design.md` · **Instruções:** `docs/superpowers/plans/2026-09-06-numpy-00-instrucoes.md`

Capítulo de três seções, todas em cima do mesmo conjunto: os 203 usuários "bons" da
DataSciencester. É o capítulo em que o laço sobre pontos some quase por inteiro — o
único laço que sobrevive é o das épocas da 11.2, porque ele *é* o algoritmo.

Todos os números deste plano foram medidos no host, com
`MPLBACKEND=Agg PYTHONHASHSEED=0 .venv/bin/python`, numpy 2.5.2 / scikit-learn 1.9.0.
Onde o valor está escrito, ele **foi obtido** — o implementador confere, não adivinha.

---

## 1. Inventário

### `index.qmd` — **nada muda**

Conferido linha a linha: não há uma única frase que descreva listas, Python puro, laço
sobre pontos ou o pacote `scratch/`. As duas ocorrências de "laço" são legítimas e
continuam verdadeiras depois da reescrita:

- linha 17: *"prometeu que a regressão linear simples tem uma **fórmula fechada** — duas médias, uma covariância, uma variância, sem laço nenhum"* — continua exato: `least_squares_fit` não tem laço nenhum.
- linha 22 (objetivo): *"Ajustar inclinação e intercepto por fórmula fechada, sem nenhum laço de otimização"* — idem.

A abertura fala de **0,9 minuto por amigo** (linha 15); o `beta` medido é
`0.9038659456058725`, então a frase se mantém. Os oito objetivos continuam válidos.
**Não edite `content/cap11/index.qmd`.**

### `01-o-modelo.qmd` (237 linhas)

| chunk | o que faz | importa de `scratch.` |
|---|---|---|
| `importa-dados` (`include: false`) | carrega os dados e fecha figuras | `statistics`: `num_friends`, `daily_minutes`, `num_friends_good`, `daily_minutes_good`, `mean`, `correlation` |
| `descreve-dados` | `len`, duas médias | — |
| `correlacao-dados` | `correlation(...)` | — |
| `correlacao-com-outlier` (dentro de `.callout-warning`) | `len` + correlação nos dados crus | — |
| `predict-error` | define `predict`, `error` escalares | — |
| `sum-of-sqerrors` | soma sobre um `zip` | `linear_algebra`: `Vector` |
| `least-squares-fit` | a fórmula fechada | `linear_algebra`: `Vector`; `statistics`: `correlation`, `standard_deviation`, `mean` |
| `teste-least-squares` | `assert least_squares_fit(x, y) == (-5, 3)` | — |
| `ajusta-dados-reais` | ajuste real + 2 `assert`s | — |
| `fig-regressao-amigos-minutos` | dispersão + reta | — |
| `r-squared` | `total_sum_of_squares`, `r_squared`, `assert` | `statistics`: `de_mean` |

Números afirmados pela prosa: 203 usuários / "quase 7 amigos" / "cerca de 29 minutos"
(saída de `descreve-dados`); "pouco mais de 0,57" (saída de `correlacao-dados`);
"0,2474" e "0,5737" e "43%" (callout do outlier); "100 amigos", "1 minuto por dia",
"49" (o segundo colocado); $\alpha \approx 22{,}95$ e $\beta \approx 0{,}904$;
R² "cerca de 0,329"; e a tabela de três linhas do callout `scikit-learn`.

Uma figura: `fig-regressao-amigos-minutos`.

**Frases que descrevem código em listas / Python puro e deixam de ser verdade:**

1. (l. 9) *"`scratch/statistics.py` já traz essa função pronta, junto com os dados da própria DataSciencester:"*
2. (l. 21) *"São duas listas paralelas, uma entrada por usuário"*
3. (l. 38) *"As listas cruas — `num_friends` e `daily_minutes`, sem o sufixo — têm 204 usuários."*
4. (l. 87 e 105) `from scratch.linear_algebra import Vector` — o alias `Vector = List[float]`
5. (l. 106) `from scratch.statistics import correlation, standard_deviation, mean`
6. (l. 124) *"é por isso que `alpha = mean(y) - beta * mean(x)`"*
7. (l. 177) `from scratch.statistics import de_mean`
8. (l. 215) `X = [[x] for x in num_friends_good]` (dentro do callout `scikit-learn`)
9. (l. 232) *"As trinta e poucas linhas desta seção"* e *"O $\beta$ difere no último dígito da representação — um único bit de mantissa, a menor diferença que um `float64` sabe expressar"*

### `02-usando-gradiente-descendente.qmd` (144 linhas)

| chunk | o que faz | importa de `scratch.` |
|---|---|---|
| `importa-fechada` (`include: false`) | funções, dados e o par fechado | `simple_linear_regression`: `error`, `sum_of_sqerrors`, `num_friends_good`, `daily_minutes_good`, `alpha`, `beta` |
| `mostra-fechada` | exibe `alpha_fechada, beta_fechada` | — |
| `treina-gradiente` (`warning: false`) | `random.seed(0)`, 10.000 épocas com `tqdm.trange`, dois `sum(... for ... in zip)`, `gradient_step`, 2 `assert`s | `gradient_descent`: `gradient_step` |
| `compara-metodos` | tabela fechada × gradiente | — |
| `fig-perda-por-epoch` | curva de perda, escala log | — |

Números afirmados: os dois valores da fórmula fechada; as diferenças **2,6 × 10⁻⁷** e
**2,1 × 10⁻⁸**; a perda inicial "perto de 1,2 × 10⁵"; "menos de 6 × 10⁴" no epoch 10;
"pouco depois do epoch 1.500 encosta", "por volta do 2.000 indistinguíveis";
"no epoch 2.000 ainda está 29,8 acima — 0,23% dela"; "no 5.000, 5 × 10⁻⁴";
"no último epoch, 5 × 10⁻¹²"; "doze ordens de grandeza".

Uma figura: `fig-perda-por-epoch`.

**Frases que deixam de ser verdade:**

1. (l. 9) *"Tudo isso vem de `scratch/simple_linear_regression.py`"*
2. (l. 34) *"as duas derivadas parciais aparecem soltas dentro do laço, em vez de embrulhadas numa função `linear_gradient`, e os gradientes são **somados** sobre os 203 pontos"* — a segunda metade continua verdadeira, a primeira precisa dizer que a soma virou redução de array
3. (l. 36) *"A perda de cada epoch vai para a barra de progresso e também para a lista `perdas`"* — some a barra, e `perdas` deixa de ser lista
4. (l. 42–43) `import random` / `import tqdm`
5. (l. 84, callout) *"`grad_a` e `grad_b` são exatamente `linear_gradient` [...] escrita por extenso"* — os dois nomes somem, vira um `grad` de dois elementos
6. (l. 84, callout) *"`gradient_step` é a mesma função de cinco linhas que o Capítulo 5 construiu"* — a versão de `scratch_np` é uma linha
7. (l. 122) *"A perda começa perto de $1{,}2 \times 10^5$"* — o palpite inicial mudou de gerador

### `03-maxima-verossimilhanca.qmd` (188 linhas)

| chunk | o que faz | importa de `scratch.` |
|---|---|---|
| `importa-mle` (`include: false`) | funções, dados, `alpha`, `beta`, `mean`, `math`, `plt` | `simple_linear_regression`, `statistics`: `mean` |
| `compara-verossimilhancas` | $S$ dos dois modelos | — |
| `verossimilhanca-inline` | produto em laço de `math.exp`; soma de logs | — |
| `verossimilhanca-underflow` | as duas $L$ com `!r` | — |
| `log-verossimilhanca-valores` | as duas $\log L$ e a diferença | — |

Nenhuma figura. Números afirmados: 13.197 e 19.670; $10^{-310}$; `0.0`;
$e^{-762{,}345}$; $10^{-331}$; −711,77; −762,35; 50,58; "203 densidades normais".
**Todos sobrevivem intactos** (medidos abaixo) — esta é a seção que menos muda.

**Frases que deixam de ser verdade:**

1. (l. 110) *"A primeira multiplica as 203 densidades normais, uma por ponto, exatamente como a fórmula do produto manda."* — continua verdadeira em espírito, mas a implementação passa a ser `np.prod`, e vale dizer por que o *underflow* não some com isso
2. (l. 88) `from scratch.statistics import mean`
3. `import math` — troca por `np`

---

## 2. O módulo `scratch_np/simple_linear_regression.py`

Código completo, pronto para salvar. As seis funções são **idênticas** às dos chunks da
11.1 (só os nomes das variáveis de teste levam `_`, como em
`scratch_np/k_nearest_neighbors.py`). Reexporta os quatro arrays de
`scratch_np.statistics` e expõe `alpha`/`beta` ajustados no import, como o Grus faz —
é daí que a 11.2 e a 11.3 importam tudo de um lugar só.

```python
"""Regressão linear simples com numpy — o que o capítulo 11 ensina.

A seção 11.1 escreve `predict`, `error`, `sum_of_sqerrors`, `least_squares_fit`,
`total_sum_of_squares` e `r_squared` inline; as seções 11.2 e 11.3 — e o
capítulo 12 — importam daqui O MESMO código. O módulo é o chunk salvo em
arquivo.

Como no Grus, o ajuste sobre os dados da DataSciencester acontece no import:
`alpha` e `beta` são os valores de mínimos quadrados sobre `num_friends_good` e
`daily_minutes_good`, e os quatro arrays de dados são reexportados daqui para
que a 11.2 e a 11.3 importem tudo de um lugar só. Os `assert`s no nível do
módulo são a suíte do próprio livro: o import é o teste.

Convenções: `x` e `y` têm forma (n,); `ddof=1` onde o Grus usa a fórmula
amostral (`standard_deviation`); as funções que devolvem escalar devolvem
`float`, não `np.float64`, para a saída na tela ser a do Python.
"""
from typing import Tuple

import numpy as np

from scratch_np.statistics import (num_friends, daily_minutes,
                                   num_friends_good, daily_minutes_good)


def predict(alpha: float, beta: float, x):
    """beta * x + alpha. Escalar -> float; array -> array."""
    return beta * x + alpha


def error(alpha: float, beta: float, x, y):
    """
    O erro ao prever beta * x + alpha
    quando o valor real é y
    """
    return predict(alpha, beta, x) - y


def sum_of_sqerrors(alpha: float, beta: float,
                    x: np.ndarray, y: np.ndarray) -> float:
    return float(np.sum(error(alpha, beta, x, y) ** 2))


def least_squares_fit(x: np.ndarray, y: np.ndarray) -> Tuple[float, float]:
    """
    Dados dois arrays x e y,
    encontra os valores de alpha e beta de mínimos quadrados
    """
    beta = np.corrcoef(x, y)[0, 1] * np.std(y, ddof=1) / np.std(x, ddof=1)
    alpha = y.mean() - beta * x.mean()
    return float(alpha), float(beta)


# Uma reta perfeita: y = 3x - 5. A igualdade é exata — ver o plano/seção 11.1.
_x = np.arange(-100, 110, 10)
_y = 3 * _x - 5
assert least_squares_fit(_x, _y) == (-5, 3)

alpha, beta = least_squares_fit(num_friends_good, daily_minutes_good)
assert 22.9 < alpha < 23.0
assert 0.9 < beta < 0.905


def total_sum_of_squares(y: np.ndarray) -> float:
    """a variação quadrática total dos y_i em torno da média deles"""
    return float(np.sum((y - y.mean()) ** 2))


def r_squared(alpha: float, beta: float,
              x: np.ndarray, y: np.ndarray) -> float:
    """
    a fração da variação em y capturada pelo modelo, que é igual a
    1 menos a fração da variação em y NÃO capturada pelo modelo
    """
    return 1.0 - (sum_of_sqerrors(alpha, beta, x, y) /
                  total_sum_of_squares(y))


rsq = r_squared(alpha, beta, num_friends_good, daily_minutes_good)
assert 0.328 < rsq < 0.330
```

**Verificado no scratchpad** (import limpo, os quatro `assert`s passam):

```
alpha 22.947552413468976   beta 0.9038659456058725   rsq 0.32910783778362984
```

Notas para quem implementa:

- `num_friends` e `daily_minutes` (os arrays **com** o outlier) entram no import só para serem reexportados — a 11.1 os usa no callout do outlier. Não são usados por nenhuma função do módulo; isso é deliberado, não sobra.
- O módulo importa `scratch_np.statistics`, **não** `scratch.` — logo **não** precisa de entrada em `REEXPORTA_DADOS` (`tests/test_scratch_np.py`). Quem faz a ponte com o Grus é o `scratch_np/statistics.py`, que já está registrado lá.
- Nenhuma função é estocástica, então nenhuma recebe `rng`.
- `Tuple` da `typing`, e não `tuple` minúsculo, para casar com `machine_learning.py` e `working_with_data.py` — ver §4.

---

## 3. Plano seção a seção

### 3.0 `index.qmd`

Sem alterações. Ver §1.

---

### 3.1 `01-o-modelo.qmd`

#### chunk `importa-dados` — **reescreve** (e deixa de ser invisível)

Motivo: os dados vêm de `scratch_np.statistics`, que já fecha as figuras que o módulo
do Grus desenha no import (verificado: `plt.get_fignums()` devolve `[]` depois do
import). Sem figura vazando, não há motivo para esconder o chunk — e nos capítulos 7,
8 e 10 já não sobrou nenhum `include: false`. O import passa a ser conteúdo: é ele que
mostra de onde vêm os dados.

Código novo (completo):

```{python}
#| label: importa-dados
import numpy as np
from scratch_np.statistics import (num_friends, daily_minutes,
                                   num_friends_good, daily_minutes_good)
```

Saída esperada: nenhuma.

Prosa afetada:

- antes: *"O primeiro passo, que você provavelmente já viu antes de chegar a este livro, é medir a **correlação**: um único número entre -1 e 1 que resume o quanto duas variáveis variam juntas. `scratch/statistics.py` já traz essa função pronta, junto com os dados da própria DataSciencester:"*
  → depois: *"O primeiro passo, que você provavelmente já viu antes de chegar a este livro, é medir a **correlação**: um único número entre -1 e 1 que resume o quanto duas variáveis variam juntas. O `numpy` já traz essa conta pronta, em `np.corrcoef`; os dados da própria DataSciencester vêm de `scratch_np/statistics.py`, que é onde as listas do livro-texto viram arrays:"*
- antes: *"São duas listas paralelas, uma entrada por usuário: `num_friends_good` guarda quantos amigos cada um tem, `daily_minutes_good` guarda quantos minutos por dia ele passa no site."*
  → depois: *"São dois arrays do mesmo tamanho, uma entrada por usuário: `num_friends_good` guarda quantos amigos cada um tem, `daily_minutes_good` guarda quantos minutos por dia ele passa no site."*

#### chunk `descreve-dados` — **reescreve**

Motivo: `len`/`mean` do Grus viram atributo e método do array.

```{python}
#| label: descreve-dados
num_friends_good.size, float(num_friends_good.mean()), float(daily_minutes_good.mean())
```

Saída esperada (medida): `(203, 6.876847290640394, 29.163300492610837)`

Prosa afetada: nenhuma frase muda por causa deste chunk (o parágrafo seguinte muda por
causa do `corrcoef` — abaixo). O `float(...)` é o mesmo idioma de
`content/cap09/02-exemplo-o-dataset-iris.qmd` (`pct_correct = float(acertos.mean())`):
sem ele a saída sairia `np.float64(6.876...)`.

#### chunk `correlacao-dados` — **reescreve**

Motivo: `correlation` do Grus vira `np.corrcoef`, que devolve a **matriz** — e a matriz
é a oportunidade de ensinar por que se escreve `[0, 1]`. O chunk mostra a matriz
inteira; o `[0, 1]` aparece no chunk seguinte e no `least_squares_fit`.

```{python}
#| label: correlacao-dados
np.corrcoef(num_friends_good, daily_minutes_good)
```

Saída esperada (medida):

```
array([[1.        , 0.57367921],
       [0.57367921, 1.        ]])
```

Prosa afetada — o parágrafo seguinte ganha a explicação da matriz:

- antes: *"São 203 usuários. Em média, cada um tem quase 7 amigos e passa cerca de 29 minutos por dia no site. E a correlação — pouco mais de 0,57 — diz que existe uma tendência real: mais amigos, mais tempo. Mas repare no que esse número **não** diz. [...]"*
  → depois: *"São 203 usuários. Em média, cada um tem quase 7 amigos e passa cerca de 29 minutos por dia no site. Repare que `np.corrcoef` não devolve um número, e sim a **matriz de correlação** das variáveis que recebeu — 2 × 2 aqui, porque são duas. Na diagonal está a correlação de cada variável consigo mesma, que é 1 por construção; fora dela, repetida nos dois cantos porque correlação é simétrica, está a que interessa. `[0, 1]` é como se pega esse canto, e é assim que o resto da seção vai escrevê-la. Pouco mais de 0,57 diz que existe uma tendência real: mais amigos, mais tempo. Mas repare no que esse número **não** diz. Ele não diz se um amigo a mais vale, em média, 10 segundos ou 10 minutos a mais de uso. Ele não dá uma equação. Para isso, precisamos de outra coisa: um modelo."*

#### chunk `correlacao-com-outlier` — **reescreve**

Motivo: mesmos dados, mesma conta, sem o `correlation` do Grus. Números
**determinísticos**: têm de bater com os do callout.

```{python}
#| label: correlacao-com-outlier
num_friends.size, float(np.corrcoef(num_friends, daily_minutes)[0, 1])
```

Saída esperada (medida): `(204, 0.24736957366478235)`

Prosa afetada:

- antes: *"As listas cruas — `num_friends` e `daily_minutes`, sem o sufixo — têm 204 usuários."*
  → depois: *"Os arrays crus — `num_friends` e `daily_minutes`, sem o sufixo — têm 204 usuários."*
- **Confirmado sem mudança:** *"Com ele dentro, 0,2474. Sem ele, 0,5737. **Um ponto em 204 derruba a correlação para 43% do valor**"* — medido: 0,24737 / 0,57368, razão 0,4312. O resto do callout não menciona código.

#### chunk `predict-error` — **reescreve**

Motivo: a fórmula é a mesma; o que muda é que ela passa a valer para um ponto **ou**
para o array inteiro, por *broadcasting*. É essa propriedade que dispensa o laço no
resto do capítulo, então ela merece ser dita.

```{python}
#| label: predict-error
def predict(alpha: float, beta: float, x):
    """beta * x + alpha. Escalar -> float; array -> array."""
    return beta * x + alpha

def error(alpha: float, beta: float, x, y):
    """
    O erro ao prever beta * x + alpha
    quando o valor real é y
    """
    return predict(alpha, beta, x) - y
```

Saída esperada: nenhuma.

Prosa afetada — parágrafo **novo**, logo depois do chunk (antes de *"Como escolher $\alpha$ e $\beta$?"*):

> Repare no nome do parâmetro: `x`, e não `x_i`. Nada nas duas funções pressupõe um
> único ponto — `beta * x + alpha` é a mesma linha para um número e para os 203 amigos
> de uma vez, por *broadcasting* ([seção 7.1](../cap07/01-explorando-seus-dados.qmd)).
> `predict(alpha, beta, 10)` responde por um usuário com dez amigos;
> `predict(alpha, beta, num_friends_good)` responde por todos, e devolve um array de
> 203 previsões. É essa propriedade que faz o resto do capítulo dispensar o laço sobre
> os pontos.

#### chunk `sum-of-sqerrors` — **reescreve**

Motivo: o `sum(... for ... in zip(x, y))` vira uma redução sobre o array de erros.

```{python}
#| label: sum-of-sqerrors
def sum_of_sqerrors(alpha: float, beta: float,
                    x: np.ndarray, y: np.ndarray) -> float:
    return float(np.sum(error(alpha, beta, x, y) ** 2))
```

Saída esperada: nenhuma.

Prosa afetada — parágrafo **novo**, logo depois do chunk:

> Três operações, nenhum laço: `error(alpha, beta, x, y)` devolve os 203 erros de uma
> vez, `** 2` eleva cada um ao quadrado e `np.sum` reduz tudo a um número. O `float()`
> no fim troca o escalar do `numpy` por um número do Python — só para a saída na tela
> ser `13196.6...`, e não `np.float64(13196.6...)`.

#### chunk `least-squares-fit` — **reescreve**

Motivo: `correlation`, `standard_deviation` e `mean` do Grus viram `np.corrcoef`,
`np.std(ddof=1)` e `.mean()`. **Sem `np.polyfit`, sem `np.linalg.lstsq`** — a fórmula
tem de continuar visível (spec, "a pergunta de desempate").

```{python}
#| label: least-squares-fit
from typing import Tuple

def least_squares_fit(x: np.ndarray, y: np.ndarray) -> Tuple[float, float]:
    """
    Dados dois arrays x e y,
    encontra os valores de alpha e beta de mínimos quadrados
    """
    beta = np.corrcoef(x, y)[0, 1] * np.std(y, ddof=1) / np.std(x, ddof=1)
    alpha = y.mean() - beta * x.mean()
    return float(alpha), float(beta)
```

O `from typing import Tuple` fica dentro do próprio chunk, exatamente onde está hoje —
o que sai é o `from scratch.linear_algebra import Vector` e o
`from scratch.statistics import correlation, standard_deviation, mean`. A função inteira
tem de caber num chunk só, começando na coluna zero (`test_nenhum_chunk_comeca_com_linha_indentada`).

Saída esperada: nenhuma.

Prosa afetada — parágrafo **novo**, entre o chunk e o `.conceito`:

> O `ddof=1` pede o desvio padrão **amostral**, o de denominador $n - 1$ que a
> [seção 7.6](../cap07/06-reescalonamento.qmd) apresentou — a mesma fórmula que o
> livro-texto usa. Aqui ele não chega a mudar o resultado: `beta` é uma **razão** de
> desvios padrão, e o denominador cancela, seja ele $n$ ou $n - 1$. Escrevê-lo mesmo
> assim mantém a linha igual à fórmula que ela representa, e evita que alguém a copie
> para um lugar onde o cancelamento não aconteça.

- **Confirmado sem mudança:** o `.conceito` inteiro (l. 118–122), inclusive *"calcula duas médias, uma correlação (que por baixo é uma covariância dividida por dois desvios padrão) e dois desvios padrão — e devolve a resposta exata"* e *"duas médias, uma covariância, uma variância, sem laço nenhum"*.
- antes (l. 124): *"é por isso que `alpha = mean(y) - beta * mean(x)`"*
  → depois: *"é por isso que `alpha = y.mean() - beta * x.mean()`"*

#### chunk `teste-least-squares` — **reescreve**

Motivo: a compreensão de lista vira `np.arange`. **A igualdade exata sobrevive** — foi
verificada, e não por sorte: ver a prosa nova.

```{python}
#| label: teste-least-squares
x = np.arange(-100, 110, 10)
y = 3 * x - 5

# Deveria encontrar que y = 3x - 5
assert least_squares_fit(x, y) == (-5, 3)
```

Saída esperada: nenhuma (o `assert` passa — medido).

Prosa afetada — parágrafo **novo**, logo depois do chunk:

> `np.arange(-100, 110, 10)` é a compreensão de lista do livro-texto escrita como
> array, e `3 * x - 5` aplica a reta aos 21 pontos de uma vez. Aqui a comparação é de
> **igualdade exata**, o que costuma ser má ideia com ponto flutuante — e neste caso
> específico funciona por um motivo que dá para apontar: `x` é simétrico em torno de
> zero, então `x.mean()` é exatamente `0.0` e `alpha` sai igual a `y.mean()`, que é
> `-5.0`; e a correlação de pontos perfeitamente colineares dá exatamente `1.0`, o que
> deixa `beta` igual à razão dos desvios padrão, `3.0` sem resto. Fora de um caso
> construído assim, o certo é comparar com `np.allclose`.

#### chunk `ajusta-dados-reais` — **mantém** (o código não muda)

Motivo: as duas linhas já são chamadas de função; os dois `assert` do Grus **continuam
valendo com `ddof=1`** (medido: `alpha = 22.947552413468976`, `beta = 0.9038659456058725`).

```{python}
#| label: ajusta-dados-reais
alpha, beta = least_squares_fit(num_friends_good, daily_minutes_good)
assert 22.9 < alpha < 23.0
assert 0.9 < beta < 0.905

alpha, beta
```

Saída esperada (medida): `(22.947552413468976, 0.9038659456058725)`

Prosa afetada: nenhuma. *"Isso dá $\alpha \approx 22{,}95$ e $\beta \approx 0{,}904$"*
continua exato.

#### chunk `fig-regressao-amigos-minutos` — **reescreve**

Motivo: `plt` deixa de vir do chunk escondido; os extremos viram um array de dois
elementos que passa por `predict` de uma vez.

```{python}
#| label: fig-regressao-amigos-minutos
#| fig-cap: "Modelo de regressão linear simples sobre os dados da DataSciencester"
from matplotlib import pyplot as plt

plt.scatter(num_friends_good, daily_minutes_good)

x_reta = np.array([num_friends_good.min(), num_friends_good.max()])
y_reta = predict(alpha, beta, x_reta)
plt.plot(x_reta, y_reta, color='black')

plt.title("Regressão linear simples")
plt.xlabel("# de amigos")
plt.ylabel("minutos por dia")
plt.show()
```

Saída esperada: a mesma figura de antes (mesmos pontos, mesma reta). `x_reta` é
`array([1., 49.])`.

Prosa afetada — uma frase **nova** no fim do parágrafo que já existe depois da figura
(l. 169), depois de *"...onde a maior parte dos dados está concentrada."*:

> A reta em si saiu de duas linhas: os dois extremos de `x` num array de dois
> elementos, e o mesmo `predict` que preveria um usuário aplicado aos dois de uma vez.

#### chunk `r-squared` — **reescreve**

Motivo: `de_mean` do Grus vira `y - y.mean()`, e o `sum(v ** 2 for v in ...)` vira
`np.sum(... ** 2)`.

```{python}
#| label: r-squared
def total_sum_of_squares(y: np.ndarray) -> float:
    """a variação quadrática total dos y_i em torno da média deles"""
    return float(np.sum((y - y.mean()) ** 2))

def r_squared(alpha: float, beta: float,
              x: np.ndarray, y: np.ndarray) -> float:
    """
    a fração da variação em y capturada pelo modelo, que é igual a
    1 menos a fração da variação em y NÃO capturada pelo modelo
    """
    return 1.0 - (sum_of_sqerrors(alpha, beta, x, y) /
                  total_sum_of_squares(y))

rsq = r_squared(alpha, beta, num_friends_good, daily_minutes_good)
assert 0.328 < rsq < 0.330

rsq
```

Saída esperada (medida): `0.32910783778362984` (e `total_sum_of_squares` = `19670.25408866995`,
`sum_of_sqerrors` = `13196.61929689318`).

Prosa afetada — uma frase **nova** abrindo o parágrafo que vem **depois** do chunk
(*"O R² dá cerca de 0,329."*):

- antes: *"O R² dá cerca de 0,329. Para entender de onde vêm os limites dessa métrica, [...]"*
  → depois: *"`y - y.mean()` é o mesmo `de_mean` da [seção 7.8](../cap07/08-reducao-de-dimensionalidade.qmd) aplicado a um vetor só: subtrai a média de todos os elementos de uma vez, por *broadcasting*. O R² dá cerca de 0,329. Para entender de onde vêm os limites dessa métrica, [...]"*
- **Confirmado sem mudança:** o `.callout-warning` *"0,329 não é 'bom' nem 'ruim'"* e os dois parágrafos sobre o modelo vazio.

#### callout `scikit-learn` — **reescreve** (fica, com texto atualizado)

Motivo: `X` deixa de ser lista de listas; a tabela precisa de nova medição; e a frase
sobre "um único bit de mantissa" não sobrevive à medição atual.

Bloco ```` ```python ```` (não executa):

```python
from sklearn.linear_model import LinearRegression

X = num_friends_good.reshape(-1, 1)   # (203, 1): uma linha por usuário
y = daily_minutes_good

modelo = LinearRegression().fit(X, y)
modelo.intercept_, modelo.coef_[0]   # alpha, beta

modelo.score(X, y)                    # R²
```

Tabela — **[RECALCULAR]**: medido no venv do host (numpy 2.5.2, scikit-learn 1.9.0);
o implementador **remede** e escreve o que sair, porque o último dígito de `beta`
depende da BLAS:

|  | à mão, nesta seção | `scikit-learn` 1.9 |
|---|---|---|
| $\alpha$ | `22.947552413468976` | `22.947552413468976` |
| $\beta$ | `0.9038659456058725` | `0.9038659456058721` |
| R² | `0.32910783778362984` | `0.32910783778362984` |

Prosa afetada dentro do callout:

- antes: *"$\alpha$ e o R² saem **idênticos**, bit a bit. O $\beta$ difere no último dígito da representação — um único bit de mantissa, a menor diferença que um `float64` sabe expressar, e nem sequer erro de método: é a ordem em que as somas de ponto flutuante foram feitas. As trinta e poucas linhas desta seção e a biblioteca que roda em produção no mundo inteiro estão calculando **a mesma coisa**."*
  → depois: *"$\alpha$ e o R² saem **idênticos**, bit a bit. O $\beta$ só discorda a partir do décimo sexto dígito significativo — uns poucos bits no último lugar da mantissa, o menor tipo de discordância que um `float64` sabe expressar, e nem sequer erro de método: é a ordem em que as somas de ponto flutuante foram feitas. As três dezenas de linhas desta seção e a biblioteca que roda em produção no mundo inteiro estão calculando **a mesma coisa**."*
- antes: *"`LinearRegression` não itera e não pede taxa de aprendizado: ela resolve o sistema de mínimos quadrados por álgebra linear direta (decomposição em valores singulares), o equivalente industrial da fórmula fechada que você acabou de escrever à mão."*
  → depois: *"`LinearRegression` não itera e não pede taxa de aprendizado: ela resolve o sistema de mínimos quadrados por álgebra linear direta (decomposição em valores singulares), que é a mesma solução fechada que você acabou de escrever — só numa forma que continua valendo para qualquer número de variáveis explicativas, a forma de que o [Capítulo 12](../cap12/index.qmd) vai precisar."*
- frase **nova**, logo **depois** do bloco de código e antes de *"Vale rodar e comparar [...] valor a valor:"*:
  > Repare no que **não** há aqui: nenhuma linha para montar `X`. A biblioteca pede a mesma convenção deste livro — uma matriz `(n, d)`, uma linha por observação —, e o `reshape(-1, 1)` só diz que os 203 números são 203 linhas de uma coluna.
- **Confirmado sem mudança:** o parágrafo do `.score()` e a frase final *"Os números da tabela foram medidos rodando o código, não estimados."*

#### Números a recalcular — 11.1

| onde | tipo | por que muda | valor |
|---|---|---|---|
| `descreve-dados` | saída | `float()` no lugar de `mean` do Grus | `(203, 6.876847290640394, 29.163300492610837)` — prosa ("203", "quase 7", "cerca de 29") **não muda** |
| `correlacao-dados` | saída | vira matriz 2 × 2 | `[[1., 0.57367921], [0.57367921, 1.]]` — "pouco mais de 0,57" **não muda** |
| `correlacao-com-outlier` | saída + callout | nenhuma (determinístico) | `(204, 0.24736957366478235)`; 0,2474 / 0,5737 / 43% **não mudam** |
| `teste-least-squares` | `assert` | igualdade exata sob risco | **passa**; mantém `== (-5, 3)` |
| `ajusta-dados-reais` | 2 `assert` + saída | `ddof=1` | `22.947552413468976`, `0.9038659456058725` — os dois `assert` **continuam valendo** |
| `r-squared` | `assert` + saída | `ddof` não entra aqui | `0.32910783778362984` — `assert` **continua valendo** |
| tabela do callout `scikit-learn` | prosa | versão de biblioteca/BLAS | **[RECALCULAR]** — medido: sklearn $\beta$ = `0.9038659456058721` |

**Semente:** nenhuma. A seção 11.1 não tem chunk estocástico.

---

### 3.2 `02-usando-gradiente-descendente.qmd`

#### chunk `importa-fechada` — **reescreve** (e deixa de ser invisível)

Motivo: passa a importar do módulo novo; `plt` sai daqui (vai para o chunk da figura,
como no cap. 9); e o import é justamente o que a prosa em volta está explicando, então
não faz sentido escondê-lo.

```{python}
#| label: importa-fechada
from scratch_np.simple_linear_regression import (
    error, sum_of_sqerrors,
    num_friends_good, daily_minutes_good,
    alpha as alpha_fechada, beta as beta_fechada,
)
```

Saída esperada: nenhuma.

Prosa afetada:

- antes: *"Tudo isso vem de `scratch/simple_linear_regression.py` — é o mesmo código, só sem repetir:"*
  → depois: *"Tudo isso vem de `scratch_np/simple_linear_regression.py` — é o mesmo código da seção anterior, salvo em arquivo, só sem repetir:"*
- antes: *"`alpha_fechada` e `beta_fechada` são os valores de mínimos quadrados que a seção anterior calculou com `least_squares_fit`. Eles são o **gabarito** desta seção: [...]"*
  → depois: *"`error` e `sum_of_sqerrors` não são redefinidas aqui: são as mesmas funções que você escreveu na [seção anterior](01-o-modelo.qmd). `alpha_fechada` e `beta_fechada` são os valores de mínimos quadrados que o próprio módulo calcula ao ser importado, com `least_squares_fit`, sobre estes mesmos dados. Eles são o **gabarito** desta seção: [...]"*

#### chunk `mostra-fechada` — **mantém**

Motivo: nada a mudar. Como `least_squares_fit` devolve `float`, a saída sai limpa.

Saída esperada (medida): `(22.947552413468976, 0.9038659456058725)`

#### chunk `treina-gradiente` — **reescreve**

Motivo: é o chunk central da seção. O laço sobre **épocas** fica (é o algoritmo); o laço
sobre os **203 pontos**, que hoje aparece duas vezes como `sum(... for ... in zip(...))`,
vira duas reduções de array. `random` da stdlib sai (proibido pela spec e por
`test_capitulo_numpy_usa_default_rng`); entra `np.random.default_rng(0)`. `tqdm` sai —
ver §4.

```{python}
#| label: treina-gradiente
import numpy as np
from scratch_np.gradient_descent import gradient_step

num_epochs = 10000
rng = np.random.default_rng(0)

guess = rng.random(2)          # começa com dois valores aleatórios

learning_rate = 0.00001
perdas = np.zeros(num_epochs)  # a perda a cada epoch

for epoch in range(num_epochs):
    alpha, beta = guess

    # os 203 erros deste palpite, de uma vez
    erros = error(alpha, beta, num_friends_good, daily_minutes_good)

    # as derivadas parciais da perda: em relação a alpha, em relação a beta
    grad = np.array([2 * erros.sum(),
                     2 * (erros * num_friends_good).sum()])

    # a perda deste epoch: os mesmos erros, ao quadrado e somados
    perdas[epoch] = np.sum(erros ** 2)

    # finalmente, atualiza o palpite
    guess = gradient_step(guess, grad, -learning_rate)

alpha, beta = guess.tolist()   # dois números do Python, não escalares do numpy
assert 22.9 < alpha < 23.0
assert 0.9 < beta < 0.905

alpha, beta
```

Saída esperada (medida): `(22.947552153373653, 0.9038659664340396)`.
Os dois `assert` **passam**. `rng.random(2)` com semente 0 dá
`array([0.63696169, 0.26978671])`. Remova o `#| warning: false` — ele existia só por
causa do `tqdm`.

Prosa afetada:

- antes: *"Muda também a forma de escrever o gradiente, e a mudança não move o destino: as duas derivadas parciais aparecem soltas dentro do laço, em vez de embrulhadas numa função `linear_gradient`, e os gradientes são **somados** sobre os 203 pontos em vez de terem a média tirada. Somar em vez de tirar a média multiplica o gradiente inteiro por 203 [...]"*
  → depois: *"Muda também a forma de escrever o gradiente, e a mudança não move o destino: as duas derivadas parciais aparecem soltas dentro do laço, num array de dois elementos, em vez de embrulhadas numa função `linear_gradient`; e cada uma é uma **redução** sobre os 203 pontos — somados, não com a média tirada. Somar em vez de tirar a média multiplica o gradiente inteiro por 203 [...]"* (o resto do parágrafo fica)
- antes: *"Aqui está o laço inteiro. A perda de cada epoch vai para a barra de progresso e também para a lista `perdas` — são essas dez mil perdas que desenham a figura que fecha esta seção."*
  → depois: *"Aqui está o laço inteiro. Ele percorre as **épocas**, que é o que o algoritmo faz; o que sumiu é o laço de dentro, sobre os 203 usuários — os dois gradientes e a perda agora são operações de array. A perda de cada epoch vai para o array `perdas` — são essas dez mil perdas que desenham a figura que fecha esta seção."*

**Callout novo** (`::: {.callout-warning}`), logo **depois** do chunk e **antes** do
`.callout-note` que já existe — é a primeira vez que este capítulo sorteia alguma coisa,
e a forma é a do cap. 9:

```
::: {.callout-warning}
## A semente não é opcional

`np.random.default_rng(0)` cria o gerador que sorteia o palpite inicial, e a semente
fica escrita aqui, ao lado do sorteio que ela produziu — não num estado global, em
algum lugar acima. Sem ela, cada execução parte de um `theta` diferente, e a tabela de
comparação e a curva de perda desta seção sairiam outras a cada vez que a página fosse
gerada.

Neste caso específico o **destino** não depende do começo: a soma dos erros ao
quadrado de um modelo linear é convexa, e qualquer ponto de partida desce para o mesmo
fundo — é o que a subseção seguinte mostra em números. O que depende do começo é o
**caminho**: a perda do primeiro epoch, quantos epochs até encostar na solução exata.
E é sobre o caminho que a prosa daqui para a frente afirma números.
:::
```

O `.callout-note` que já existe (l. 83–87) — **reescreve o primeiro parágrafo, mantém o
segundo**:

- antes: *"`grad_a` e `grad_b` são exatamente `linear_gradient` da [seção 5.5](../cap05/05-ajustando-modelos.qmd) escrita por extenso: a soma (não a média) das derivadas de `error(...) ** 2` sobre todos os pontos, uma parcial para cada parâmetro. `gradient_step` é a mesma função de cinco linhas que o Capítulo 5 construiu e que este livro reaproveita em todo capítulo que ajusta um modelo — aqui ela recebe a soma dos gradientes em vez da média, o que só reescala a taxa de aprendizado, não muda o destino."*
  → depois: *"`grad` é o `linear_gradient` da [seção 5.5](../cap05/05-ajustando-modelos.qmd) escrito por extenso: a soma (não a média) das derivadas de `error(...) ** 2` sobre todos os pontos, uma parcial para cada parâmetro. A parcial em relação a $\alpha$ é `2 * erros.sum()`; a parcial em relação a $\beta$ é a mesma soma com cada erro pesado pelo `x` do seu ponto — `2 * (erros * num_friends_good).sum()`, um produto elemento a elemento seguido de uma redução, no lugar do laço sobre os 203 usuários. `gradient_step` é a mesma função que o Capítulo 5 construiu e que este livro reaproveita em todo capítulo que ajusta um modelo, agora em arrays: `v + step_size * gradient`, com o passo negativo descendo, exatamente a convenção de lá. Aqui ela recebe a soma dos gradientes em vez da média, o que só reescala a taxa de aprendizado, não muda o destino."*
- **Confirmado sem mudança:** o segundo parágrafo (`learning_rate = 0.00001` contra o `0.001` da 5.5).

#### chunk `compara-metodos` — **mantém**

Motivo: o código já é formatação; `alpha`/`beta` e `alpha_fechada`/`beta_fechada` são
todos `float` do Python.

Saída esperada (medida):

```
            fórmula fechada gradiente descendente   diferença
alpha        22.947552413469        22.947552153374     2.6e-07
beta          0.903865945606         0.903865966434    2.08e-08
```

Prosa afetada: **nenhuma**. As diferenças medidas são `2.60095e-07` e `2.08282e-08` —
exatamente as mesmas ordens e os mesmos dois primeiros dígitos que o texto já afirma
(*"diferem em **2,6 × 10⁻⁷** no intercepto e **2,1 × 10⁻⁸** na inclinação"*, *"a
sétima casa decimal"*, *"a oitava"*). O implementador **confere** a saída e só mexe se
o número sair outro.

#### chunk `fig-perda-por-epoch` — **reescreve**

Motivo: `plt` passa a ser importado aqui (era do chunk escondido), e o eixo x vira
`np.arange`.

```{python}
#| label: fig-perda-por-epoch
#| fig-cap: "Soma dos erros ao quadrado a cada epoch do gradiente descendente, contra a perda da solução de fórmula fechada (escala log no eixo y)"
from matplotlib import pyplot as plt

perda_fechada = sum_of_sqerrors(alpha_fechada, beta_fechada,
                                num_friends_good, daily_minutes_good)

def pt_br(x: float) -> str:
    """13196.6 -> '13.196,6' — troca o ponto e a vírgula de lugar"""
    return f"{x:,.1f}".translate(str.maketrans(",.", ".,"))

plt.plot(np.arange(1, num_epochs + 1), perdas, label="gradiente descendente")
plt.axhline(perda_fechada, color="black", linestyle="--",
            label=f"fórmula fechada ({pt_br(perda_fechada)})")
plt.yscale("log")
plt.xlabel("epoch")
plt.ylabel("soma dos erros ao quadrado")
plt.legend()
plt.show()
```

Saída esperada: a figura; a legenda da tracejada diz `fórmula fechada (13.196,6)`
(`perda_fechada = 13196.61929689318`, medido).

Prosa afetada (os dois parágrafos depois da figura):

- antes: *"A perda começa perto de $1{,}2 \times 10^5$: `theta` nasceu como dois números sorteados entre 0 e 1, e uma reta dessas erra feio nos 203 pontos."*
  → depois: *"A perda começa perto de $1{,}6 \times 10^5$: `theta` nasceu como dois números sorteados entre 0 e 1, e uma reta dessas erra feio nos 203 pontos."* — **[RECALCULAR]**, medido `160785.57` no epoch 1
- **Confirmado sem mudança:** *"Os dez primeiros epochs sozinhos já a derrubam para menos de $6 \times 10^4$"* (medido: `56691.99` no epoch 10) e *"pouco depois do epoch 1.500 a curva encosta na tracejada; por volta do 2.000 as duas ficam indistinguíveis"* (excesso de 1,43% no 1.500 e 0,23% no 2.000).
- antes: *"No epoch 2.000, a perda ainda está 29,8 acima da perda da fórmula fechada — 0,23% dela, menos de um pixel nesta escala. No 5.000, a distância é de $5 \times 10^{-4}$; no último epoch, de $5 \times 10^{-12}$. Os oito mil epochs do trecho reto à direita não estão parados: estão limpando doze ordens de grandeza que nenhum eixo poderia mostrar junto com a queda inicial."*
  → depois: *"No epoch 2.000, a perda ainda está 30,3 acima da perda da fórmula fechada — 0,23% dela, menos de um pixel nesta escala. No 5.000, a distância é de $5 \times 10^{-4}$; no último epoch, de $4 \times 10^{-12}$. Os oito mil epochs do trecho reto à direita não estão parados: estão limpando quase treze ordens de grandeza que nenhum eixo poderia mostrar junto com a queda inicial."*
- **Confirmado sem mudança:** *"Repare, por fim, que a curva se aproxima da tracejada **sempre por cima**, sem nunca cruzá-la."* e o `.conceito` inteiro que fecha a seção, e o parágrafo final sobre os capítulos 12/13.

#### callout `Na prática: diferenciação automática` — **reescreve uma frase**

- antes: *"`grad_a` e `grad_b` acima foram derivadas à mão: alguém (o Grus, neste caso) pegou `sum_of_sqerrors`, aplicou a regra da cadeia em relação a cada parâmetro, e transcreveu o resultado em código."*
  → depois: *"As duas parciais de `grad` acima foram derivadas à mão: alguém (o Grus, neste caso) pegou `sum_of_sqerrors`, aplicou a regra da cadeia em relação a cada parâmetro, e transcreveu o resultado em código."*
- antes: *"e é literalmente a mesma ideia que `grad_a` e `grad_b` calculam aqui, só que mecanizada [...]"*
  → depois: *"e é literalmente a mesma ideia que as duas linhas de `grad` calculam aqui, só que mecanizada [...]"*
- **Confirmado sem mudança:** o resto do callout (nada nele fala de listas ou de velocidade).

#### Números a recalcular — 11.2

| onde | tipo | por que muda | valor |
|---|---|---|---|
| `mostra-fechada` | saída | — | `(22.947552413468976, 0.9038659456058725)` |
| `treina-gradiente` | 2 `assert` + saída | sorteio novo (`default_rng(0)` no lugar de `random.seed(0)`) | `(22.947552153373653, 0.9038659664340396)`; os dois `assert` **passam** |
| `compara-metodos` | saída + prosa | sorteio novo | diferenças `2.6e-07` e `2.08e-08` — a prosa **não muda** |
| perda do epoch 1 | prosa | ponto de partida novo | `160785.57` → "$1{,}6 \times 10^5$" (era $1{,}2 \times 10^5$) |
| perda do epoch 10 | prosa | ponto de partida novo | `56691.99` → "menos de $6 \times 10^4$" **continua** |
| excesso no epoch 2.000 | prosa | ponto de partida novo | `30,34` (0,2299%) → "30,3 acima — 0,23% dela" |
| excesso no epoch 5.000 | prosa | ponto de partida novo | `5,3 × 10⁻⁴` → "$5 \times 10^{-4}$" **continua** |
| excesso no epoch 10.000 | prosa | ponto de partida novo | `3,64 × 10⁻¹²` → "$4 \times 10^{-12}$" (era $5 \times 10^{-12}$) |
| legenda da figura | saída | — | `13.196,6` |

**Semente:** `np.random.default_rng(0)` em `treina-gradiente` — a única do capítulo.
Justificada no callout *A semente não é opcional*, logo abaixo do chunk. A semente 0 é a
mesma que o livro-texto usa (`random.seed(0)`), então a correspondência com o Grus fica
óbvia mesmo com o gerador trocado.

---

### 3.3 `03-maxima-verossimilhanca.qmd`

Esta é a seção com menos mudança de número: **todos os valores da prosa sobrevivem
intactos**, porque nada aqui depende de sorteio e a conta é a mesma.

#### chunk `importa-mle` — **reescreve** (e deixa de ser invisível)

Motivo: módulo novo; `mean` do Grus vira `.mean()`; `math` vira `np`; `plt` sai (esta
seção não tem figura).

```{python}
#| label: importa-mle
import numpy as np
from scratch_np.simple_linear_regression import (
    error, sum_of_sqerrors, num_friends_good, daily_minutes_good,
    alpha, beta,
)
```

Saída esperada: nenhuma.

Prosa afetada — a frase que introduz o chunk:

- antes: *"A [seção 11.1](01-o-modelo.qmd) já comparou o modelo de mínimos quadrados com o modelo de referência mais simples possível — sempre prever a média de `y`, ignorando `x` — para calcular R². Vamos reaproveitar essa comparação aqui:"*
  → depois: *"[...] para calcular R². Vamos reaproveitar essa comparação aqui — o ajuste e as duas funções vêm do módulo da seção 11.1, sem repetir código:"*

#### chunk `compara-verossimilhancas` — **reescreve**

Motivo: só o `mean` do Grus sai.

```{python}
#| label: compara-verossimilhancas
S_minimos_quadrados = sum_of_sqerrors(alpha, beta,
                                      num_friends_good, daily_minutes_good)
S_sempre_a_media = sum_of_sqerrors(daily_minutes_good.mean(), 0,
                                   num_friends_good, daily_minutes_good)

S_minimos_quadrados, S_sempre_a_media
```

Saída esperada (medida): `(13196.61929689318, 19670.25408866995)`

Prosa afetada: **nenhuma**. *"soma cerca de 13.197 [...] soma cerca de 19.670 — a mesma
variação total que a seção 11.1 chamou de `total_sum_of_squares`"* continua exato
(`total_sum_of_squares(daily_minutes_good)` = `19670.25408866995`, medido na 11.1).

#### chunk `verossimilhanca-inline` — **reescreve**

Motivo: o produto em laço de `math.exp` vira `np.prod` sobre o array de densidades; a
soma de logs vira `np.sum`. **Verificado que o produto vetorizado dá exatamente o mesmo
`float` do produto em laço** (`7.62654839154014e-310`, bit a bit) — logo a lição sobre
*underflow* continua funcionando, que é o ponto inteiro da subseção.

```{python}
#| label: verossimilhanca-inline
def verossimilhanca(alpha: float, beta: float, x, y, sigma: float) -> float:
    """L(alpha, beta | dados, sigma): o produto das densidades normais."""
    e = error(alpha, beta, x, y)
    return float(np.prod(np.exp(-e ** 2 / (2 * sigma ** 2))
                         / (np.sqrt(2 * np.pi) * sigma)))

def log_verossimilhanca(alpha: float, beta: float, x, y, sigma: float) -> float:
    """log L: a mesma conta, com a soma dos logs no lugar do produto."""
    e = error(alpha, beta, x, y)
    return float(np.sum(-e ** 2 / (2 * sigma ** 2)
                        - np.log(np.sqrt(2 * np.pi) * sigma)))
```

Saída esperada: nenhuma.

Prosa afetada:

- antes: *"A primeira multiplica as 203 densidades normais, uma por ponto, exatamente como a fórmula do produto manda."*
  → depois: *"A primeira calcula as 203 densidades normais de uma vez e as multiplica com `np.prod`, exatamente como a fórmula do produto manda. Vetorizar não muda nada aqui: `np.prod` multiplica os 203 fatores um a um, na ordem em que estão, como o laço faria — o que a vetorização troca é o laço, não a aritmética, e é por isso que o problema que vem a seguir acontece exatamente do mesmo jeito."*
- **Confirmado sem mudança:** *"A segunda usa $\log(ab) = \log a + \log b$ [...] O número minúsculo nunca chega a existir na memória."*

#### chunk `verossimilhanca-underflow` — **reescreve**

Motivo: só o `mean` sai.

```{python}
#| label: verossimilhanca-underflow
sigma = 8.0
media = daily_minutes_good.mean()

L_ajustado = verossimilhanca(alpha, beta,
                             num_friends_good, daily_minutes_good, sigma)
L_media = verossimilhanca(media, 0,
                          num_friends_good, daily_minutes_good, sigma)

print(f"L  (mínimos quadrados) = {L_ajustado!r}")
print(f"L  (sempre a média)    = {L_media!r}")
```

Saída esperada (medida — idêntica à de hoje):

```
L  (mínimos quadrados) = 7.62654839154014e-310
L  (sempre a média)    = 0.0
```

Prosa afetada: nenhuma.

#### chunk `log-verossimilhanca-valores` — **mantém**

Motivo: o código já é só chamada e formatação.

Saída esperada (medida — idêntica à de hoje):

```
log L (mínimos quadrados) = -711.7697
log L (sempre a média)    = -762.3450
diferença                 = 50.5753
```

Prosa afetada: **nenhuma**. O `.callout-warning` *O produto certo é o número errado*
sobrevive palavra por palavra — conferidos um a um: "casa de $10^{-310}$" (é
`7,63 × 10⁻³¹⁰`), "abaixo de $2{,}2 \times 10^{-308}$", "exatamente `0.0`",
"$e^{-762{,}345}$" (é `−762.3450`), "perto de $10^{-331}$" (é `10^-331,08`),
"−711,77 e −762,35", "a diferença de 50,58 intacta" (é `50.5753`), "203 densidades
normais". A referência ao [*underflow* do Capítulo 10](../cap10/02-um-filtro-mais-sofisticado.qmd)
também continua de pé: aquela seção mantém o título *O problema do underflow* e o
argumento das milhares de probabilidades por palavra.

#### callout `Na prática: o que se faz com isso` — **mantém**

Motivo: não descreve o nosso código, e sim `LinearRegression`, `statsmodels` e a
suposição de normalidade. Nada nele fala de listas ou de velocidade. Conferido.

#### Números a recalcular — 11.3

| onde | tipo | por que muda | valor |
|---|---|---|---|
| `compara-verossimilhancas` | saída + prosa | nada muda | `(13196.61929689318, 19670.25408866995)` — "13.197" e "19.670" **continuam** |
| `verossimilhanca-underflow` | saída + callout | nada muda | `7.62654839154014e-310` e `0.0` — **continuam** |
| `log-verossimilhanca-valores` | saída + callout | nada muda | `-711.7697`, `-762.3450`, `50.5753` — **continuam** |

**Semente:** nenhuma. A seção 11.3 não tem chunk estocástico.

---

## 4. Riscos e decisões

**1. `assert least_squares_fit(x, y) == (-5, 3)` sobrevive — e por um motivo, não por sorte.**
Medido: `least_squares_fit(np.arange(-100, 110, 10), 3 * x - 5)` devolve exatamente
`(-5.0, 3.0)`. `np.corrcoef` dá exatamente `1.0` para pontos colineares, a razão dos
desvios padrão dá exatamente `3.0`, e `x.mean()` é exatamente `0.0` porque o `arange` é
simétrico. A armadilha 6 do briefing (igualdade exata de float) **não se aplica aqui**,
e o plano explica isso na prosa em vez de esconder. Se, ainda assim, o `assert` falhar
na máquina do implementador, a correção é `assert np.allclose(least_squares_fit(x, y), (-5, 3))`
**com a frase da prosa ajustada** — e o desvio registrado no fim deste plano.

**2. Os três `assert`s do Grus com `ddof=1`.** Medidos e válidos: `alpha` = 22,9476
(dentro de 22.9–23.0), `beta` = 0,90387 (dentro de 0.9–0.905), `rsq` = 0,32911 (dentro
de 0.328–0.330). Nenhum `assert` precisa ser afrouxado. Como `beta` é uma razão de
desvios, `ddof` cancela; `ddof=1` fica por fidelidade à fórmula amostral, e a prosa
diz isso.

**3. `tqdm` sai do `treina-gradiente`. Decisão, com medida.** As 10.000 épocas
vetorizadas rodam em **0,055 s** no host. Uma barra de progresso que termina antes de
ser desenhada não informa nada e ainda obriga o chunk a carregar `#| warning: false`
para esconder a saída dela em `stderr`. O laço vira `for epoch in range(num_epochs)`, o
que também dá o índice para escrever direto em `perdas[epoch]`. **Nenhuma frase da
seção afirma tempo** — a única menção à barra de progresso é a que este plano reescreve
(armadilha 9: não se afirma velocidade sem medida, e aqui não há afirmação nenhuma a
fazer). `t.set_description` e o nome `loss` desaparecem junto; a perda passa a ser
`perdas[epoch] = np.sum(erros ** 2)`, reaproveitando os erros já calculados em vez de
chamar `sum_of_sqerrors` de novo. `sum_of_sqerrors` continua importado — é ele que
calcula `perda_fechada` no chunk da figura.

**4. Os `include: false` somem nos três arquivos.** Eles existiam por causa das figuras
que `scratch.statistics` desenha no nível do módulo. `scratch_np/statistics.py` já faz
`plt.close("all")` no import — verificado: `plt.get_fignums()` devolve `[]` logo depois
de `import scratch_np.statistics`. E nos capítulos 7, 8 e 10 já reescritos não sobrou
nenhum `include: false`. Consequência boa: o leitor passa a ver de onde vêm os dados e
as funções, que é justamente o que as três seções explicam em prosa.

**5. `perdas` vira `np.zeros(num_epochs)`, não lista.** Pré-alocar é o idioma de array
e deixa a figura receber o array direto. Nenhum ganho de tempo relevante (10.000
`append` não custam nada) — a razão é de forma, e a prosa acompanha ("o array `perdas`").

**6. `Tuple` da `typing`, não `tuple` minúsculo.** A spec escreve o contrato como
`-> tuple[float, float]`; `scratch_np/machine_learning.py` e
`scratch_np/working_with_data.py` usam `Tuple` da `typing`, e é o que o `.qmd` de hoje
usa. Mantive `Tuple` por coerência com o pacote e com o texto que já está no livro — o
que o contrato fixa é o nome, os parâmetros e o que a função devolve, e isso não muda.
Se o revisor preferir o built-in, é uma troca mecânica em dois arquivos.

**7. `predict` e `error` sem anotação de retorno.** É a forma que
`scratch_np/probability.py` já usa para as funções que aceitam escalar ou array
(`def normal_cdf(x, mu=0, sigma=1):` com a docstring dizendo *"Escalar -> float;
array -> array"*). O contrato da spec (`-> np.ndarray | float`) descreve o
comportamento, e a docstring o diz em português.

**8. O único laço que fica é o das épocas.** Nenhum outro laço sobre pontos sobrevive no
capítulo. Não há subida de gradiente aqui (o passo é `-learning_rate`), então
`tests/test_gradiente.py` não pede registro nenhum.

**9. Nada de `scipy`, `np.linalg.lstsq` ou `np.polyfit`.** A fórmula fechada é o assunto
da 11.1: escondê-la atrás de um resolvedor destruiria a seção. `np.corrcoef` é
aceitável pela regra da spec — correlação é assunto de Bases 3, assumido, e não é o que
o capítulo ensina.

**10. O cap. 12 ainda importa `scratch.simple_linear_regression`.**
`content/cap12/05-qualidade-do-ajuste.qmd:24` faz
`from scratch.simple_linear_regression import total_sum_of_squares`. Isso **não é
problema deste capítulo**: o cap. 12 ainda não está em `CAPITULOS_NUMPY`, e o módulo
novo já expõe `total_sum_of_squares` com a assinatura do contrato, pronta para quando a
vez dele chegar. **Não edite `content/cap12/`.**

**11. Notebook de aula (um kernel só).** A 11.1 define `alpha`/`beta` locais; a 11.2 os
importa como `alpha_fechada`/`beta_fechada` e depois rebate `alpha`/`beta` com o
resultado do gradiente; a 11.3 os reimporta do módulo (voltando aos valores fechados,
que é o que ela quer). Confira a ordem executando `executar-notebooks.py cap11`. Os
nomes `x` e `y` de `teste-least-squares` (11.1) não são usados por nenhum chunk
posterior — no módulo eles são `_x`/`_y`, para não serem exportados.

**12. A tabela do callout `scikit-learn` é a única coisa dependente de ambiente.** O
último dígito do `beta` da biblioteca mudou entre a medição antiga
(`...724`) e a de hoje (`...721`). É por isso que a prosa nova não afirma "um único bit"
e sim "uns poucos bits no último lugar da mantissa". O implementador **remede** os três
pares no venv e escreve o que sair.

---

## 5. Verificação

```bash
export MPLBACKEND=Agg PYTHONHASHSEED=0

# o módulo sozinho — os quatro asserts rodam no import
.venv/bin/python -c "import scratch_np.simple_linear_regression as m; print(m.alpha, m.beta, m.rsq)"

.venv/bin/python scripts/gerar-notebooks.py            # regenera os 17
.venv/bin/python scripts/executar-secoes.py 11         # cada .qmd num kernel próprio
.venv/bin/python scripts/executar-notebooks.py cap11   # o capítulo num kernel só
.venv/bin/pytest tests/ -q
```

**Só depois dos quatro verdes**, em `tests/test_scratch_np.py`:

```python
CAPITULOS_NUMPY: set[int] = {6, 7, 8, 9, 10, 11}
```

`PYTHON_PURO_PERMITIDO` e `RANDOM_PERMITIDO` **não ganham entrada nenhuma** neste
capítulo: a expressão "Python puro" não aparece em nenhum dos três `.qmd` (conferido), e
depois da reescrita não sobra `random.` da stdlib nem `np.random.seed` — o único sorteio
é `np.random.default_rng(0)`, em `02-usando-gradiente-descendente.qmd`.
`REEXPORTA_DADOS` também não muda: o módulo novo importa `scratch_np.statistics`, não
`scratch.`.

**Arquivos do commit** (`git add` caminho a caminho, nunca `-A`):

```
content/cap11/01-o-modelo.qmd
content/cap11/02-usando-gradiente-descendente.qmd
content/cap11/03-maxima-verossimilhanca.qmd
scratch_np/simple_linear_regression.py
notebooks/cap11-regressao-linear-simples.ipynb
tests/test_scratch_np.py
docs/superpowers/plans/2026-09-06-numpy-cap11.md   # só se houver "Desvios na implementação"
```

`content/cap11/index.qmd` **não entra** — nada nele muda.

Mensagem: `feat(cap11): reescrita em numpy — fórmula fechada, gradiente vetorizado e verossimilhança`

---

## Desvios na implementação

*(2026-09-07, implementador. Ambiente: host, numpy 2.5.2 / scikit-learn 1.9.0,
`MPLBACKEND=Agg PYTHONHASHSEED=0`. Os quatro comandos de verificação verdes;
`pytest`: 52 passed com `CAPITULOS_NUMPY = {6, 7, 8, 9, 10, 11}`.)*

**Nenhum `assert` foi afrouxado e nenhum número do plano saiu diferente.** Os
sete `assert`s do capítulo (módulo e chunks) passam como estão; em particular
`assert least_squares_fit(x, y) == (-5, 3)` fechou com igualdade exata — o
plano A do §4.1 valeu, o plano B (`np.allclose`) não foi necessário. Conferido
na saída real: `x.mean()` é `0.0`, `np.corrcoef` dá `1.0`, a razão dos desvios
dá `3.0`. Todos os valores das três tabelas de "números a recalcular" foram
lidos das células executadas (`executar-secoes.py 11` e um despejo das saídas
do notebook) e batem dígito a dígito com os do plano: `(203, 6.8768…, 29.1633…)`,
`0.57367921`, `(204, 0.24736957366478235)`, `(22.947552413468976,
0.9038659456058725)`, `0.32910783778362984`; gradiente `(22.947552153373653,
0.9038659664340396)`, diferenças `2.6e-07` / `2.08e-08`, perda no epoch 1 =
`160785.57`, excesso no 2.000 = `30.34` (0,2299%), no 10.000 = `3.64e-12`;
`(13196.61929689318, 19670.25408866995)`, `7.62654839154014e-310`, `0.0`,
`-711.7697`, `-762.3450`, `50.5753`. A tabela do callout `scikit-learn`
remedida deu exatamente o que o plano previa (`intercept_` e `score` idênticos;
`coef_[0] = 0.9038659456058721`).

Quatro desvios, todos de prosa, nenhum de código nem de número:

1. **`01-o-modelo.qmd`, parágrafo "Como escolher α e β?"** — o texto antigo
   escrevia `x_i`, `y_i`, `x_1`, `x_2` em fonte de código. Depois da reescrita o
   código não tem mais nenhum nome desses (o parágrafo novo logo acima diz
   justamente "`x`, e não `x_i`"), então eles viraram símbolos matemáticos
   ($x_i$, $y_i$, $x_1$, $x_2$). O plano não flagrou; a regra do revisor
   ("nome citado que o código não tem") pegaria.

2. **`01-o-modelo.qmd`, parágrafo "Sem passar pela conta exata"** — além do
   `alpha = y.mean() - beta * x.mean()` que o plano pedia, a mesma frase seguia
   com "a previsão aumenta `correlation(x, y)` desvios padrão de `y`".
   `correlation` era a função do Grus e não existe mais no capítulo; virou
   `np.corrcoef(x, y)[0, 1]`, espelhando a linha do chunk.

3. **`01-o-modelo.qmd`, parágrafo depois de `r-squared`** — "o que corresponde
   a `alpha = mean(y)` e `beta = 0`" virou `alpha = y.mean()`, pelo mesmo
   motivo do item 2. O plano reescreveu a abertura desse parágrafo mas não
   essa expressão.

4. **Callout `scikit-learn`, frase sobre o β** — o plano propunha "uns poucos
   bits no último lugar da mantissa, o menor tipo de discordância que um
   `float64` sabe expressar". Medido: a diferença é de **exatamente 4 ULPs**
   (`abs(diff) / np.spacing(beta) == 4.0`), então "o menor tipo de
   discordância" não seria verdade (o menor é 1 ULP). O texto publicado diz
   "quatro unidades no último lugar da mantissa, uma discordância que só existe
   porque um `float64` tem um último lugar". O resto da frase é o do plano.

Duas observações que não são desvios:

- O chunk `treina-gradiente` mede **0,055 s** para as 10.000 épocas, como o
  plano previa (§4.3); nenhuma frase do capítulo afirma tempo.
- `content/cap11/index.qmd` não foi tocado, como o plano manda; o `beta`
  medido (0,9039) sustenta o "0,9 minuto por amigo" da abertura.
